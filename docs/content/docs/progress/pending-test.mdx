---
title: 待测试
description: 影策已实现功能的手工验证清单与未确认边界
---

# 待测试

## 前端 UI 技术栈收敛（2026-09-03）

- [x] 首批将通用空、加载、错误状态收敛为 `components/ui/pc` 下唯一的 `WorkspaceState` 组件族，21 个页面与功能入口已切换到新边界；未修改 DOM 类名、业务条件、接口或数据。
- [x] 删除无人使用的第二套 `EmptyState` 及其 112 行样式，并新增静态门禁，阻止旧布局导入路径和重复公共空态重新出现。
- [x] UI 设计系统补充 Ant Design、项目公共组件、Tailwind/页面 CSS 的职责边界；画布和创作页具有业务引导语义的专属空态继续保留。
- [x] 第二批将页面、页头、列表工具栏、表格/网格容器和分页拆入公共 UI 层，16 个用户端入口已迁移；旧 layout 路径仅作为 Admin 兼容转发层，未修改 Admin 页面。
- [x] 页面骨架保持原 DOM、类名、筛选展开和分页算法，新增静态门禁阻止非 Admin 代码重新依赖兼容路径。
- [x] 第三批不新增全局 Button 包装层，将任务网格卡与列表行重复的查看/失败重试操作收敛为任务域共享组件，统一内容审核禁重试和任务级 loading 判断。
- [x] 项目卡、素材卡、技能卡、画布节点和影视工作台具有不同点击面与交互语义，继续保留领域组件，不强行合并为单一 Card。
- [ ] 在任务列表和网格视图分别打开详情、重试普通失败任务，并检查内容审核失败任务不可重试；确认 tooltip、键盘焦点、loading 与原布局一致。
- [x] 第四批将 Eagle 与项目素材页重复的根目录/多级文件夹路径收敛为中性 `PathBreadcrumb`，保留原按钮样式和业务回调，并补齐项目素材路径导航语义。
- [ ] 分别在 Eagle 和项目素材页进入三层以上文件夹，用鼠标和键盘逐级返回中间目录与根目录；确认路径截断、分隔符、焦点和当前目录内容一致。
- [x] 第五批将素材库与技能库重复的新增卡结构收敛为 `LibraryCreateCard`，保留页面级尺寸、比例、hover、权限判断和点击业务。
- [ ] 在素材库点击“新增素材”、在技能库点击“安装技能”，确认弹窗、键盘焦点、卡片等高和桌面/窄屏网格表现与改造前一致。
- [x] 第六批将 3D 导演台、节点编辑器与时间线工具重复的全屏加载壳收敛为画布域 `CanvasWorkspaceLoadingOverlay`，保留主题化/中性背景、层级、文案和懒加载时机，并统一状态语义。代码回滚点：`e3eadc3`。
- [ ] 分别首次打开角色参考、绘图/文本编辑、肖像排查、字幕/时间线和 3D 导演台，确认慢网下加载占位的背景、文案、层级与弹窗首屏衔接正常。
- [x] 第七批将分镜脚本、节点搜索、分享/导入、素材库、图片工具、任务/预览和上传/节点信息的重复加载遮罩收敛为 `CanvasDialogLoadingOverlay`，保留原样式、文案、懒加载时机和两个可关闭入口。代码回滚点：`f05bf2e`。
- [ ] 在慢网或 DevTools 限速下首次打开节点搜索、分镜脚本、分享、两类导入、两类素材库、图片工具、任务详情、媒体预览和上传；确认遮罩、文案与首屏衔接正常，且搜索/脚本加载期可关闭。
- [x] 第八批删除仅有定义与导出、没有产品调用方的上传/媒体公共组件族及 459 行死样式；真实上传、媒体预览、数据和业务合同均未修改。PC 公共 CSS 分包从约 33.78 kB 降至 23.09 kB。代码回滚点：`6d3e6b7`。
- [x] 第九批将普通节点设置/配置编排与多角度/表情工作区重复的可关闭加载卡收敛为 `CanvasInlinePanelLoading`，保留原高度、样式、拖动与生成回调，并统一关闭按钮的可访问名称。代码回滚点：`a39cf7d`。
- [ ] 在慢网下首次打开普通节点设置、配置编排、多角度编辑和表情工作区，确认加载卡不跳高、跟随节点拖动，且鼠标与键盘均可关闭。
- [ ] 登录后抽查首页、项目、素材、任务、插件、技能、钱包、设置和画布的加载、空、错误状态，确认标题、说明、图标、重试按钮、紧凑模式与键盘焦点保持原表现。
- [x] 完成 PC 公共模块、公共样式和剩余画布加载态的收尾审计：现存公共组件均有真实调用，未发现静态孤儿选择器；其余相似结构因坐标、尺寸、关闭语义或任务状态不同而保留，不再增加无收益抽象。
- [x] 完成未登录本地浏览器验收：公开四页与登录页在 1440×900、首页与认证入口在 390×844 均无横向溢出或页面错误；移动菜单、登录/注册切换、桌面品牌视频、移动海报降级、减少动态效果和九个受保护入口的 `next` 跳转均符合合同。登录后验收仍按上方各业务清单执行。
- [x] 修复本地登录接口 502：后端改由 macOS `launchd` 常驻托管，继续读取既有数据库并禁用自动迁移；强制重启后直连、IP/localhost 前端代理均为 200，登录表单无配置错误且填写后按钮可启用，既有账号已完成一次真实登录并生成新会话。

## 公开品牌官网与内容发布（2026-09-03）

- [x] 根路径升级为完整品牌首页，并新增产品、作品、关于三个公开内页；创作、项目、画布与管理后台保持原路径，`/home` 补上登录保护。
- [x] 首页复用品牌设置中的登录背景视频与海报，也支持官网独立视频 URL；移动端、减少动态效果或视频错误时自动降级，不阻断官网与登录。
- [x] 管理后台新增“官网内容”，支持首页章节、作品、B 站/外部链接、SEO 与站点链接编辑；草稿保存、发布、默认草稿彼此独立。
- [x] 首页补齐片场风格交互：首屏分层入场、章节滚动揭示、按钮光带/箭头/按下反馈、导航与文字链接下划线、卡片与流程联动；键盘焦点可见并尊重系统“减少动态效果”。
- [x] 官网配置沿用 `system_settings`，未新增迁移；服务端使用修订 CAS、URL/长度校验、管理审计与公开 ETag，公开接口只返回已发布版本。
- [x] 后端官网 service/handler 测试、前端公开入口/草稿发布/视频降级专项测试、TypeScript 检查通过；完整实施与回滚记录见 `docs/design/public-brand-site-20260903.mdx`。
- [ ] 使用管理员账号保存但不发布一版明显不同的文案，确认无痕窗口仍看到旧版；再发布并刷新，确认首页和对应内页更新且旧修订无法覆盖。
- [ ] 公开官网路由恢复后，分别填写 B 站作品 URL、HTTPS 封面/视频 URL 和站内部署链接，检查新窗口、安全跳转、封面裁切、视频失败降级与内容可读性。
- [ ] 公开官网路由恢复后，在 1366×768、1440×900、390×844 和 430×932 视口检查首页、产品、作品、关于的导航、标题换行、作品网格、页脚、移动菜单与横向溢出。
- [ ] 公开官网路由恢复后，使用鼠标、触控与键盘 Tab 逐一操作首页导航、主次按钮、文字链接、作品链接、移动菜单和页脚链接；同时检查系统“减少动态效果”模式。

## 网站品牌与登录页自定义化（2026-09-03）

- [x] 新增全局服务端品牌配置，统一管理名称、英文名、工作台名称、标语、描述、主色、登录页文案、浏览器标题和描述；沿用 `system_settings`，不新增数据库迁移。
- [x] 管理后台品牌入口重做为亮色双区工作台：左侧固定显示品牌摘要、状态、主题色和四个纵向分区，右侧每次只显示一张编辑工作表，底部固定保存与恢复操作；同时保留修订冲突保护、离开未保存提醒和管理审计记录。
- [x] Logo、favicon、登录背景和视频海报使用既有资源存储；服务端校验真实文件魔数、类型和大小，当前激活资源不能从资源管理中直接删除，历史资源保留用于回滚。
- [x] 登录/注册统一请求账号开放状态，首个账号只显示管理员初始化，关闭注册时不再暴露注册入口；登录背景在移动端或减少动态效果时使用海报/渐变，不自动播放视频。
- [x] 品牌配置已接入登录、注册、工作区侧栏与顶栏、首页、创作页、共享画布、浏览器元信息和注册验证码邮件；配置接口失败时回退上次缓存或内置默认值，不阻断登录。
- [x] 后端品牌 service/handler/repository 专项测试、前端品牌主题与入口专项测试和 TypeScript 检查通过；完整实施与回滚记录见 `docs/design/website-branding-and-auth-customization-20260903.mdx`。
- [x] 登录/注册入口推倒重做为浅色画布、独立品牌媒体画框和无卡片认证区，移除黑色玻璃卡片、紫色渐变主按钮、返回首页及装饰性英文；既有品牌媒体、账号与配置存储协议未变。
- [x] 暂时启用单一公开入口：根地址、产品、作品和关于地址均重定向到登录页，后台“官网内容”菜单隐藏且旧地址转向品牌中心；公开官网组件、草稿、发布数据和接口均保留，未做删除。
- [x] 新版品牌设置与登录入口复验通过：前端 241 个文件、1668 项测试、9645 个断言全绿，TypeScript、生产构建和全部体积预算通过；真实浏览器完成桌面及 390×844 移动端检查，根地址自动进入登录页，自动填充账号后登录按钮可用，控制台无警告或错误。品牌接口仍为 7 个，认证接口仍为 8 个主要接口和 1 个兼容回调；代码回滚点：`be0f75d`。
- [ ] 在真实管理员浏览器依次修改品牌名和主色、上传 Logo/favicon/登录背景图片，刷新登录页、工作台、共享画布和浏览器标签，确认全部入口同步且无旧品牌残留。
- [ ] 上传 MP4/WebM 登录背景并配置海报，在桌面、移动端和系统“减少动态效果”下验证自动播放、海报降级、加载失败降级与文字可读性。
- [ ] 打开两个管理后台标签页同时编辑，先后保存，确认后保存的旧修订收到冲突提示且不会覆盖新配置；再执行“恢复默认”，确认修订和管理审计完整。
- [ ] 配置真实 SMTP 后发送一次注册验证码，确认发件显示名、主题和正文使用当前品牌；自定义发件名称时仍以管理员设置为准。
- [ ] 在 1366×768、1440×900 和 430×932 视口继续检查全屏品牌背景、视频/海报裁切、长品牌名称、登录/注册切换、键盘焦点和表单滚动；390×844 已通过，移动端不自动播放视频且主要操作无需横向滚动。

## 创作历史云同步与入口边界（2026-09-03）

- [x] 创作页对话、消息、引用、任务关联和结果引用新增账号级增量同步；旧 IndexedDB 历史首次读取后迁移，云端失败保留本地副本和待上传队列，不再把本地落盘显示为云端成功。
- [x] 每条对话使用独立版本和软删除墓碑；并发写入按稳定消息 ID 合并，重试替换消息使用删除标记，避免旧设备把已替换消息或已删除对话重新带回。
- [x] 工具条新增读取、等待、同步、成功、失败和冲突状态，失败与冲突可以手动重试，恢复在线会自动重试。
- [x] 后端增加结构迁移 v6、账号隔离、2 MiB/消息数量/删除标记限制、结构化存储与会话数量配额，并补充 service、handler 和前端冲突合并测试。
- [x] 完成全入口数据边界盘点；画布项目、素材、短剧领域数据、任务和资源维持服务端主数据，未提交草稿、密钥/自定义头、Agent 本机日志和 UI 偏好保持设备私密或本地状态。
- [x] 集中验证通过：前端 231 个文件、1642 项测试，后端 `go test ./...`，Canvas Agent 构建及 316 项通过/5 项平台跳过，前端生产构建与全部体积预算均通过。
- [ ] 使用同一账号在两个浏览器分别新增消息、完成生成结果回填、重试一条失败消息并删除整个对话，确认合并后顺序、附件引用、任务 ID、结果 URL 和删除状态一致。
- [ ] 断网编辑后关闭页面，再恢复网络重新进入创作页，确认本地历史可读、状态显示失败/等待、自动或手动重试后转为“已同步”，且另一个设备可以读取。
- [ ] 将绘图文档中的内嵌图片迁移到资源存储后，再实现绘图结构化文档云同步；预览和生成渲染继续作为可淘汰本地缓存。详细方案见“用户数据同步边界”。

## 超大文件首批边界拆分（2026-09-03）

- [x] 后端将声明式协议的请求归一化、视频扩展参数别名和媒体角色投影从 `provider.go` 迁入 `provider_protocol_request.go`；原文件从 5,199 行降至 5,108 行，网络执行、轮询和下载逻辑未改动。
- [x] 新增协议请求合同测试，覆盖时长/数量归一化、Token 上限、扩展参数别名、空媒体剔除，并复验首尾帧与普通参考图角色。
- [x] 将 AI Markdown 的 102 行业务样式从 `globals.css` 迁到组件同目录，由 `ai-message-markdown.tsx` 显式引入；新增样式边界回归测试，避免选择器重新流入全局文件。
- [ ] 在真实页面检查创作结果、画布 Agent、思考折叠区和 Markdown 节点的标题、列表、代码、表格、链接与任务复选框；本轮仅做静态合同、TypeScript 和生产构建验证。

## 前端构建体积预算与入口文档（2026-09-03）

- [x] Vite 构建固定生成 manifest，并在 `bun run build` 末尾校验启动壳、画布/创作页首访增量、最大业务入口及最大 JS/CSS 单文件；预算只比 2026-09-03 基线高约 2%–5%。
- [x] 当前 gzip 基线为：启动壳 JS 587.9 KiB、启动壳 CSS 120.5 KiB、画布首访新增 JS 833.3 KiB、创作页首访新增 JS 397.0 KiB；本地预算检查通过。
- [x] 修正 README 的 5 个失效文档入口、不存在的 docs 构建命令和媒体本地降级描述；统一模型目录部署指南移除个人绝对路径、npm 旧命令、模糊杀进程步骤和错误的开关默认值。
- [x] 新增 README、AI 文档索引和部署指南的本地链接回归测试，防止入口文档再次指向缺失文件。

## 统一模型目录报价（2026-09-03）

- [x] 关闭“前台模型”时，`POST /api/model-catalog/quote` 已改为对系统渠道模型报价，不再返回 501。
- [x] 系统报价与真实任务预留共用价格档匹配、Token 估算和积分倍率逻辑；专项测试已校验同一图片规格的报价与账单金额一致。
- [x] 停用模型、停用/非系统渠道、能力不匹配和未命中有效价格档会在报价阶段被拒绝，不会暴露渠道密钥或上游配置。
- [ ] 在真实登录浏览器中关闭“前台模型”，分别切换图片质量及视频分辨率/时长，确认即时报价、任务确认金额和最终账单一致；该项会触发真实付费生成，本轮不执行。

## 媒体上传持久化与 HTTP 读取边界（2026-09-03）

- [x] 图片、视频、音频和文件的新上传只在后端资源持久化成功后返回；未登录、配额不足、请求过大或服务端失败不再被静默伪装成 IndexedDB 本地上传成功。
- [x] 历史 `image:*` / `generation-image:*` 及其他本地媒体键保留读取、清理和云端迁移能力，不因收紧新上传合同丢失既有数据。
- [x] 新增统一 Blob 读取边界，参考素材、生成结果、本机即梦、MiniMax、Seedance 和 tldraw 导入不再将 HTTP 非 2xx 响应当作有效媒体；122 项相关测试与 TypeScript 检查通过。
- [ ] 在真实登录浏览器中分别模拟 401、413、429 和 500，确认创作页、画布、素材库与项目声音上传都显示真实错误，不新增本地占位记录或误报“已保存”。

## 自动化质量基线（2026-09-03）

- [x] 前端默认 `test` 脚本改为自动发现全部测试，不再维护手工文件白名单；当前集中验收全量 231 个文件、1642 项测试通过。
- [x] 修复 Canvas Agent poll heartbeat 测试因 `unref` 计时器导致的父测试提前退出，并放宽跨 Runtime 队列接管的过窄测试窗口；全量 321 项测试为 316 通过、5 个平台跳过、0 失败、0 取消。
- [x] CI 新增 `develop/yingce-platform` push 触发、前端生产构建，以及 Canvas Agent 独立构建与全量测试任务。
- [x] 本地验证前端生产构建、Canvas Agent TypeScript 构建和后端 `go test ./...` 全部通过。
- [ ] 在下一次实际 push 或 PR 中确认 GitHub Actions 的 Ubuntu/Chrome 环境同样全绿，并保留 Director P0 Chrome E2E 结果作为托管环境验收证据。

## PC 画布专项检查修复（2026-09-02）

- [x] 阶段 A：工具栏恢复入口固定保留；拖动排序仅在放下后保存并支持 Alt+方向键排序；多选工具按真实可操作节点数量禁用并显示原因。
- [x] 阶段 A：绘图节点删除与清空画布不再提前清理绘图文档，撤销前会立即提交 180ms 合并窗口内的最新变更；项目永久删除时统一清理该项目绘图记录。
- [x] 阶段 A：输入框、Agent/工具面板和可见模态框拥有键盘优先权，画布不再在捕获阶段抢走缩放、保存、搜索或粘贴快捷键。
- [x] 阶段 A 局部验证：TypeScript 检查通过；画布交互、键盘、批量连线、绘图和文档共 21 项测试通过。
- [x] 阶段 B：Agent 展开时主工具栏为左侧视图/素材 Dock 预留空间；1280×720 实测左侧止于 x=332，主工具栏从 x=433 开始，无覆盖。
- [x] 阶段 B：视频播放按钮按画布倍率反向补偿，24% 画布倍率实测为约 24.2×24.2px；未开放超分显示为禁用项并说明原因。
- [x] 阶段 B：图片、视频、音频设置共用定位、自动上下翻转、Esc/外部关闭和固定头尾外壳；音频首次点击会延续播放意图，不再要求“先加载、再播放”。
- [x] 阶段 B 局部验证：TypeScript 检查通过；交互、视频播放、性能模式和键盘共 26 项测试通过；本地真实画布检查无 warning/error。
- [x] 阶段 C：逻辑选区与 DOM 挂载分离，屏幕外普通节点不再因全选而常驻；拖动中的节点按移动后位置判断可见性，编辑中的专项节点仍保持挂载。
- [x] 阶段 C：视频封面任务支持同请求共享和消费者级取消；性能模式只显示一张清晰封面并关闭重复模糊/背景滤镜，画质模式仍保留完整预览层级。
- [x] 阶段 C：Agent 只保留外层抽屉动画，画布高频 Dock 关闭逐项弹簧放大；TypeScript 检查通过，交互/性能/视频/键盘 30 项测试通过。1280×720 真实 39 节点画布挂载 34 个节点、25 个视频封面、25 张封面图和 0 个完整播放器。
- [x] 阶段 D：时间线合成视频回画布时复用统一避让落位；批量上传只在完成后统一选择和紧凑排版，格式不支持与上传失败分开统计，失败项保留在窗口内重试。
- [x] 阶段 D：分享链接创建与剪贴板复制分开反馈，复制失败不再误报创建失败；系统减少动画时停止高光指针计算。TypeScript 检查及画布交互、时间线、性能、视频、键盘 57 项测试通过。
- [x] 阶段 D 真实页面冒烟：分享弹窗能读取未分享状态且创建入口可达；添加节点中的上传入口可达，批量上传弹窗标题、已选数量和提交按钮正常，关闭后不残留遮罩。
- [x] 阶段 E：修复项目加载后的媒体恢复误生成初始撤销步骤；真实页面首次打开时撤销/重做均为禁用。节点即时工具改为在 PC 鼠标按下阶段提交，保留 Enter/Space 键盘触发，避免悬浮工具栏移动吞掉点击。
- [x] 阶段 E 集中验证：生产构建通过；41 个画布/时间线测试文件共 267 项通过、0 失败。4 节点隔离夹具验证重命名失焦提交、删除/撤销、自动整理/撤销、外观/撤销、锁定键盘操作；324 节点夹具只挂载 30 个节点，Cmd+A 后仍为 30 个。
- [x] 真实 1280×720 画布完成顶栏、画布菜单、第三方导入菜单、搜索、添加节点、外观、媒体模式、工具栏设置、缩放、快捷键、专注模式、Agent、分享/上传弹窗、图片/视频设置和视频按需挂载的非破坏性回归。
- [ ] 发布前人工补测：绘图编辑器删除后重新打开、工具栏真实拖动取消、弹窗内系统剪贴板与中文输入法，以及 Chrome/Safari 下 1024/1440/1920 桌面宽度。
- [ ] 使用专门测试数据执行真实上传、LibTV/TapNow 导入、分享创建/轮换/停止、本机 Agent 写回和付费生成；本轮为保护真实作品与余额未执行这些外部副作用动作。

## 上线前任务与计费闭环

- [x] 任务创建支持用户级幂等键、请求指纹和并发唯一约束；同键同请求重放原任务，同键不同请求返回冲突，重复请求不会生成第二笔冻结流水。
- [x] 超时账单后台巡检只在“请求明确未发上游”时自动退款；费用不明的失败保留冻结并进入人工核对，Token 补扣受余额和安全阈值约束，不会产生负余额。
- [x] 创作页提交会在素材上传、素材替换期间锁定模型和参数并阻止发送；Token 模型展示匹配当前规格的每百万 Token 单价，完成后仍以真实 usage 结算。
- [ ] 在浏览器快速双击提交，并模拟首次响应丢失后重试，确认只出现一个任务、一笔账单和一条冻结流水，页面恢复到同一任务。
- [ ] 分别用可美视频的无参考视频/含参考视频与 480p/720p/1080p 价格档完成真实任务，确认创作页单价、命中价格档、上游 usage、实际积分和账单明细一致。
- [ ] 上传、素材库导入和替换素材期间尝试切换模式、模型、分辨率、时长、会话及点击生成，确认均被阻止；上传失败有可见提示，成功后可正常提交。
- [ ] 在任务已发上游但尚未回传结果时重启后端，确认任务继续轮询或恢复结果；费用不明时保持人工核对，不自动退款或重复扣费。
- [ ] 核对升级前已有的 `uncertain` 订单数量和冻结总额，确认本次启动巡检不会把有上游调用证据的旧订单自动退款。

## 服务升级运行基础

- 完整架构、状态机、Release 清单、备份和回退边界见 `docs/design/dynamic-update.md`。
- [x] 后端新增权威 BuildInfo、`schema_migrations`、独立 `migrate-schema` 命令、分层健康检查以及可停止 Worker；专项 Go 测试通过。
- [x] 生产 Compose 在 Backend 前运行一次性迁移服务，并使用 `CANVAS_AUTO_MIGRATE=false` 阻止业务进程隐式修改 Schema；Backend 收到 SIGTERM 后进入 drain、停止领取任务并等待活动任务。
- [ ] 使用 PostgreSQL 部署执行一次无迁移启动和一次跨版本迁移，确认 migrate 成功后 Backend 才启动，checksum 漂移和高版本 Schema 均会阻止旧 Backend 就绪。
- [ ] 在存在图片、视频和文本长任务时发送 SIGTERM，确认 readiness 立即返回 503、新任务被拒绝、已领取任务在宽限期内完成，超时后租约可由重启实例恢复。
- [ ] 构建带版本参数的前后端镜像，确认 `/api/system/version`、管理后台版本、OCI Commit 和 Release Tag 一致。

## 素材库网格密度

- [x] 素材库桌面端缩小卡片并改为每行 6 张；新增素材占首格，默认每页 35 项，可切换 70/105 项，首屏正好组成 6×6 完整网格；中窄屏按 4/3/1 列收敛，前端生产构建通过。
- [ ] 在 1280px、1440px、1920px 与窄屏下检查图片、视频、音频、文本、模型和新增素材卡片，确认标题、徽标、批量选择和操作菜单不拥挤或溢出。

## 短剧长篇项目分页读取

- [x] 短剧工作台已拆分项目核心、制作概览、章节摘要、当前章节生产包、画布分页、资产分页和候选分页接口；章节列表不返回正文，当前章节生产包不返回其他章节镜头和产物。
- [x] 资产库按目录/分类分页，候选角色单独分页；分镜工作台资产栏服务端分页，当前章节响应只携带镜头已绑定资产；项目设置仅在打开主图选择器时读取图片资产。
- [x] 新增章节、镜头、章节产物、画布、项目目录资产、候选资产和项目任务组合索引；后端聚合/隔离/分页专项测试与前端生产构建通过。
- [ ] 导入至少 1000 章、每章 10 个镜头的测试项目，分别打开概览、章节、分镜、画布、资产和设置页，确认首屏请求不再出现整项目详情响应，切章只读取目标章节生产包。
- [ ] 在浏览器网络面板记录上述页面的响应体积、请求耗时和内存占用；连续切换 30 个章节、翻阅 10 页画布和资产，确认 React Query 缓存及 DOM 数量没有持续异常增长。
- [ ] 在资产超过 80 项、角色候选超过 24 项、画布超过 40 张时验证分页、分类、目录、候选确认、镜头资产绑定和主图选择；确认新增、移动、删除、确认后总数与当前页自动刷新。
- [ ] 使用 SQLite 与 PostgreSQL 各执行一次大项目冒烟，确认新增组合索引成功创建，概览聚合、章节隔离和分页顺序一致。

## 多文件技能包库

- [x] 后端新增技能版本与文件索引，支持单个 Markdown、ZIP 和公开 GitHub 仓库安装；ZIP 路径越界、软链接、重复路径、文件数、单文件大小和总解压大小均在写入前校验，后端全量测试通过。
- [x] 技能详情改为约 82vw × 82vh 的弹窗，左侧支持多层文件树，右侧支持 Markdown 预览、源码、代码/文本及媒体文件查看；前端生产构建通过。
- [x] 在线 Agent 只在系统上下文保留技能元数据，并通过入口、文件清单、单文件读取和搜索工具渐进式加载；本机 Agent 只把用户本轮显式提及的完整技能包写入临时目录，321 项 Agent 测试与 TypeScript 构建通过。
- [x] 新增统一 Skill Runtime：画布、创作页和短剧工作台共用同一技能选择、引用解析、版本/文件来源记录与上下文预算；普通生成只加载 `SKILL.md` 和入口直接引用的相关文本，在线 Agent 使用渐进工具，本机 Agent 使用原生完整包，专项测试与前端类型检查通过。
- [ ] 分别安装无 Frontmatter 的单个 Markdown、两个用户提供的示例 ZIP，确认名称/简介推断、文件树层级、中文路径、预览/源码切换和相对 Markdown 链接正确。
- [ ] 使用公开 GitHub 仓库的默认分支、指定分支和子目录安装技能，修改仓库后手动同步并等待一次后台检查，确认 commit、版本、检查时间、同步时间和失败信息准确。
- [ ] 在浅色、深色、常规桌面、窄屏下检查大弹窗尺寸、双栏滚动、文件筛选、长路径、省略文本、键盘焦点和媒体预览；确认弹窗不会触发画布缩放或快捷键。
- [ ] 在线 Agent 使用示例技能完成一次任务，确认先读 `SKILL.md`，仅在入口引用时读取对应 reference；本机 Agent 确认 scripts/assets 保持目录结构、turn 完成后临时目录被清理，且聊天历史不出现完整技能正文。
- [ ] 分别在画布图片/视频/文本生成、创作页文本/图片/视频、短剧分镜图/动作预演/镜头视频中选择示例技能，确认任务只记录本轮技能版本和实际读取文件，不重复注入历史消息，也不加载 `scripts/`、`assets/` 或整个技能包。
- [x] 画布、创作页和 Agent 共用的 `@` 引用菜单改为真实光标定位，输入框或页面滚动、尺寸变化时同步更新；向上弹出按实际内容高度贴边。技能候选改为工作流图标、名称、说明、版本和文件数的统一信息行，专项测试和前端生产构建通过。
- [ ] 登录后分别在高输入框首行、末行、滚动后的中间行输入 `@`，确认菜单与光标保持约 8px 间距；在浅色/深色及窄屏下确认技能说明、版本、文件数和键盘选中态清晰可读。

## 短剧分镜工作流 v2

- [x] 后端新增镜头版本、镜头产物和生产任务上下文；分镜修改与资产版本变更会局部将旧产物标记为 `stale`，专项测试通过。
- [x] 短剧工作流调整为“剧情与章节 → 资产拆分 → 分镜脚本 → 黑白动作预演 → 视频生成 → 交付与打包”，旧 v1 实例保留，新章节使用 v2。
- [x] 项目工作台新增独立分镜工作流路由，支持初始化章节流程、新增镜头、保存分镜版本、绑定资产版本、查看预演/视频版本和交付缺失状态；前端生产构建通过。
- [x] 镜头轨道按当前制作阶段显示待生成、生成中、已生成和生成失败状态，状态标签固定在卡片右下角；横向镜头只在轨道内部滚动，不再撑宽脚本区或挤出产物预览。制作概览六个流程节点改为独立可点击卡片；前端类型检查、生产构建与相关专项测试通过。
- [x] 分镜生成设置接入统一模型选择、能力和计价链路，支持前台模型、系统模型与用户自定义模型；切换模型会归一画幅、分辨率/画质和时长，MiniMax-H3 的旧 `720` 会切换为 `768P`。项目设置可保存默认生图/视频模型；后端全量测试、MiniMax 回归测试和前端生产构建通过。
- [x] 单镜生成改为任务提交后立即返回，不同镜头可并行进入排队/生成状态；角色卡主图、绑定声音样本和场景资产会按 `图片N` / `音频N` 编译进提示词。MiniMax 项目资产使用 `reference_image` / `reference_audio`，仅显式首尾帧节点使用帧角色；产物固定绑定任务提交时的镜头版本。前后端全量测试与生产构建通过。
- [x] 视频图片语义统一为首帧、尾帧和普通参考图三类，并贯穿前端直连、后端任务与声明式协议；Agnes、Seedance `/videos`、火山方舟、NewAPI 媒体任务、xAI 和 MiniMax 不再按图片数量误猜项目角色资产。Gemini Veo、Novita 等仅支持起始图的协议会明确拒绝 `reference_to_video`，不再静默降级成首帧。
- [x] 交付阶段新增浏览器本机打包：按镜头顺序读取已选中视频，本地合成 MP4，并输出 SRT、分镜 JSON/CSV、资产清单、交付清单与说明文件 ZIP；不新增上传或后端协议。导出前按设备能力预检资源容量，下载时按 Blob 实际体积二次校验，超限会在 FFmpeg 前停止并提示拆分章节。容量边界、真实 ZIP 解包、TypeScript、PC 预检和生产构建已通过；隔离 Chrome 内现场生成两个无费用 WebM，实际完成容量核对、FFmpeg 合成、下载和解包，11/11 验收通过。
- [x] 交付阶段新增独立的后端异步任务：提交时冻结镜头版本、已选中视频与资产引用，不进入模型任务或计费队列；后台以数据库租约恢复任务，按镜头顺序合成 MP4 并打包同一套生产资料，页面关闭后继续执行。完成文件通过鉴权流式下载保留 7 天，过期后由资源删除 Outbox 回收；重复点击只保留一个活动任务，项目或章节删除会阻止仍在运行的交付任务。服务器镜像已包含 FFmpeg，本机导出继续作为未安装服务器组件时的兜底。schema v5 会为已运行旧版迁移的实例补建交付任务表；隔离发布镜像已用 H.264/AAC MP4 与不同尺寸、无音轨 VP9 WebM 跑通规格统一、中文 ZIP、SRT/CSV、206 Range、重启接管及到期物理回收。
- [x] 分镜制作工作台重排为影视制作台结构：左侧资产/章节/镜头导航、中央画面监看、右侧镜头脚本与生成检查器、底部分镜时间线；预览改为完整画面适配，历史版本使用自适应网格，危险操作与生成主操作分层。布局专项测试、TypeScript 检查和生产构建通过。
- [x] 视频阶段新增“批量生成缺失视频”：只选择缺少已选中视频、没有排队/运行任务且存在已保存脚本版本的镜头；按镜头资产、声音和时长统一做模型兼容预检，费用确认后最多并发提交 3 个独立后台任务，单项失败不影响其他任务。4 项规划、版本投影、按钮与并发隔离测试、TypeScript 和生产构建通过；未确认费用弹窗，未触发真实生成。
- [x] 章节“提取角色”升级为“拆分资产”：统一输出角色、场景、服饰、道具、武器五类候选；服务端新增受保护 JSON 契约和结果字段校验，任务列表投影 `assets` 上下文并兼容旧角色任务；前端按分类/别名去重，刷新后恢复结果，“角色与资产”页可在全部或单类视图确认非角色候选。解析与恢复专项、前端完整脚本、后端 `go test ./...`、TypeScript 和生产构建通过；未触发真实模型。
- [x] 黑白动作预演升级为严格 3×4 / 12 宫格动作板：格子按时间排序，保持角色、服装、场景、道具和光线连续，产物元数据记录行/列/帧数；新增批量生成缺失预演，只选择有保存版本、无已选中预演且无运行任务的镜头，逐镜头预检后经费用确认最多并发 3 个独立任务。提示词、规格和批量入口测试、TypeScript、快速预检和生产构建通过；未确认费用弹窗。
- [ ] 在真实项目中初始化一个已有章节，确认不会误用旧 v1 工作流，刷新和切换章节后阶段、镜头和当前版本保持一致。
- [ ] 在同一章节准备待生成、生成中、已生成和生成失败镜头，确认灰、蓝、绿、红标签始终显示在卡片右下角；增加到几十个镜头后确认只有底部轨道横向滚动，中间脚本和右侧产物预览仍完整可见。
- [ ] 在浅色、深色、常规桌面和窄屏下检查制作概览六张流程卡片的边界、间距、完成态、悬停和键盘焦点，确认跳转目标正确。
- [ ] 连续保存同一镜头多个版本并绑定不同资产版本，确认只有该镜头的动作预演和视频转为过期，其他镜头不受影响。
- [ ] 使用画布任务和项目任务分别登记镜头产物，确认 `production_task_links` 正确记录 Project、Canvas、Unit、Shot 和 WorkflowStep，且账号归属校验有效。
- [ ] 在浅色、深色、窄屏和常规桌面验证阶段导航、镜头列表、表单、检查器、媒体预览和长章节内容滚动。
- [ ] 在项目设置分别选择系统、用户自定义的默认生图/视频模型，刷新后进入分镜图、动作预演和镜头视频，确认项目默认优先、已下架模型回退全局默认；切换 Seedance、MiniMax-H3 等能力不同的模型时，画幅、时长、分辨率和本次预计积分立即更新，并与实际账单一致。
- [ ] 连续选择两个镜头并分别提交视频生成，确认两个镜头独立显示“正在提交/生成中”，第二个任务不会被第一个全局锁阻止；刷新页面后两个任务状态都能恢复，完成产物分别绑定各自提交时的镜头版本。
- [ ] 准备“已有视频、正在生成、缺少视频、没有保存版本”四类镜头后点击“批量生成缺失视频”，确认弹窗数量只包含可提交项；在明确预算后确认一次真实批量，核对并发任务、逐镜头计费、部分失败提示和刷新恢复。
- [ ] 在明确文本模型与预算后，对一章同时包含角色、固定场景、服饰、道具和武器的正文执行一次真实资产拆分；核对五类结果、角色字段质量、同名别名去重、刷新恢复和逐类确认。
- [ ] 在明确图片模型与预算后，准备“已有预演、正在生成、缺少预演、没有保存版本”四类镜头并真实提交一次批量 12 宫格预演；核对弹窗数量、逐镜头计费、3 并发、部分失败提示、刷新恢复，以及成图是否严格 3×4 且人物/场景连续。
- [ ] 给镜头绑定带主图和声音样本的角色卡及场景资产，使用 MiniMax-H3 生成并检查请求日志：文本包含 `【资产参考】`、角色声音说明和台词，角色/场景图片为 `reference_image`、声音为 `reference_audio`，请求不再出现误判的 `first_frame`，画幅和 `768P` / `2K` 与界面选择一致。
- [ ] 使用同一镜头资产依次切换 Agnes 2.5、Seedance `/videos`、火山方舟、NewAPI 媒体任务和 xAI，确认单张角色图仍走 reference 字段；再切换 Gemini Veo、Novita，确认提交前给出“不支持 reference_to_video”的明确提示，而不是把角色图当起始帧生成。
- [ ] 使用一个每镜头均有已选中视频的真实长章节分别生成后台与本机交付包，在主流播放器中确认镜头顺序与声画质量，并核对资产引用及服务器磁盘峰值；使用对象存储验证大文件 Range/断点下载和到期回收。再在 Chrome、Safari 和 Edge 使用接近/超过本机安全预算的长章节，确认本机超限提示可读且不启动合成，同时后台模式仍可提交。
- [ ] 在 1024、1440、1920 桌面宽度及浅色/深色主题下检查分镜制作新布局：中央监看画面不裁切，右侧脚本字段和底部操作不溢出，左侧导航按断点收起，底部时间线可横向浏览；再验证 760px 以下继续回退为单列镜头编辑器。

## 角色声音素材绑定

- [x] 角色卡声音绑定移除无效的预设声音下拉框，改为从素材库选择或实时上传声音样本；支持 MP3、WAV、M4A/AAC、FLAC、OGG/Opus 和 WebM 音频。前后端共同校验资源归属、完成状态、资源类型和格式白名单，画布生成引用会保留资源真实 MIME 与文件扩展名。
- [ ] 在角色卡分别选择素材库已有音频并上传 MP3、WAV、M4A/AAC、FLAC、OGG/Opus、WebM 样本，确认选择器格式标签、浏览器试听、绑定、版本历史和刷新后的声音状态一致；再验证不支持格式、伪装扩展名、视频 WebM、未完成资源和其他账号资源均被拒绝。
- [ ] 使用绑定了不同格式声音样本的角色执行画布生成，确认请求中的参考音频使用资源真实 MIME 和对应文件扩展名，不再统一伪装成 MP3；生成成功、失败及刷新恢复后引用关系不丢失。

## 短剧章节后台生成与项目头部

- [x] “提取角色”和“生成到分镜制作”继续使用后端任务中心，确认配置后立即关闭弹窗；章节按钮按任务真实开始时间每秒显示“已运行 X分钟XX秒”，任务完成后恢复可点击并显示“已生成”。刷新页面后会按章节从任务中心恢复运行计时和完成态，并接管尚未写回的角色或分镜结果。
- [x] 项目名称、状态、六个项目导航、章节选择和新建画布操作已注册到全局公用头部，项目内容区不再重复占用一整行头部；相关专项测试、TypeScript 检查与前端生产构建通过。
- [ ] 在真实慢任务中分别启动角色提取和章节分镜，确认弹窗立即关闭、按钮计时连续增长、可编辑正文和切换章节，完成后按钮显示“已生成”且仍可再次运行。
- [ ] 在任务运行中和任务刚完成尚未写回结果时分别刷新项目，确认对应章节继续显示累计计时或“已生成”，成功结果会自动写回；再检查无新角色、生成失败和重新生成替换确认场景的按钮恢复、错误提示和旧分镜保护。
- [ ] 在浅色、深色、常规桌面和窄屏下检查全局头部：项目名、状态、项目导航、右侧全局操作和新建画布/章节选择不重叠，导航可横向滚动，内容区高度确实增加。

## 画布生成结果命名

- [x] 快捷键复制节点生成图片后保留 `_copyN`，参数变体生成图片后保留 `A/B/C/D` 版本标识；批量输出追加结果序号。
- [x] 下载文件名继续使用节点标题，因此复制、参数变体和同批多图结果不会再生成相同文件名。
- [ ] 分别使用快捷键复制和“创建参数变体”生成图片，确认生成中、生成完成和刷新画布后标题标识不丢失。
- [ ] 下载原图、复制结果、多个参数变体和同批多图结果，确认浏览器不会再提示同名文件覆盖。

## 画布生成进度与全屏视频控件

- [x] 移除图片、视频任务进入模型调用时统一写入的假 35%；排队、后端接管和连接上游阶段只显示真实阶段文案，不再显示伪造百分比。
- [x] 后端从图片、视频上游成功的创建与轮询响应中识别 `progress`、`percent`、`percentage`、`completion` 及常见百分比变体，支持数字、百分数字符串和 0~1 小数；进度单调写回任务，前端既有 2 秒轮询会同步到画布节点。
- [x] 画布视频节点保留紧凑控件；进入全屏后恢复完整顶部控制组，底部按钮调整为 52px 操作目标，同时放大图标、时间文字、进度轨道和拖动滑块。
- [ ] 分别使用会返回整数百分比、小数进度、百分数字符串和不返回进度的图片/视频渠道生成，确认前三者按上游实时增长，不返回进度的渠道仅显示阶段文案且不会卡在固定百分比；刷新页面后继续从后端任务恢复最新进度。
- [ ] 在桌面、窄屏和触屏设备全屏播放横版、竖版视频，确认播放/暂停、音量、时间、画中画、设置、全屏退出和进度拖动尺寸清晰且不会遮挡字幕。

## 视频画面与片段提取

- [x] 视频节点“尾帧截取”已合并为“提取画面”，支持首帧、当前帧、尾帧和最多 30 个自选时间点；批量结果保留来源节点与时间点元数据。
- [x] “截取重生成”已调整为“截取片段”，默认只创建片段素材；可选同时创建待生成节点，但不会自动提交生成任务。
- [x] 多张提取图片会保持全部选中，可继续使用画布现有批量连接能力自行创建并连接生成节点。
- [ ] 在真实视频节点提取首帧、多个中间帧和尾帧，确认画面、时间标记、网格布局、来源连线和多选状态正确。
- [ ] 选择接近重复的时间点、超过 30 个时间点及部分帧解码失败场景，确认去重、上限提示和部分成功反馈正确。
- [ ] 分别验证“只创建视频片段”和“同时创建待生成节点”，确认前者不要求模型，后者创建后不会自动扣费或启动任务。
- [ ] 选中多张提取图片后新建视频生成节点，确认批量连线和手动生成流程可用。

## 画布节点搜索

- [x] 搜索结果改为紧凑媒体行，图片、视频、绘图优先显示真实缩略图，文本节点显示内容片段；素材类型、尺寸、时长、文件大小和模型按可用信息摘要展示。
- [x] 节点新增可持久化创建时间与最后编辑时间；旧画布按项目时间建立迁移基线，素材 URL/尺寸后台恢复不会误记为用户编辑。
- [x] 默认按最近编辑排序，支持节点、章节、镜头、模型和标签搜索，以及方向键选择、Enter 定位、Esc 关闭。
- [ ] 在含图片、视频、音频、绘图、文本、分镜和插件节点的真实画布验证缩略图、摘要、时间和排序；确认失效媒体能降级为类型图标。
- [ ] 在窄屏、常规桌面、浅色和深色主题验证行密度、长标题截断、时间列布局、滚动与键盘焦点。

## 画布素材批量插入与右键菜单动效

- [x] 画布素材库复用既有多选能力，一次确认后按三列网格批量创建节点并选中全部新增节点；本地素材与插件来源共用同一 payload 转换路径。
- [x] 时间线素材入口继续保持单选，图片和文本不会被误当作可直接入轨媒体。
- [x] 右键主菜单、添加节点子菜单、菜单项、图标与箭头已移除位移和缩放动画，只保留淡入、颜色变化与 Spotlight 光影跟随；专项测试、TypeScript 检查与生产构建通过。
- [ ] 在画布空白处右键后缓慢移动光标经过各菜单项，确认菜单、文字、图标和箭头位置稳定，光影仍跟随光标，主菜单与子菜单打开时不跳动。
- [ ] 从画布工具栏和右键菜单分别打开素材库，混选图片、视频、音频、文本及插件来源素材，确认一次插入全部节点、顺序稳定、节点不重叠且全部保持选中。
- [ ] 从时间线打开素材库，确认仍只能单选；音视频可直接入轨，图片或文本会保留弹窗并显示明确错误。

## 画布本机 Agent 连接教程

- [x] 未连接、连接中和连接失败时，Agent 对话正文由连接教程替代；连接成功后自动恢复原聊天或历史区域，不清空消息和线程状态。
- [x] 教程按 macOS/Linux 与 Windows 生成安装、构建和启动命令，并将当前网页 Origin 写入 `FRAMEFIELD_TRUSTED_WEB_ORIGINS`；命令生成专项测试、前端类型检查与生产构建通过。
- [x] 页面明确区分网页侧边栏 Codex、外部 Codex/Claude Code MCP 与尚未验证的 Hermes/WorkBuddy，不再把 Codex 插件误写成本机侧边栏前置条件。
- [x] 画布全局 `Cmd/Ctrl+C` 在存在真实文字选区时交还浏览器处理，不再把 Agent 消息、日志或 Markdown 文本复制误判成节点复制；快捷键边界专项测试通过。
- [ ] 在 Windows、macOS 和 Linux 分别复制并执行教程命令，确认 Runtime 能连接当前线上 Origin，平台自动识别和手动切换均正确。
- [ ] 在未连接、自动重试、Origin 未授权、连接成功和运行中断后重连状态下验证教程与对话区域切换；确认原消息、历史会话和待确认工具调用不丢失。
- [ ] 在 360px 窄面板、常规宽度、浅色和深色主题下确认命令块可横向滚动、复制按钮可见、教程不遮挡画布。
- [ ] 在 Agent 回复、运行日志、代码块和普通 Markdown 中分别框选文字后使用右键复制及 `Cmd/Ctrl+C`，确认系统剪贴板内容正确；无文字选区时 `Cmd/Ctrl+C` 仍复制所选画布节点。

## 画布复制粘贴命名

- [x] 节点首次复制粘贴使用“原名称\_copy1”，后续按当前画布已有最大序号递增。
- [x] 复制已有 `_copyN` 副本时回到原始名称计算，避免生成 `_copy1_copy1`；旧版空格 `Copy` 后缀已有兼容测试。
- [ ] 在画布中连续粘贴单个节点、多个同名节点和包含子节点的背板，确认名称依次递增且节点关系不变。

## 画布媒体下载文件名

- [x] 图片、视频和音频下载统一使用“画布名\_节点名\_yyyyMMdd.扩展名”，中文名称和跨平台非法字符已有专项测试覆盖。
- [x] 扩展名优先按媒体 MIME 类型判断，并兼容远程 URL 后缀；JPEG、WebM 和 MOV 已有专项测试覆盖。
- [x] 前端 TypeScript 检查与生产构建通过。
- [ ] 在浏览器中分别下载本地上传、生成结果、LibTV 导入和 TapNow 导入的图片与视频，确认文件名、日期和实际媒体格式一致。
- [ ] 在同一天重复下载同名节点，确认浏览器的重名处理不会覆盖用户已有文件。

## 创作输入区参考内容管理

- [x] 自动化覆盖首个、中间和末尾参考内容按稳定 ID 独立删除，且删除后其余顺序保持不变。
- [x] 折叠态提供明确的“查看全部”入口，展开态提供图片、视频、音频、文件筛选、清空全部和收起入口。
- [x] 在桌面浏览器验证上方“参考内容”打开素材选择、折叠堆叠态中间卡片删除、展开态任意卡片删除、筛选、拖动排序和收起。
- [ ] 在真实登录数据下验证“清空全部素材”同步移除全部附件引用，不影响技能引用和素材库原始文件。
- [ ] 在触屏及窄窗口验证删除按钮无需悬停也可见，素材轨道可横向浏览且不会遮挡提示词和底部控制区。

## Agnes Video 2.5 JSON 协议

- [x] 前端专项测试确认 Agnes 创建请求使用 `application/json`，不再发送 `input_reference[]`、`resolution_name`、`preset` 或像素型 `size`。
- [x] 后端专项测试确认内置 `host:agnes-video` 插件直接进入 Agnes JSON 协议执行器，不再回退到 OpenAI 风格 multipart；旧渠道能力读取时会收紧为官方 4–12 秒和 720P/960P/2K 档位。
- [x] Agnes 插件升级到 `1.2.0`：公共层像素画幅（如 `1280x720`）只转换为 `aspect_ratio: "16:9"`，公共层分辨率（如 `720`/`720p`）只转换为 `size: "720P"`，不再把两类字段混用。
- [x] 后端专项测试确认自定义渠道和系统渠道均允许官方 `/agnesapi?video_id=...&model_name=...` 轮询，并将该路径转发到 Agnes 主机根路径。
- [x] Agnes 完成态兼容 2.5 的 `metadata.url` 与 V2.0 的顶层 `url`；前端 provider 和后端协议专项测试均已覆盖。
- [ ] 使用真实 Agnes API Key 分别验证文生视频、单张首帧、三张图片 reference、视频/音频 reference；确认 `metadata.url` 可播放并且失败信息可见。
- [ ] 使用 Agnes Video V2.0 完成一次真实任务，确认顶层 `url` 能进入视频资源保存流程，不再报“没有 metadata.url”。
- [ ] 在系统渠道中验证 4 秒与 12 秒边界、720P/960P/2K、横竖画幅，以及 2.5 Flash 的 720P/最多 5 图/不支持视频限制。

## 画布生成按钮价格

- [x] 系统渠道价格按当前 operation、分辨率、时长、图片数量、质量和画幅匹配价格档，并按图片数量或视频秒数计算本次积分；逻辑前台模型继续使用后端报价。
- [x] 画布生成按钮改为价格与圆形生成操作一体式胶囊，保留完整无障碍标签；价格匹配专项测试与前端生产构建通过。
- [ ] 在画布中分别选择按次图片模型、按秒视频模型和前台逻辑模型，确认修改数量、时长、分辨率和参考素材后价格立即更新且与实际账单一致。
- [ ] 在浅色/深色主题及生成中、禁用状态下确认胶囊价格可读、圆形操作区清晰且不挤压模型与参数控件。

## 系统渠道默认积分价格

- [x] 新建渠道模型默认使用“所有规格统一价格”，管理员只需填写一次积分；1K/2K/4K、画幅和生成方式差异定价收进可选的“按规格定价”。
- [x] 系统渠道直连、前台逻辑模型按渠道计价和失败切换线路均按任务实际规格匹配价格档；缺失价格档 ID 不再被误转成 `"<nil>"`。
- [x] 后端全量测试、价格表单专项测试和前端生产构建通过。
- [ ] 在 `/admin/channels` 新建图片和视频模型，仅填写统一价格后分别生成不同质量、尺寸、分辨率和时长，确认都命中默认价格。
- [ ] 在默认价格之外新增一个 2K 或指定分辨率价格，确认精确规格优先、其他规格继续使用默认价格；关闭“可供用户使用”后该档不再参与匹配。

## 系统渠道价格折扣换算

- [x] 模型价格编辑器新增“直接填写 / 原价换算”两种录入方式；原价换算按“人民币原价 ×（上游折扣 + 售价增加）÷ 10”实时展示上游成本和用户积分售价，保存请求仍只提交最终积分价格。
- [x] 普通按次、按秒、文本/视频 Token 价格档和视频四格批量矩阵共用换算规则；可美与火山方舟均可按“无视频/含视频 × 480p/720p/1080p”自动生成 6 个可路由价格档。12 项价格表单专项测试、后端价格档匹配测试、TypeScript 检查与前端生产构建通过。
- [ ] 在 `/admin/channels` 分别编辑普通价格档和可美 Token 矩阵，使用 7.5 折、增加 0.5 折录入原价 46，确认预览为上游 ¥34.5、用户售价 36.8 积分；保存、重新打开并生成任务后，模型价格和实际账单均为换算后的最终积分价格。
- [ ] 在火山方舟 Seedance 2.0 中选择 Token 计费，使用 8 折、增加 0.5 折录入原价 `46/51/28/31`，确认售价为 `39.1/43.35/23.8/26.35` 积分/百万 Token；分别生成无视频与含参考视频的 720p、1080p 任务，确认命中对应价格档并按上游实际 `completion_tokens` 结算。
- [ ] 切换直接填写/原价换算、调整两项折扣、删除与新增规格价格，在浅色、深色和窄屏下确认原价校验、未编辑价格保护、布局和键盘操作正常。

## 系统渠道模型 Logo

- [x] 后端保存系统渠道模型 Logo，并通过脱敏模型目录下发；前端类型检查与生产构建通过。
- [ ] 在 `/admin/channels` 的模型管理中新增和编辑模型，确认 Logo 可选择、清除并在列表中立即更新。
- [ ] 关闭“前台模型”功能后刷新创作端，确认模型选择器显示系统渠道模型配置的 Logo，浅色和深色主题下均清晰可见。

## 提示词位置型资源引用与缩略图

提示词中的画布连接使用 `@图片N`、`@视频N`、`@音频N`、`@文本N` 等位置型槽位，不再把节点 ID 写入提示词；创作页附件对用户仍显示同类编号，但内部使用稳定附件身份，避免删除中间项后引用错位。以下场景仍需要在浏览器中由用户确认：

- [x] 29 项提示词引用专项测试、TypeScript 检查与生产构建通过。
- [x] 旧 `@[node:节点ID]` 仅做读取迁移；新写入、配置组装、故事板物化、Agent 建图和快捷生成均只生成位置型引用。
- [x] 图片、视频、音频、文本和角色分别独立计数；生成时按当前连线顺序解析槽位，删除连线不再删除提示词。
- [x] 视频生成与重新生成不再丢弃已连接文本；专项测试确认输入框“开始”会与上游“雨夜天台…”正文一起组成实际提示词。
- [x] 只有 `storageKey` 的图片/视频引用会异步解析预览地址；生成中按钮使用节点强调色与可见旋转动画。
- [ ] 在画布中连接图片 A 并写入 `@图片1`，删除 A 的连线后连接图片 B，确认提示词不变、缩略图切换到 B、生成请求使用 B。
- [ ] 混合连接两张图片、一段音频和一段文本，确认标签依次为 `@图片1`、`@音频1`、`@图片2`、`@文本1`，调整不同类型的连线不会串号。
- [ ] 在视频节点连接文本节点，输入一段简短动作要求后生成，确认任务详情和供应商请求同时包含输入框文字与连接文本；重新生成时保持一致。
- [ ] 打开包含旧 `@[node:...]` 的画布节点，确认面板自动迁移成可见位置标签，保存和后续编辑不再出现节点 ID。
- [ ] 使用只有 `storageKey`、没有 `content` 的本地图片和服务端资源图片，确认顶部参考栏、行内引用和放大预览均能显示。
- [ ] 点击生成，确认按钮保持节点强调色并显示连续旋转动画，不再出现纯红色空圆。
- [ ] 在 `/create` 添加至少三张参考图，并在提示词中重复引用 `@图片2`；拖动参考内容轨道中的另一张图片覆盖行内缩略图，确认提示词文本保持不变且所有 `@图片2` 缩略图同步刷新。
- [ ] 从本机拖入图片覆盖行内缩略图，确认上传期间不能发送，上传成功后原编号保持，上传失败时旧图和旧提示词不变。
- [ ] 分别测试图片模式、视频模式和文本模式，确认只有图片引用可成为替换目标，视频、音频、文件和技能引用不会误触发。
- [ ] 替换一个已经位于参考轨道中的图片，确认附件不会重复，其他引用仍指向正确素材。
- [ ] 确认浅色/深色主题、窄窗口和同一图片多次出现时，拖入高亮与“替换”遮罩清晰且不遮挡相邻文字。

## 已有图片节点图生图引用

- [x] 已有图片节点显式使用 `@图片1` 时会把节点自身作为参考图提交；未显式引用时仍保持文生图语义。
- [x] 无法解析的 `@图片N`、`@视频N`、`@音频N` 等画布引用会在提交前报错，不再静默丢失素材。
- [x] 后端专项测试确认 OpenAI Images 图生图使用 `POST /v1/images/edits` 和 `multipart/form-data`，并携带图片文件、模型、提示词、尺寸及响应格式。
- [ ] 在线上画布打开一张已有图片，输入“将 `@图片1` 图片变清晰”并生成，确认任务日志包含参考图、请求路径为 `/v1/images/edits`、Content-Type 为 `multipart/form-data`。
- [ ] 在同一图片节点移除 `@图片1` 后再次生成，确认请求恢复为 `/v1/images/generations`，不会隐式携带旧图片。

## 画布图片与音频组合的视频模型选择

- [x] 图片或角色与音频组合时继续按 `image_to_video` 匹配，音频仅作为附加参考；纯音频仍按 `audio_to_video`，包含参考视频时仍按 `reference_to_video`。
- [x] 模型选择专项测试覆盖“支持图生视频且允许参考音频”的模型，不再错误提示“不支持全模态参考”。
- [ ] 在线上画布只连接一张图片和一段音频，确认支持图生视频与参考音频的视频模型可选择，并能正常提交生成任务。
- [ ] 分别验证纯音频、图片加音频、视频加音频三种输入，确认模型筛选和任务 operation 依次为 `audio_to_video`、`image_to_video`、`reference_to_video`。

用户确认上述场景后，再将对应条目移动到功能文档的已验证范围。

## 工作区、Agent、画布复制、模型配置与创作附件修复

- [x] 前端 TypeScript 检查与生产构建通过；提示词引用、创作附件和侧栏主题专项测试通过。
- [x] 空间工作区与普通页面的左侧菜单统一使用 `--workspace-navigation`，不再随路由切换表面色。
- [x] 本机 Agent 输入框改为 Enter 换行、Cmd/Ctrl+Enter 发送；本轮模型失败会保留原请求快照并提供手动重试，不重复插入用户消息。
- [x] 文本节点展示区支持框选复制，复制文本在 Clipboard API 不可用时使用兼容回退；图片节点右键增加“复制图片”，非 PNG 图片会转为 PNG 后写入系统剪贴板。
- [x] 后台模型配置的章节标题与内容、价格档匹配条件与计费状态均改为上下结构。
- [x] 创作页附件删除按钮隔离 Reorder 拖拽事件；附件引用使用稳定 token，删除第三张不会让第四、第五张引用串位。
- [ ] 在普通列表页、创作页、空间工作区之间切换，确认左侧菜单明暗色调始终一致，折叠与移动端展开状态无闪色。
- [ ] 在本机 Agent 输入多行文本，确认 Enter 只换行、Cmd/Ctrl+Enter 和发送按钮正常提交；模拟模型中途失败后点击“重试本轮”，确认沿用原会话、文本、图片和技能且不重复用户气泡。
- [ ] 在画布文本节点中拖选部分文字并复制；分别右键 PNG、JPEG、服务端 `storageKey` 图片执行“复制图片”，确认可粘贴到系统应用。
- [ ] 在后台打开文本、图片、视频模型配置，确认所有章节为纵向阅读顺序，宽屏和窄屏都没有左右主从栏或价格档竖分隔线。
- [ ] 在创作页上传 5 张参考图，展开后依次删除第 3、5、1 张，确认每张都能就地删除，轨道不触发拖拽，提示词中的其余引用仍指向原附件。

## Agnes AI 官方视频协议

- [x] Agnes 协议已启用，覆盖 `agnes-video-v2.0`、`agnes-video-2.5`、`agnes-video-2.5-flash` 的官方创建字段、状态、失败信息和 `metadata.url` 结果解析。
- [x] 轮询使用同一 Base URL 主机的根路径 `/agnesapi`，专项测试确认 `https://apihub.agnes-ai.com/v1` 不会被拼成错误的 `/v1/agnesapi`。
- [x] Flash 的 720P、最多 5 张图片、禁止参考视频规则已在请求前强校验；协议与 provider service 专项测试通过。
- [ ] 使用真实 Agnes API Key 分别完成 V2.0 文生视频、单图生视频、2.5 关键帧、2.5 reference、2.5 Flash 的创建与轮询冒烟。
- [ ] 验证真实失败任务能显示官方 `error.message`，429/5xx 保持现有上游错误语义，生成成功后视频能进入影策资源保存流程。

用户确认上述场景后，再将对应条目移动到功能文档的已验证范围。

## 插件分层管理

- [ ] 管理员在 `/admin/plugins` 停用官方应用插件后，普通用户的有效状态和新请求同时被阻止；重新开放后保留原个人启用选择。
- [ ] 两个普通用户分别启用和停用同一官方应用插件，确认状态、Eagle 配置和工作流入口互不串号。
- [ ] 普通用户不能切换系统协议或管理员上传插件；关闭“向普通用户显示系统插件”后，两类插件均不可见。
- [ ] 用户停用 RunningHub / ComfyUI 后不能提交新任务，但已接收任务、Bridge 轮询、心跳和结果回传可以完成。
- [ ] 管理员上传、停用、启用和卸载自定义插件后，插件包、平台状态、用户状态与审计日志一致。
- [ ] 验证 `/plugins` 与 `/admin/plugins` 的浅色/深色主题、窄屏、空态、筛选、上传弹窗和开关失败回滚。

## RunningHub 与 ComfyUI Bridge 工作流

- [x] 后端全量测试、前端类型检查、Canvas Agent 构建和原生 Bridge Go 测试通过。
- [x] 工作流插件停用状态覆盖设置入口、画布创建、配置面板、任务创建、管理 API 和 worker 执行校验。
- [x] 覆盖后端重启后请求领取、结果先落库再恢复、原请求 ID 幂等复用，以及 RunningHub API Key 不进入 URL 的自动化测试。
- [ ] 使用真实 RunningHub Workflow 和 App 分别验证参数发现、图片/视频/音频输入映射、提交、轮询、取消和结果下载。
- [ ] 使用真实 ComfyUI API 工作流验证 Bridge 注册、心跳、领取、完成、取消和大文件结果回传。
- [ ] 在 Bridge 执行中重启后端，确认结果回传成功且任务不重复执行；在请求未领取时重启后端，确认原请求继续投递。
- [ ] 验证浅色/深色主题、窄屏设置页、画布工作流节点和任务中心状态展示。

## AI 提示词优化器

当前代码和生产构建已完成，但以下场景仍需要在真实配置和不同窗口尺寸下由用户确认：

- [x] `web` 前端 TypeScript 检查与生产构建通过。
- [x] 优化面板打开、关闭不再等待 Popover 入场或退出动效。
- [ ] 在 `/create` 中分别测试图片和视频模式的打开、发送、取消、失败重试和关闭。
- [ ] 在图片、视频画布节点中测试优化入口、结果采用和原提示词回填。
- [ ] 逐项测试五种优化方式，确认模式切换不会丢失当前输入。
- [ ] 测试优化模型切换，并确认目标生成模型与优化模型显示和请求职责分离。
- [ ] 测试已连接图片、文本参考，以及“结合参考”没有参考内容时的提示和禁用状态。
- [ ] 测试长提示词、长优化结果、滚动、拖动和四边缩放；确认窗口接近边缘时不会跑出视口。
- [ ] 测试浅色/深色主题和窄窗口布局，确认按钮、输入框和结果文本仍可读且可操作。
- [ ] 使用自定义文本渠道实际发送一次，确认渠道、模型和权限配置正确时不会误报“必须指定系统渠道和模型”。

用户确认上述场景后，再将对应条目移动到功能文档的已验证范围。

## 肖像可识别性排查画布插件

首版画布节点、本机签名运行时、本地 ONNX 预检、百度候选链路、模型作业协调器和四种报告导出已接入；以下边界仍需要真实环境确认：

- [x] Agent TypeScript 构建、Web TypeScript 检查通过。
- [x] 本机任务持久化、幂等、取消/重试清理、候选 URL 安全校验和报告转义测试通过。
- [x] Markdown、无脚本 HTML、DOCX 共用检测结论、本地预检、六维特征、修改建议和失败候选说明；报告结构回归测试通过。
- [ ] 使用固定真人/虚拟人图片建立 Python 原实现与 TS 实现的检测框、embedding 和风险金标。
- [ ] 在安装 `det_10g.onnx`、`w600k_r50.onnx` 的 Windows 环境完成直接比对和本地仅模式验证。
- [ ] 在系统 Chrome/Edge 可用环境完成百度识图、验证码、页面结构变化和候选下载失败验证。
- [ ] 配置支持双图输入的项目视觉模型，验证 Path A、Path B、模型租约、并发、取消和页面重开恢复。
- [ ] 浏览器确认插件首次关闭、旧节点只读、明暗主题、窄屏、键盘焦点和候选添加到画布。

用户确认上述场景后，再将对应条目移动到功能文档的已验证范围。

## 画布节点自动整理

自动整理入口和布局提交逻辑已完成，以下场景仍需要在实际画布中手工确认：

- [x] `web` 前端 TypeScript 检查与生产构建通过。
- [x] 画布底部视图控制中显示“自动整理节点”按钮。
- [ ] 无选中节点时，确认多个顶层节点会按连线流程布局；无连线时会按网格布局。
- [ ] 选中节点时，确认只移动选中的可移动节点，未选中节点保持原位。
- [ ] 确认锁定节点、容器节点和折叠批次隐藏子节点不会被移动。
- [ ] 点击“撤销”确认可以恢复整理前的位置，并确认刷新画布后布局仍能保存。
- [ ] 在浅色/深色主题、窄窗口和节点数量较多的画布中确认底部按钮可见、可操作且不遮挡主要内容。

## 分镜脚本资产关联

1. 在同一画布放入角色、场景图片、参考视频和音频后生成分镜，确认各镜头只关联模型返回的真实画布资产，不出现失效或编造 ID。
2. 分镜卡片按“序号、时长、视频提示词、台词/旁白、关联资产”五列显示；图片、视频和音频缩略图可点击并打开对应预览。
3. 从单个镜头及批量入口创建视频节点，确认脚本行输出和每个关联资产都自动连到视频节点，重复同步不会产生重复连线。
4. 打开视频节点提示词，确认关联资产以标准 `@资产` 缩略图展示，生成请求能解析图片、视频和音频引用。
5. 修改镜头资产关联后再次同步，确认已移除的托管资产连线被清理，而用户手工创建的普通连线仍保留。

## 零价免费模型

1. 管理员设置启用系统渠道模型的按次价格为 `0` 后，普通用户刷新设置即可看到并成功创建任务。
2. 前台逻辑模型使用统一按次价格 `0` 后，普通用户可见并能成功创建任务。
3. 零价任务完成后，账单状态为已结算，用户可用积分和预留积分均不变化。
4. 零价 Token 任务在上游未返回 usage 时仍能以 `0` 结算；正价 Token 任务继续依赖 usage 结算。
5. 管理端提交任一负价格仍会被拒绝。
6. 未勾选价格已配置的默认零价模型仍不会向普通用户公开。

## 用户诊断包导出

当前实现已完成前后端链路、脱敏和设置页 UI，但仍需要真实环境验证下载链路和不同错误场景：

- [x] `web` 前端 TypeScript 检查与生产构建通过。
- [x] 诊断服务已覆盖脱敏、ZIP 结构和跨用户任务拒绝测试。
- [x] 设置页桌面布局和 680px 窄屏布局已完成浏览器预览。
- [ ] 登录后从设置页导出成功，确认 ZIP、诊断编号和文件名均可用。
- [ ] 从失败任务详情进入诊断页，确认 `taskId`、`projectId` 和对应日志范围正确带入。
- [ ] 后端未重启或接口未部署时，确认前端能显示可操作的错误提示，而不是原始 Blob/502 文案。
- [ ] 验证浅色/深色主题、15 分钟到 24 小时范围、长问题描述和导出中的 loading 状态。

## Eagle 素材库

- [x] 增加素材无文件夹、无标签时的空集合归一化单元测试。
- [ ] 使用真实 Eagle 服务验证无文件夹素材、无标签素材、文件夹路径和预览引用。

## Windows 本地开发

- [ ] 在一台依赖已安装和一台全新工作区分别运行 `.\scripts\start-local.ps1`。
- [ ] 验证端口被占用时脚本会安全退出，且不会关闭已有进程或使用正式开发数据目录。

## 画布生成结果资源恢复

- [x] `web` 前端 TypeScript 检查与生产构建通过。
- [ ] 模拟图片生成任务已成功但结果下载失败，确认节点显示“重新加载资源”和“重新生成”两个按钮。
- [ ] 点击“重新加载资源”，确认只重新查询并消费原任务结果，不创建新的生成任务或重复扣费。
- [ ] 模拟视频结果落盘/OSS 上传失败后再次加载，确认恢复成功会清除错误状态并保留原任务信息。
- [ ] 点击“重新生成”，确认仍走原有生成重试流程；普通生成失败仍只显示“重新生成”。

## 数据同步与资源删除重构

- [x] 前端远端基线、登录 hydrated 屏障、上传并发编辑和删除持久化回归测试通过。
- [x] Generation 失败只回滚生成字段，保留同节点并发标题编辑；重复 Generation 仍按明确 effect key 幂等。
- [x] 资源引用识别改为显式字段合同，未登记的字段名和自由文本不会被推断为资源引用。
- [x] 后端素材删除改为数据库事务 + `resource_deletion_jobs` Outbox，数据库失败时物理文件保持不动。
- [x] Outbox Worker 本地文件删除、对象不存在幂等成功和失败退避测试通过。
- [ ] 使用真实 OSS 配置验证对象删除成功、权限失败后的退避重试和多实例租约接管。
- [ ] 浏览器真实环境验证登录快照慢响应期间工作区保持不可写，以及删除后立即刷新不复活素材。

## S3 兼容存储

- [x] 后端 S3 单次上传、Range 读取、预签名地址、Session Token、幂等删除、配置版本和迁移清单自动化测试通过。
- [x] 前端 S3 预设、关键字段重测判断、默认 `open-ai-canvas` 路径前缀、TypeScript 检查和生产构建通过。
- [ ] 分别使用 AWS S3、Cloudflare R2、Backblaze B2 和 RustFS 完成写入、Range 读取、删除及历史资源恢复验证。
- [ ] 验证 Path Style 开启时强制路径寻址，关闭时 AWS 使用虚拟主机寻址、其他自定义 Endpoint 自动使用路径寻址。
- [ ] 验证平台切换到本机后只影响新上传，历史 S3 资源仍可读取和删除；个人 S3 停用后正确回退平台存储。
- [ ] 验证同一 S3 修改 Bucket、Endpoint、Region、前缀或 Path Style 时，留空 Secret 和 Session Token 可继续复用，并要求重新连接测试。
- [ ] 使用私网 RustFS 验证浏览器同源代理，以及提交给模型上游时转换为影策公网 HTTPS 短时地址；未配置公网 HTTPS 地址时应明确拒绝。
- [ ] 验证管理员关闭个人 S3 后不再接受个人新上传，但已有个人 S3 资源仍可读取和删除。

## 3D 导演台时间轴关键帧删除与快捷键

关键帧删除入口、全局快捷键和交互控件守卫已接入，以下场景仍需要在浏览器中由用户确认：

- [x] `web` TypeScript 检查、导演台专项测试与全量前端测试通过。
- [x] 关键帧删除覆盖对象 transform、对象骨骼和摄影机三类轨道；未命中时返回同一场景引用，不记历史、不产生修订。
- [x] 时间轴关键帧为可 Tab 聚焦的真实 button；点击或 Enter/Space 只选择并定位播放头，Delete/Backspace 或顶栏删除按钮才删除，避免普通点击误删。
- [x] 所选关键帧可切换保持、线性、平滑缓动；缓动同时作用于对象/摄影机 Transform 与骨骼旋转插值，旧数据默认线性。
- [x] 切换镜头/摄影机或外部删除关键帧后会清除陈旧选择，顶栏缓动和删除控件不会继续修改不可见轨道。
- [x] 全局快捷键在输入框、按钮、链接、role 型控件、可编辑区域及其子元素内一律不解析；仅在动作真正执行时 `preventDefault`。
- [x] 鼠标点选场景对象、dock 工具、模式按钮、快速添加和顶栏图标按钮后会释放焦点，全局快捷键立刻可用；键盘激活（Enter/Space）保留焦点，Tab 序列不断。
- [ ] 在真实画布中记录多个关键帧后逐个删除，确认删除的是目标帧、其余帧顺序不变，且「撤销」能恢复。
- [ ] 折叠对象轨道后仅通过汇总轨删除关键帧，确认收起状态下仍可删除。
- [ ] 用键盘 Tab 走到关键帧上按 Enter/Space，确认只选择并把播放头定位到该帧；再按 Delete，确认删除的是该关键帧而不是整个对象。
- [ ] 对同一段动画依次选择保持、线性、平滑缓动，确认播放结果明显不同，撤销可恢复上一个缓动设置。
- [ ] 选中摄影机关键帧后切换到使用另一台摄影机的镜头，确认旧选择和顶栏缓动/删除控件立即消失。
- [ ] 确认浅色/深色主题下关键帧的悬停环与焦点环可见，命中区易于点中。
- [ ] 逐个验证 W/E/R、Delete/Backspace、H、Escape、Ctrl/Cmd+Z、Ctrl/Cmd+Shift+Z、Ctrl/Cmd+Y 和空格；确认在标题输入框内输入这些字符不会触发快捷键。
- [ ] 在场景列表点选一个对象后直接按 Delete，确认删除的是该对象；再点 dock 的「旋转」后按 W，确认能切回移动。
- [ ] 用键盘 Tab 走到场景对象行按 Enter 选中，确认焦点仍停在该行、可继续 Tab 到下一项。

## 3D 导演台场景模板与四模式骨架

新建场景模板和摆场/姿态/动画/摄影机四个一级模式已接入，以下场景仍需要在浏览器中由用户确认：

- [x] `web` TypeScript 检查、导演台专项测试与全量前端测试通过。
- [x] 导演台 P0 Chrome E2E 全部通过；场景 A 已同步为「默认摆场不显示时间轴与 AutoKey，切到动画模式后 AutoKey 默认关闭」。
- [x] 五个模板结构确定、每次实例 id 独立、对象 XZ 占位两两不相交，判据与 `resolveDirectorPlacement` 一致。
- [x] 空场景不含任何演员；未知模板 id 回落到空场景。
- [x] 模板不改动 `DirectorScene` 结构与 `version`；`mode` 只存在于工作台 store，不写入场景。
- [x] 离开动画模式会停止播放并关闭 Auto Key，并把渲染视图夹回当前模式允许的集合。
- [x] 动画模式会根据 Transform 关键帧绘制可见演员与摄影机的空间轨迹，包含起终点、方向和当前播放进度；隐藏演员及零长度轨迹不显示。
- [x] 播放头按镜头 FPS 而非固定 60 次/秒更新，跨过镜头末尾时保留余量；轨迹长度只在对象/摄影机内容变化时重算。
- [x] 「按运镜生成轨迹」只更新首尾关键帧，保留手工中间帧、关键帧 ID 与 easing，并提示切到动画模式继续编辑。
- [x] 截图/录制绑定开始时的场景快照；上传完成后重新核验节点与镜头，并只把预览引用合入最新场景，不用旧快照覆盖并发编辑。
- [ ] 从工具栏和画布右键菜单分别新建镜头，确认都会先弹出模板选择，且右键位置会作为新节点位置。
- [ ] 逐个使用五个模板新建镜头，确认空场景没有演员，单人/双人/走位演员数量正确，产品镜头含产品主体、背景板和侧面道具且没有演员。
- [ ] 确认双人对话两名演员面对面、走位三名演员互不重叠、产品镜头道具不穿模。
- [ ] 打开一个已保存的镜头，确认不弹模板选择、场景内容未被改写。
- [ ] 在摆场模式确认时间轴、Auto Key、记录关键帧、骨骼与姿势预设、深度/法线视图都不可见。
- [ ] 在姿态模式选中演员，确认骨骼与姿势入口出现；未选中演员时不出现；确认不会自动产生关键帧。
- [ ] 在动画模式开启 Auto Key 并开始播放，再切到其他模式，确认播放停止、Auto Key 关闭，且场景没有被继续写入关键帧。
- [ ] 在动画模式为演员和摄影机分别记录至少两枚位置不同的 Transform 关键帧，确认轨迹、起终点、方向锥和当前进度点可见；隐藏演员后其轨迹同步消失，离开动画模式后全部轨迹消失。
- [ ] 在摄影机模式确认右栏固定显示镜头/摄影机检查器，对齐视图与运镜可用，深度/法线视图可选。
- [ ] 在摄影机已有关键帧且播放头不在 0 秒时执行「摄影机对齐当前视图」，确认当前帧立即保持新机位，旧关键帧仍在且撤销可恢复。
- [ ] 在摄影机已有首尾帧和至少一枚带缓动的手工中间帧时执行「按运镜生成轨迹」，确认只更新首尾位置，中间帧、缓动与撤销历史都保留。
- [ ] 开始截图或白膜录制后切换活动镜头、删除源节点或继续编辑场景，确认旧输出被拒绝或安全合并，不写进另一项目、不复活节点、不覆盖新编辑。
- [ ] 确认模式切换不丢失草稿、撤销历史和保存状态，切换后标题与保存指示器仍正确。
- [ ] 确认浅色/深色主题下模式按钮的选中态、悬停态与焦点环可见，选中态不是主操作色。
- [ ] 在窄窗口（小于 lg 断点）确认模式切换仍可见可点击。

用户确认上述场景后，再将对应条目移动到功能文档的已验证范围。

## 3D 导演台取景模式（3D/CAM/五个正交视图）

新增 `viewMode`（自由/机位/顶前后左右五个正交轴向）取景切换，独立于摆场/姿态/动画/摄影机四个一级模式；以下场景仍需要在浏览器中由用户确认：

- [x] `web` TypeScript 检查通过；`director-view-modes.test.ts`、`director-modes.test.ts`、`director-template-mode-wiring.test.ts` 专项测试通过。
- [x] 三台相机对象（自由观察、机位、正交）互相独立持有，切换 `viewMode` 只挪动视口的活动相机指针，源码层面确认不共享可变状态。
- [x] 正交轴向使用真正的 `THREE.OrthographicCamera`，取景来自场景内容包围盒，而非把透视相机换个角度伪装；取景解算与视口接线均有专项测试覆盖退化场景（空场景、非法数值）。
- [ ] 逐个切换 3D/CAM/顶视/前视/后视/左视/右视，确认画面确实是对应视角，正交视图没有透视畸变。
- [ ] 放入大比例背景板和旋转后的长道具，逐个切换正交视图，确认对象完整进入画面，不再因只按对象中心构图而裁切。
- [ ] 在自由视角环绕后切到机位或任一正交视图，再切回自由视角，确认之前的环绕位置原样恢复，没有被拉回默认机位。
- [ ] 在机位视图播放/拖动时间轴，确认摄影机运镜正确预览；切到正交视图确认环绕已锁定、拖拽无效。
- [ ] 确认取景切换器（3D/CAM/TOP/FRONT/BACK/LEFT/RIGHT 七个按钮）可键盘 Tab 到达、Enter/Space 激活，`aria-pressed` 状态正确朗读。
- [ ] 确认切换取景模式不产生新的撤销历史记录，也不触发保存/未保存提示。
- [ ] 在机位或正交视图下点击「导出预览图」「录制白膜视频」，确认导出流程仍正常完成（不因当前相机是正交投影而失败）。
- [ ] 确认浅色/深色主题下取景切换器的选中态、悬停态与焦点环可见。
- [ ] 在窄窗口（小于 lg 断点）确认取景切换器仍可见可点击，不与其他浮层重叠遮挡。

用户确认上述场景后，再将对应条目移动到功能文档的已验证范围。

## 3D 导演台首次引导与窄屏属性面板

首次引导已接入真实登录用户 scope，并按 Issue #305 改为“添加演员 → 移动 → 调姿 → 轨迹 → CAM → 应用”六步；以下场景仍需要浏览器确认：

- [x] 引导进度按认证用户 ID 隔离，不回落 guest；重启入口在加载期间也可用，重置失败会重新读取原进度。
- [x] 已跳过或已结束的引导可从标题栏重新开始；最后一步使用“结束引导”中性文案，不宣称用户已经完成实际任务。
- [x] 小于 `lg` 断点时右侧属性区移动到工作区下方且可滚动，不再直接隐藏姿势与骨骼控制。
- [x] `director-onboarding.test.ts` 专项测试与 `web` TypeScript 检查通过。
- [ ] 使用两个真实账号分别推进、跳过和重启引导，确认进度互不串号；切换账号时旧读取结果不会闪现。
- [ ] 从新账号首次打开导演台，逐步完成添加演员、移动、姿态、两枚 Transform 关键帧、CAM 构图与应用，确认六步文案都指向真实可见入口。
- [ ] 在引导加载期间连续点击“重新开始引导”，再模拟 IndexedDB 写入失败与恢复，确认界面不会永久隐藏或假装推进。
- [ ] 在窄窗口检查摆场、姿态、动画、摄影机四种模式：标题栏可横向到达，底部属性面板可滚动，时间轴与引导不互相遮挡。

用户确认上述场景后，再将对应条目移动到功能文档的已验证范围。

## 模型请求失败日志完整性

模型请求日志已补齐 HTTP 失败关联、错误搜索与请求前失败兜底；以下场景仍需要在管理后台由用户确认：

- [x] HTTP 非 2xx 日志同时保留节点使用的中文错误归类和上游原始错误详情。
- [x] API 调用日志关键词搜索覆盖 `errorCode` 与 `error`，可按节点中文提示、上游错误或错误码定位。
- [x] 参数校验、协议构造、路由准备等请求尚未发出的最终失败会补记 `request_not_sent`，且不保存任务输入、API Key 或请求正文。
- [x] 已有真实 HTTP 调用日志的任务不会重复补记请求前失败日志。
- [ ] 在画布分别触发一次成功请求、一次上游 400 和一次本地参数校验失败，确认 `/admin/logs` 均能看到对应记录，并可按节点错误文案搜索。

## 渠道视频分辨率枚举兼容

画布已将渠道声明的分辨率视为不透明枚举，不再把带方向或本地化后缀的值静默回退为 `720p`；以下场景仍需浏览器确认：

- [x] 前端分辨率规范化、画布选中态、价格匹配与请求映射专项测试通过。
- [x] AutoDL 插件 `1.0.2` 已改用通用 `lower` 并通过协议包测试；宿主仅保留旧 `1.0.1` 清单所需的兼容 shim。
- [x] Manifest 工作流的视频能力改为按 `parameters` 投影：只声明分辨率时不再补画幅能力，也不会继续提交旧的全局 `size`。
- [x] 同时支持分辨率和画幅的渠道按当前比例与分辨率推导只读宽高，例如 `16:9 + 720P = 1280×720`、`16:9 + 1080P = 1920×1080`、`16:9 + 4K = 3840×2160`。
- [x] `web` TypeScript 检查与生产构建通过。
- [x] 后端允许视频能力明确省略画幅，并通过全量 Go 测试；前端能力投影、尺寸计算和画布模型专项测试通过。
- [ ] 在画布选择 `480p竖`、`768p竖`、`480p横`、`768p横`，确认按钮选中态、底部摘要和重新打开后的状态一致。
- [ ] 分别提交四个枚举值，确认请求中的 `resolution` 与插件声明一致，且其他标准 `720p`、`1080p` 渠道不受影响。
- [ ] 使用仅声明 `resolution` 的 AutoDL 工作流，确认画布和创作页不显示画幅/尺寸，生成请求不包含无意义的 `aspectRatio`。
- [ ] 使用同时声明画幅与分辨率的渠道，依次切换 `16:9` 下的 720P、1080P、4K 及 `9:16`，确认宽高展示随选择即时更新且请求仍按渠道协议发送比例与分辨率。

## 画布生成重复提交保护

画布生成入口已增加节点级同步提交锁，并为手动生成记录“提示词、模型、参数、参考素材”指纹；以下场景仍需浏览器确认：

- [x] 连续三次调用同一节点只执行一次的专项测试通过；提示词、模型、参数或参考素材变化会产生不同指纹。
- [x] `web` TypeScript 检查、21 个相关专项测试与生产构建通过。
- [ ] 在同一节点快速连续点击三次生成，确认任务中心只新增一个任务，且不会重复扣除积分。
- [ ] 首次任务完成后不修改提示词、模型、参数和参考素材，再次点击生成，确认出现“再次生成相同内容”二次确认；取消不创建任务，确认后只创建一个新任务。
- [ ] 分别修改提示词、模型、分辨率/时长和参考素材后生成，确认视为新内容，不弹重复确认。
- [ ] 执行分镜批量生成、Agent 自动生成和显式重试，确认不会被手动重复确认弹窗阻塞。

## 画布快捷键中心

画布搜索已统一为 `Ctrl/Cmd + F`，专注模式调整为 `Shift + Ctrl/Cmd + F`；原快捷键长列表已改为可搜索、可分类的快捷键中心。以下场景仍需浏览器确认：

- [x] 快捷键目录筛选专项测试、`web` TypeScript 检查与生产构建通过。
- [ ] 在 Windows 和 macOS 分别按 `Ctrl/Cmd + F`，确认打开画布节点搜索且不会触发浏览器页内查找。
- [ ] 按 `Shift + Ctrl/Cmd + F`，确认进入或退出专注模式；按 `Ctrl/Cmd + K` 不再打开节点搜索，并保留工作区命令面板入口。
- [ ] 打开快捷键中心，分别搜索“粘贴”“缩放”和“Alt L”，确认结果与分类计数正确，清空搜索后恢复完整列表。
- [ ] 确认浅色/深色主题下标题、分类选中态、搜索焦点环、键帽和滚动区域清晰可见。
- [ ] 在窄窗口确认分类可横向滚动、快捷键列表改为单列，弹窗不超出视口且长内容仅在列表区滚动。

## 管理后台在线更新

管理后台已新增“系统配置 → 系统更新”，并通过宿主机 Host Updater 执行强制备份、迁移、切换、健康验证和失败回退；以下场景需要在真实 Linux Docker Compose 服务器完成最终确认：

- [x] 后端 Host Updater、Unix Socket 客户端、管理员 API、版本比较和状态持久化相关测试通过。
- [x] 前端 TypeScript 检查、生产构建、侧栏入口、更新器未安装空态、更新器连接态、GitHub Release 日志、浅色/深色主题浏览器验收通过。
- [x] Compose 配置校验、systemd 安装脚本语法和 Release 二进制构建命令通过。
- [ ] 在生产更新前生成 PostgreSQL + 数据目录 ZIP，确认 `.partial` 不会被当作有效备份，最终 ZIP 可打开且 SHA-256、元数据、`database.dump`、`backend-data.tar` 均有效。
- [ ] 从首个在线更新版本升级到包含 schema v2 的测试版本，确认旧服务先 drain，迁移服务独占执行，目标 build version 和 schema version 在稳定窗口内一致。
- [ ] 人为让目标版本健康检查失败，确认自动恢复旧 Compose、旧镜像和数据库备份；旧版本因 schema 过高拒绝启动时，必须在数据库恢复完成后才能重新健康。
- [ ] 在拉取镜像、创建备份、迁移、启动和健康检查阶段分别模拟失败，确认停服前失败不会改变运行版本，停服后失败进入回退，回退失败明确显示“需人工介入”。
- [ ] 更新期间刷新页面、关闭浏览器和 Backend 短暂不可用，确认宿主机操作不中断，恢复后日志与最终状态完整。

## 管理后台存储资源

管理后台已新增资源列表、筛选、去重容量统计、预览下载和安全批量删除；以下场景仍需使用真实资源数据确认：

- [x] 后端专项测试覆盖管理员权限、筛选校验、共享对象去重统计、历史本地资源预览、部分删除成功、公告/画布引用阻止、共享物理对象保留、审计失败事务回滚和 100 项上限。
- [x] `web` TypeScript 检查通过；只读阶段与删除阶段均复用同一资源页面和统一 API 客户端。
- [ ] 在 `/admin/resources` 分别按资源 ID、对象路径、用户 ID、类型、状态和存储类型筛选，确认分页与筛选 chip 一致。
- [ ] 预览并下载本地、阿里云 OSS、腾讯云 COS、七牛云 Kodo 和 S3 的图片、视频、音频与普通文件，确认 Range、登录 Cookie 和错误提示正确。
- [ ] 选择一个仍被公告、素材、画布、项目、风格、工作流或镜头产物引用的资源，确认删除结果显示真实引用来源且业务数据和物理对象均保留。
- [ ] 批量选择“可删除、仍被引用、不存在”三类资源，确认只删除安全项，列表和统计刷新，审计事件与 outbox 数量一致。
- [ ] 创建两条共享同一物理对象的资源记录，只删除其中一条，确认另一条仍能预览且物理对象未清理；两条同时删除时确认只产生一个物理删除任务。
- [ ] 模拟对象存储暂时失败，确认资源记录事务仍成功、删除任务保留并重试，恢复后物理对象最终清理。

## 短剧分镜工作流工作台

短剧工作流已按镜头生产重构为单层项目工作台，并将资产绑定、镜头编辑、生成预览和整集镜头轨道放入同一视口；以下场景仍需用户在真实项目数据与模型渠道下确认：

- [x] 工作流路由保留系统全局侧栏和顶栏；项目头部压缩为单行，移除额外六阶段横条，项目导航入口改为“分镜制作”。
- [x] 项目详情 API 对旧后端可能缺失的工作流、镜头版本、产物、引用、候选资产和文件夹数组统一规范为空数组；使用缺失这些字段的响应完成浏览器复现，页面正常加载且控制台无错误。
- [x] 未初始化章节不再显示阻断式居中空态；镜头编辑器可直接查看和保存，首次真实生成时按当前章节自动建立工作流记录。
- [x] 分镜脚本、黑白动作预演、视频生成共用资产栏、镜头表单、预览/历史栏和底部镜头轨道；前端生产构建通过。
- [x] 模型、时长、画幅、分辨率和镜头语言合并为默认折叠的“生成设置”，模型提示词与接戏信息合并为默认折叠的“生成补充”；折叠摘要会显示当前关键参数，浏览器展开/收起验证通过。
- [x] 删除项目头部重复且发生重叠的“新增分镜”入口，只保留底部镜头轨道中的新增卡片；1280px 与 1680px 浏览器检查无横向溢出，系统全局侧栏保持可见。
- [x] 右侧产物阶段从标题旁的隐藏下拉框改为常驻三段切换，分镜图、动作预演和镜头视频始终可见；1280px 与 1680px 下切换路由、阶段文案、最新/历史状态和全局侧栏浏览器验证通过。
- [x] 底部镜头卡片取消悬浮上移和向外扩张的选中阴影，改为不改变坐标与尺寸的底色、边框和内描边反馈；右侧移除“复制参数”“入素材库”，仅保留并排的重新生成与下载，1280px 浏览器悬浮和布局验证通过。
- [x] 镜头编辑器改为脚本优先顺序：镜头名称与画面、镜头资产、表演与动作、对白/旁白优先展示，生成规格和模型补充保持折叠；字段统一为画幅、镜头时长、机位角度、运镜方式、排除内容和接戏备注等生产术语。
- [x] 编辑器标题实时反映镜头名称与“已保存/有未保存修改/保存中”状态；无修改时禁止重复保存，切换或新增镜头前会拦截未保存修改。浏览器交互验证通过。
- [x] 镜头保存使用真实版本写接口；资产点击绑定具体版本；生成统一进入后端任务生命周期，成功后登记对应镜头产物；下载使用带登录凭据的资源接口。
- [x] “生成设置”新增技能库多选，分镜图、动作预演和镜头视频统一通过 Skill Runtime 解析，任务与镜头产物记录技能版本和实际引用文件。
- [x] 镜头资产支持再次点击或从已绑定标签取消引用；解绑使用归属校验的独立写接口，并使该镜头旧产物失效。动态提示词复用画布 `@` 引用编辑器，已绑定图片和角色会按实际参考图顺序解析后提交；后端全量测试、前端专项测试和生产构建通过。
- [x] 镜头生成资产解析改为同时保留绑定版本视觉快照与当前角色卡声音配置；角色主图、场景图和声音样本按 `图片N` / `音频N` 写入 `【资产参考】`，并自动补充声音画像与镜头台词。历史角色版本 + 新版声音绑定的后端/前端专项回归通过。
- [x] 镜头编辑器底部在“保存脚本”左侧新增“删除镜头”；二次确认后原子清理该镜头的脚本版本、资产引用、生成产物、候选资产和生产任务关联，压紧剩余镜头顺序并自动切换到相邻镜头。
- [ ] 使用包含角色、场景、道具和画风的真实项目，确认左侧分类、绑定状态、参考素材标签和镜头切换保持一致。
- [ ] 分别删除首个、中间、末尾和唯一镜头，确认二次提示、相邻镜头切换、空态、镜头编号、总镜数以及刷新后的数据一致；有未保存修改时确认提示明确且取消不会丢失内容。
- [ ] 在同一镜头绑定角色和场景后输入 `@`，确认菜单、行内缩略图和生成请求中的“图片 N”顺序一致；分别从左侧资产行和已绑定标签取消引用，确认刷新后状态一致，旧 token 会阻止生成并提示重新选择或删除引用。
- [ ] 使用“分镜绑定角色旧版本、角色卡随后绑定声音”的真实项目生成镜头视频，确认请求包含旧版角色主图、场景图、当前声音样本、声音画像和镜头台词，且媒体角色为参考资产而非首帧。
- [ ] 配置一个图片模型和一个视频模型，分别生成分镜预览、动作预演与视频，确认任务中心、镜头历史、最新预览和阶段状态同步更新。
- [ ] 在浅色/深色主题及 1280px、1050px、760px 三个断点检查项目导航、表单滚动、右侧预览和底部镜头轨道，确认没有遮挡或不可达控件。
- [ ] 验证下载视频、复制生成参数、将任务物化产物加入项目素材库、章节切换与新增分镜；确认失败任务展示真实错误且不会伪造产物成功。

## 章节分镜生成到分镜制作

剧情章节的分镜入口已从“在画布中分镜”纠正为“生成到分镜制作”；生成结果直接写入镜头脚本，并在本章已有分镜时执行显式替换确认。以下场景仍需使用真实项目与已安装技能确认：

- [x] 章节正文、项目画风、当前角色版本和项目资产统一进入 `agent_storyboard_rows` 任务；分镜生成继续经过 `Skill Runtime` 的 `shortDrama` profile，并记录实际技能版本和引用文件。
- [x] 本章待确认角色也会进入分镜上下文；模型在 `characterIds` 中返回角色名称时，已确认角色自动映射到真实资产版本，待确认或未知角色降级为仅名称引用，不再触发结构修复请求或报废整份分镜。后端专项测试、前端映射测试与生产构建通过。
- [x] 分镜任务结果映射到镜头画面、动作、对白、景别、机位、运镜、图片/动态提示词与接戏备注；模型返回的项目资产关联随镜头在同一替换事务中保存。
- [x] 本章无分镜时直接写入“分镜制作”并跳转到分镜图阶段；已有分镜时展示镜头、脚本版本、资产引用、生成产物和候选资产影响数量，生成失败不修改旧数据。
- [x] 技能多选框占满弹窗内容宽度；专项测试、TypeScript 检查、后端服务测试和前端生产构建通过。
- [ ] 选择 1 至 4 个已安装技能生成真实章节，确认任务中心中的技能版本、文件来源和生成结果一致，完成后进入对应章节的“分镜制作 / 分镜图”。
- [ ] 使用已提取但尚未确认资产的“张振天、张天昊”重新生成真实章节，确认镜头正常落库、提示词保留角色名称；确认角色资产后再次生成，确认同名角色自动带上真实资产与版本 ID。
- [ ] 对已有镜头、历史脚本、资产引用和生成产物的章节重新生成，分别验证取消替换、模型生成失败和确认替换：前两者保留旧数据，确认替换后旧关联被移除且新资产引用可用。
- [ ] 在 560px 及更窄视口打开设置弹窗和技能下拉框，确认选择器、选项面板、长技能名称和替换警告没有横向溢出。

## 短剧项目导航与制作概览

项目设置、章节导航、制作概览、镜头轨道和画布新增入口已按长篇短剧工作流重构；以下场景仍需使用登录态真实项目确认：

- [x] 项目新增 `cover_resource_id`，服务端校验资源归属、就绪状态和图片类型；项目主图被资源删除流程识别为直接引用。章节新增持久化 `word_count`，新建、导入、更新和旧数据迁移均维护字数摘要；后端全量测试通过。
- [x] 项目设置可从个人图片素材、项目图片素材或实时上传中设置主图；项目列表优先显示主图，未设置时回退画风示意图。分镜制作头部改为上一章、搜索定位、下一章的虚拟章节导航；前端生产构建通过。
- [x] 修复项目更新仓储遗漏 `cover_resource_id`，避免接口返回“主图已更新”但数据库字段仍为空；回归测试同时校验设置与移除主图后的数据库重读结果，后端全量测试通过。
- [x] 剧情章节列表去除装饰性侧边表达并显示状态、字数、画布数；制作概览新增六阶段标准流程、当前指标、制作缺口和可点击快速入口；底部镜头卡增加画面、加粗时长、镜头参数、产物版本与绿色成功状态。
- [x] 画布空白区域双击改为直接打开已有新增节点面板，并保留双击位置作为节点落点；原“适配内容”能力继续由缩放控制区提供。
- [ ] 在项目设置分别从个人素材、项目素材和上传图片设置主图，刷新项目列表后确认图片一致；替换、移除主图后确认回退逻辑正确，尝试删除当前主图资源时确认显示项目引用来源。
- [ ] 使用 2500 章项目搜索章节序号和标题，确认定位弹窗桌面端为 5 列、默认每页 100 章，搜索区与分页区保持同一表面；连续切换上一章、下一章并刷新页面，确认当前位置、总章数、字数和路由一致。
- [ ] 在制作概览点击剧情、资产、分镜、预演、视频、交付和三个快速入口，确认跳转到正确章节及阶段，真实镜头产物数量与完成状态一致。
- [ ] 在浅色、深色主题和 1280px、1680px 宽度检查镜头卡，确认时长明显、标题与镜头参数不溢出、“已生成”为绿色且选中态仍使用工作台强调色。
- [ ] 在画布不同缩放和平移位置双击空白处，确认新增节点面板直接打开，选择文本、图片、视频和扩展节点后落点接近双击位置；双击节点、弹窗和其他浮层不应触发新增。

## 工作台首页创作仪表盘改造

参考模板 Dashboard 将 `/home` 首页重构为创作仪表盘（欢迎区 + 统计卡 + 活跃度/素材图表 + 最近项目表格），以下场景仍需要浏览器确认：

- [x] `web` 前端 TypeScript 检查通过；旧首页样式类已随页面重写移除。
- [ ] 登录用户确认欢迎区显示用户名、主操作跳转 `/projects?create=1`、打开画布按钮在未登录时引导登录。
- [ ] 确认统计卡数值与项目/画布/素材/任务页面一致：进行中项目、自由画布、素材资产、本周任务。
- [ ] 确认创作活跃度图表随 7/14/30 天切换刷新，生成任务与画布更新两个系列与任务中心/画布列表一致。
- [ ] 确认素材构成环形图按类型统计正确，无素材时显示空态文案。
- [ ] 确认最近项目表格的搜索、状态筛选、排序与分页行为，行点击进入项目概览；无项目时显示创建入口。
- [ ] 确认快捷创建与工作流面板入口可用，工作流提示显示最近项目阶段与章节完成度。
- [ ] 验证浅色/深色主题与窄窗口（表格横向滚动、统计卡换行）下可读可操作。

## 创作页生成结果跨页面锁兼容

创作页的远程生成结果认领、素材入库和画布持久化已在浏览器原生 Web Locks 不可用时改用 IndexedDB 租约锁，避免后端生成成功后显示“不支持跨页面生成副作用互斥”。

- [x] 兼容锁模块已通过独立 TypeScript 严格检查，目标文件通过格式与空白检查。
- [ ] 在不提供 `navigator.locks`、但支持 IndexedDB 的浏览器中完成一次视频生成，确认结果正常显示并进入素材库。
- [ ] 同时打开两个创作页等待同一任务完成，确认只物化一次素材，两个页面最终状态一致。
- [ ] 在支持原生 Web Locks 的 Chrome 中回归图片、视频生成与画布结果持久化，确认仍优先使用原生锁。

## 画布全流程交互统一

画布已统一媒体尺寸、插入避让、节点拖拽、媒体播放、底部工具栏和外观底纹；并修正了主体无法拖动和批量素材间距过大的回归。本次仅修改 PC 前端，不改变后端与接口。

- [x] 极端横竖比媒体尺寸、连续插入避让、批量紧凑布局、点阵可见性和旧外观值兼容的 11 项专项测试通过；6 个画布测试文件共 45 项通过。
- [x] TypeScript 与生产构建通过；1440 × 900 下统一拖拽把手、六种底纹、更多面板和图片素材入口浏览器回归通过，控制台无 warning/error。
- [x] 使用既有本地视频素材验证：非控件画面可拖动节点且不会误播放；中央播放按钮可独立播放且不会拖动节点。
- [x] 三种不同尺寸素材批量插入后按真实尺寸紧凑排列，相邻间距为 24px；画布 P0 E2E 41 项全部通过。
- [x] 点阵在 125% 浏览器实测可见；工具栏设置为双列 5 行、单项高度 56px；顶栏返回首页链接为 `/`。
- [x] R15 修复视频需要点击两次播放：封面中央按钮直接完成选中、加载与播放；三种模式、键盘激活、点击暂停、普通选择保持暂停及拖动防误触经真实画布验证；新增入口回归测试，相关 27 项测试和前端构建通过。
- [x] 浅色主题下播放按钮改为固定白色图标、深色半透明底和独立层级，不再被全局 `button` 颜色继承覆盖；浅色真实画布 29 个视频节点可见性验证、32 项相关测试、TypeScript 和生产构建全部通过。
- [x] 画布、任务中心和工作流测试统一使用用户端生成阶段映射：“正在连接上游 / 上游生成中 / 后台仍在生成”显示为“生成中”，取消、恢复和模型异常文案改为正式产品表达；后端阶段值、Admin、诊断和画布节点关系术语保持不变。相关 78 项测试、TypeScript 和生产构建通过，回滚点 `6b8bfed`。
- [x] Agent 展开后可点击画布左侧空白处收纳；点击节点保持 Agent 展开，拖动画布不误收纳。真实浏览器验证通过，44 个画布相关文件共 324 项测试、TypeScript 和生产构建通过，回滚点 `256095e`。
- [x] Agent 支持画布焦点下 Esc 优先收纳且不影响输入框按键；运行中任务详情实时跟随状态、进度与日志；Agent 面板从首包拆出并空闲预取，画布项目主包 gzip 减少约 11.6%。最终 44 个文件 326 项测试、1,185 个断言及生产构建通过，回滚点 `69331cb`、`66ec2da`、`7192e07`。
- [ ] 用户验收中央按钮单击播放；补充 Safari/Firefox 以及慢网加载期间取消选中、重新选中的验证，确认不会迟到自动播放。修改与回滚节点见 `docs/design/canvas-interaction-system-20260901.md` 的 R15 记录。
- [ ] 在真实大画布副本连续插入图片、视频、音频、文本、章节和文件夹，确认自动避让位置符合实际创作习惯。
- [ ] 在 30 个以上节点的画布执行自动整理，确认整理后自动适应视口，并回归撤销、重做、保存与刷新恢复。
- [ ] 使用超宽视频、竖屏视频、长图和小尺寸图片确认预览比例、最大边界、自由缩放和锁定节点行为。

## 画布入口增量模块化

画布按低风险职责逐步缩小项目入口文件；每一阶段保留现有接口、状态顺序和交互行为，并独立建立 Git 回滚点。

- [x] 第一阶段将节点正文复制、图片剪贴板转换、媒体地址复制和方舟受控素材同步迁入 `use-canvas-node-sharing.ts`；入口只保留 hook 装配和菜单回调，`project.tsx` 从 2891 行降至 2779 行。新增资源地址解析直测，41 个画布测试文件共 263 项、TypeScript 和生产构建通过。代码回滚点：`c03af57`。
- [x] 第二阶段将 LibTV、TapNow 导入后的节点/连线合并、保存顺序和失败回滚迁入 `use-canvas-project-import.ts`；两种来源继续保留独立解析弹窗，只复用进入画布后的事务。`project.tsx` 从 2779 行降至 2739 行；新增成功提交与保存失败回滚直测，42 个画布测试文件共 265 项、TypeScript 和生产构建通过。代码回滚点：`a4faf82`。
- [x] 第三阶段将个人素材库、项目素材库和时间线素材的打开状态、插入作用域、音视频直连转换及不支持类型提示迁入 `use-canvas-asset-insertion.ts`；入口继续负责渲染弹窗与消费回填 ref。`project.tsx` 从 2739 行降至 2655 行；新增音视频转换与混合素材分流直测，43 个画布测试文件共 267 项、TypeScript 和生产构建通过。代码回滚点：`7c1db39`。
- [x] 第四阶段将画布、节点和连线右键选择，以及系统剪贴板与画布内部副本的粘贴优先级迁入 `use-canvas-context-interactions.ts`；浮层忽略选择器和原有菜单坐标换算保持不变。`project.tsx` 从 2655 行降至 2607 行；新增右键目标分类与剪贴板回退直测，44 个画布测试文件共 270 项、TypeScript 和生产构建通过。代码回滚点：`cfcdd66`。
- [x] 第五阶段将创作结果通过 `mode=handoff` 进入画布后的资产等待、重复尝试抑制、缺失资产重试、节点去重、持久化及路由清理迁入 `use-canvas-asset-handoff.ts`；旧结构断言同步改为验证入口装配和 hook 内的持久化顺序。`project.tsx` 从 2607 行降至 2572 行；画布及跨页交接共 46 个测试文件 290 项、TypeScript 和生产构建通过。代码回滚点：`05ed1c4`。
- [x] 第六阶段将关联短剧项目的角色素材同步、项目文件夹元数据刷新、节点归档、文件夹插入与打开、素材预览节点映射迁入 `use-canvas-linked-project-assets.ts`；归档服务改为实际拖入时再加载，避免初始加载触发无关插件注册，并保护归档返回期间已被用户修改的节点。`project.tsx` 从 2572 行降至 2491 行；新增文件夹外观兼容、归档并发保护和预览映射直测，47 个相关测试文件共 293 项、TypeScript 和生产构建通过。代码回滚点：`4abf8da`。
- [x] 第七阶段将标准/专业工作区、媒体性能模式、Agent 面板断点宽度与延迟预加载、专注 Dock 显示和画布标题编辑迁入 `use-canvas-workspace-shell.ts`；入口继续只负责把这些状态装配到顶部栏、工具栏和 Agent 容器。`project.tsx` 从 2491 行降至 2444 行；新增面板宽度断点与范围约束直测，48 个相关测试文件共 295 项、TypeScript 和生产构建通过。代码回滚点：`eda947a`。
- [x] 第八阶段将节点单击后的通用设置/独立编辑器路由、生成配置保留、画布取消选择、文本/绘图/肖像编辑入口及视频播放前的唯一选中迁入 `use-canvas-node-focus.ts`；并避免在 React 状态更新器内部触发其他状态写入。`project.tsx` 从 2444 行降至 2375 行；新增节点类型聚焦规则直测，49 个相关测试文件共 298 项、TypeScript 和生产构建通过。代码回滚点：`59b87e3`。
- [x] 第九阶段将任务取消确认、任务详情打开和失败资源重新加载收回 `use-canvas-generation.ts`，与任务订阅、日志刷新、节点绑定、结果消费和中断恢复统一管理；页面入口不再直接调用任务中心接口。`project.tsx` 从 2375 行降至 2320 行；新增任务操作归属检查，50 个相关测试文件共 299 项、TypeScript 和生产构建通过。代码回滚点：`ad964e1`。
- [x] 第十阶段将节点悬浮开始/结束、工具栏延迟收起、拖动期间抑制、图片/视频设置面板互斥及历史恢复清理迁入 `use-canvas-node-hover-toolbar.ts`；同时补上组件卸载时取消延迟任务，避免离开页面后旧状态回写。`project.tsx` 从 2320 行降至 2281 行；新增悬浮显示条件与 120ms 移入容错直测，51 个相关测试文件共 301 项、TypeScript 和生产构建通过。代码回滚点：`81ab471`。
- [x] 第十一阶段将节点下载、复制、删除、元数据合并、内容尺寸校正、版本预览、图片查看与导演台入口统一迁入 `use-canvas-node-action-bindings.ts`，并集中生成批次子图和主节点共用的动作 Context；按钮参数与调用顺序保持不变。`project.tsx` 从 2281 行降至 2277 行；新增元数据合并和目标尺寸更新直测，52 个相关测试文件共 303 项、TypeScript 和生产构建通过。代码回滚点：`01b0ad2`。
- [x] 第十二阶段将参考素材解除连接与旧版 `@[node:…]` 提示词迁移统一迁入 `use-canvas-node-references.ts`；解除参考只删除素材到目标/配置的输入边，保留目标到配置的主链，也不影响同一素材连接的其他节点。`project.tsx` 从 2277 行降至 2248 行；新增参考边删除边界直测，53 个相关测试文件共 305 项、TypeScript 和生产构建通过。代码回滚点：`f9c21c9`。
- [x] 第十三阶段将脚本重试、图片批次失败项筛选/预备状态/并发重试/根节点回收、单个批次子图重试及普通节点生成重试统一迁入 `use-canvas-node-retry.ts`；生成请求、模型参数、任务绑定和计费逻辑保持在原模块不变。`project.tsx` 从 2248 行降至 2195 行；新增脚本、批次根/子图和通用重试分流直测，54 个相关测试文件共 308 项、TypeScript 和生产构建通过。代码回滚点：`29047d5`。
- [x] 第十四阶段将文本节点创建图片生成节点的内容读取、右侧落位、默认生图参数、连线、选中与设置面板打开迁入 `use-canvas-text-to-image.ts`；该步骤只建立前端画布流程，不直接提交生成任务。`project.tsx` 从 2195 行降至 2156 行；新增空文本拦截、位置、模型/规格/数量和连线方向直测，55 个相关测试文件共 310 项、TypeScript 和生产构建通过。代码回滚点：`65c525a`。
- [x] 第十五阶段将肖像排查运行态更新、旧节点默认状态与输入绑定同步、上游节点排序以及候选图片上传/落位/连线迁入 `use-canvas-portrait-clearance.ts`；底层分析任务仍由原有运行协调器执行。`project.tsx` 从 2156 行降至 2111 行；新增输入排序与旧节点绑定迁移直测，56 个相关测试文件共 312 项、TypeScript 和生产构建通过。代码回滚点：`88f3f28`。
- [x] 第十六阶段将脚本/绘图独立编辑器、生成配置组合器和普通节点提示词设置的面板分流迁入 `use-canvas-node-panel-renderer.tsx`，纯路由规则放在 `canvas-node-panel-routing.ts`；页面只装配当前节点和操作回调。`project.tsx` 从 2111 行降至 2092 行；新增节点类型面板分流直测，57 个相关测试文件共 314 项、TypeScript 和生产构建通过。代码回滚点：`5008ef7`。
- [x] 第十七阶段将角色卡、画风占位、故事输入、分镜脚本、导演镜头和普通配置节点的内容渲染迁入 `use-canvas-node-content-renderer.tsx`，优先级规则放在 `canvas-node-content-routing.ts`；分镜批次、行操作、连线句柄和生成参数保持原调用顺序。`project.tsx` 从 2092 行降至 2003 行；新增六类节点内容分流直测，58 个相关测试文件共 316 项、TypeScript 和生产构建通过。代码回滚点：`7314656`。
- [x] 第十八阶段将角色参考、富文本、绘图和肖像排查编辑弹窗统一迁入 `canvas-project-node-editor-dialogs.tsx`，文本与绘图保存后的节点归一化放在 `canvas-node-editor-updates.ts`；节点信息弹窗仍保留原位置，避免改变多弹窗同时出现时的层级顺序。`project.tsx` 从 2003 行降至 1959 行；新增文本与绘图保存直测，59 个相关测试文件共 318 项、TypeScript 和生产构建通过。代码回滚点：`2b07daf`。
- [x] 第十九阶段将字幕、视频抽帧、音视频分段和多轨时间线弹窗迁入 `canvas-project-timeline-dialogs.tsx`，字幕保存后的时间线同步边界放在 `canvas-timeline-dialog-updates.ts`；项目时间线不存在时仍不隐式创建，现有素材库、上传和合成回填通道保持原引用。`project.tsx` 从 1959 行降至 1921 行；新增无时间线与字幕轨偏移同步直测，60 个相关测试文件共 320 项、TypeScript 和生产构建通过。代码回滚点：`d998338`。
- [x] 第二十阶段将分镜脚本独立编辑器装配迁入 `canvas-project-script-editor.tsx`，可见列更新和关键帧视频分流规则放在 `canvas-script-editor-updates.ts`；分镜行、引用节点、图片生成、两种视频生成路径及输入模式更新继续调用原函数。`project.tsx` 从 1921 行降至 1907 行；新增列配置保留与视频模式分流直测，61 个相关测试文件共 322 项、TypeScript 和生产构建通过。代码回滚点：`6e01960`。
- [x] 第二十一阶段将 3D 导演台懒加载、加载占位和页面装配迁入 `canvas-project-director-workbench.tsx`，可用图片素材筛选放在 `canvas-director-workbench-inputs.ts`；场景保存、应用结果、删除图片节点和持久化刷新仍走原回调。`project.tsx` 从 1907 行降至 1897 行；新增导演台图片输入边界直测，62 个相关测试文件共 323 项、TypeScript 和生产构建通过。代码回滚点：`751595f`。
- [x] 第二十二阶段将版本对比、个人素材和项目素材弹窗装配迁入 `canvas-project-library-dialogs.tsx`，定位关闭顺序与项目文件夹插入作用域放在 `canvas-project-library-routing.ts`；版本定位仍先关闭弹窗再聚焦，只有画布作用域允许整体插入项目文件夹。`project.tsx` 从 1897 行降至 1896 行，入口不再直接依赖三种素材弹窗组件；新增路由顺序和作用域直测，63 个相关测试文件共 325 项、TypeScript 和生产构建通过。代码回滚点：`634c169`。
- [x] 第二十三阶段将节点搜索弹窗及定位装配迁入 `canvas-project-node-search.tsx`，父级画框与图片批次展开规则放在 `canvas-node-search-routing.ts`；定位仍依次展开容器、建立唯一节点选择、清除连线选择并聚焦目标。`project.tsx` 从 1896 行降至 1891 行；新增折叠容器与已展开边界直测，64 个相关测试文件共 327 项、TypeScript 和生产构建通过。代码回滚点：`3edd098`。
- [x] 第二十四阶段将分享、LibTV/TapNow 导入、项目画风和导演模板弹窗迁入 `canvas-project-entry-dialogs.tsx`，导演模板落点解析放在 `canvas-project-entry-dialog-routing.ts`；分享前保存、两类导入事务、画风应用和导演镜头创建仍调用原函数。由于页面改为显式传递五组有类型边界的入口参数，`project.tsx` 从 1891 行调整为 1902 行，但不再直接依赖五个弹窗组件；新增导演模板落点直测，65 个相关测试文件共 329 项、TypeScript 和生产构建通过。代码回滚点：`26d456e`。
- [x] 第二十五阶段在 `canvas-project-feedback.tsx` 建立统一项目状态提示层，集中装配上传进度、视频合并进度和 Agent 写回提示；各提示组件、动画、查看/撤销/关闭动作保持不变，入口不再分别判断三种状态。`project.tsx` 从 1902 行降至 1898 行；新增提示层职责归属检查，65 个相关测试文件共 330 项、TypeScript 和生产构建通过。代码回滚点：`bcdec8f`。
- [x] 第二十六阶段将角度生成、情绪编辑、普通节点设置和连线后创建菜单迁入 `canvas-project-node-overlays.tsx`，拖动跟随与独立编辑器/框选互斥规则放在 `canvas-node-overlay-state.ts`；浮层位置、拖动状态、生成回调和连线创建能力仍使用原数据。`project.tsx` 从 1898 行降至 1858 行；新增拖动跟随和浮层互斥直测，66 个相关测试文件共 332 项、TypeScript 和生产构建通过。代码回滚点：`2453f1f`。
- [x] 第二十七阶段在 `canvas-project-selection-toolbar.tsx` 收口多选工具栏的显示判断、节点 ID 展开和合并动作装配，显示规则放在 `canvas-selection-toolbar-visibility.ts`；只有存在有效选择边界且不在框选/拖动时显示。显式传递选择能力后 `project.tsx` 从 1858 行调整为 1859 行，但入口不再承担多选动作参数转换；新增显示边界直测，67 个相关测试文件共 334 项、TypeScript 和生产构建通过。代码回滚点：`b08acb7`。
- [x] 第二十八阶段将单节点悬浮工具栏装配迁入 `canvas-project-node-toolbar.tsx`，显示拦截、角色信息入口和字号上下限放在 `canvas-node-toolbar-routing.ts`；角度/情绪与普通设置互斥、节点 ID 转换、媒体操作和删除动作保持原顺序。`project.tsx` 从 1859 行降至 1858 行，入口只传状态设置器和原动作集合；新增显示、角色入口和字号边界直测，68 个相关测试文件共 337 项、TypeScript 和生产构建通过。代码回滚点：`8836f3b`。
- [x] 第二十九阶段将小地图、缩放控制和素材托盘迁入 `canvas-project-bottom-dock.tsx`，唯一选择与专注模式显示规则放在 `canvas-bottom-dock-state.ts`；自动整理仍先关闭菜单、整理节点并在下一帧适配视口，切换小地图与快捷键也保持原顺序。`project.tsx` 从 1858 行降至 1843 行；新增唯一素材选择和专注模式边界直测，69 个相关测试文件共 339 项、TypeScript 和生产构建通过。代码回滚点：`0f59714`。
- [x] 第三十阶段将运行中任务、专注模式控制、文件拖入提示和空画布状态迁入 `canvas-project-workspace-overlays.tsx`，任务面板避让规则放在 `canvas-workspace-overlay-state.ts`；Agent 开关、专注 Dock、缩放适配和空态内容保持原回调。`project.tsx` 从 1843 行降至 1835 行；新增普通/专注模式与 Agent 宽度避让直测，70 个相关测试文件共 341 项、TypeScript 和生产构建通过。代码回滚点：`ba84292`。
- [x] 第三十一阶段将主工具栏的显隐、Agent 面板避让、内置/扩展节点创建与选中项删除装配迁入 `canvas-project-main-toolbar.tsx`，显示和右侧边距规则放在 `canvas-main-toolbar-state.ts`；工具栏顺序、按钮回调和清空确认保持不变。`project.tsx` 从 1835 行降至 1826 行；新增专注 Dock 显隐与 Agent 宽度避让直测，71 个相关测试文件共 343 项、TypeScript 和生产构建通过。代码回滚点：`88aed54`。
- [x] 第三十二阶段将标准/专业工作区开关和短剧引导条迁入 `canvas-project-workspace-chrome.tsx`，专注模式显隐与 Agent 面板避让规则放在 `canvas-workspace-chrome-state.ts`；两个控件仍在节点搜索的原有前后位置渲染，避免改变弹层层级。`project.tsx` 从 1826 行降至 1825 行，入口不再直接依赖两个展示组件；新增专注显隐和 Agent 宽度避让直测，72 个相关测试文件共 345 项、TypeScript 和生产构建通过。代码回滚点：`44b512e`。
- [x] 第三十三阶段将 Agent 面板的懒加载、加载占位、宽度列和顶部避让迁入 `canvas-project-assistant-column.tsx`，普通/专注模式的顶边距规则放在 `canvas-assistant-column-state.ts`；预加载入口仍由工作区偏好 hook 触发，收起动画与 Agent 全部业务参数保持不变。`project.tsx` 从 1825 行降至 1820 行；新增顶栏避让直测并更新懒加载职责检查，73 个相关测试文件共 346 项、TypeScript 和生产构建通过。代码回滚点：`ff32be0`。
- [x] 第三十四阶段将自由画布、关联项目和短剧三种空画布起点迁入 `canvas-project-empty-state.tsx`，空态分流与项目首章选择放在 `canvas-empty-state-routing.ts`；关联项目仍按 position 选择首章，短剧 Agent 仍保持在线模式与影视入口标记。`project.tsx` 从 1820 行降至 1813 行；新增三路空态和首章无副作用排序直测，74 个相关测试文件共 348 项、TypeScript 和生产构建通过。代码回滚点：`0f5f49d`。
- [x] 第三十五阶段将右键菜单的世界/屏幕坐标换算、单节点集合和节点/连线删除目标收口到 `canvas-context-menu-routing.ts`，`canvas-project-context-menu.tsx` 直接消费原始节点操作；图片预览、保存素材、内容复制和画框开关也不再由入口做节点转换。`project.tsx` 从 1813 行降至 1807 行；新增坐标与复制/删除目标直测，75 个相关测试文件共 350 项、TypeScript 和生产构建通过。代码回滚点：`6a6bc36`。
- [x] 第三十六阶段在 `canvas-project-viewport.tsx` 建立主视口层级，统一装配无限画布、Leafer 图形层、节点动作 Context、节点图 Context 和世界节点层；视口事件、连线/节点参数及 Context 嵌套顺序保持不变。`project.tsx` 从 1807 行降至 1801 行，入口不再直接持有四个底层渲染组件；新增层级所有权检查，76 个相关测试文件共 351 项、TypeScript 和生产构建通过。代码回滚点：`ed54c61`。
- [x] 第三十七阶段在 `canvas-project-chrome.tsx` 统一装配关联项目侧栏与画布顶栏，专注模式显隐和项目上下文注入规则放在 `canvas-project-chrome-state.ts`；只有已关联的短剧项目显示侧栏和项目导航信息。由于入口改为显式传递带类型的顶栏参数，`project.tsx` 从 1801 行调整为 1802 行，但不再直接依赖侧栏和顶栏展示组件；新增外壳显隐与项目上下文直测，77 个相关测试文件共 353 项、TypeScript 和生产构建通过。代码回滚点：`5f3931c`。
- [x] 第三十八阶段将上传弹窗、隐藏文件入口、节点信息弹窗和无界面本地 Agent 迁入 `canvas-project-utility-overlays.tsx`，导入 MIME/扩展名与无界面 Agent 挂载规则放在 `canvas-utility-overlay-state.ts`；图片、视频、MP3/WAV 支持和紧凑 Agent 条件保持不变。由于显式传递三组工具参数，`project.tsx` 从 1802 行调整为 1803 行，但不再直接依赖三个底层工具组件；新增导入范围和无界面 Agent 边界直测，78 个相关测试文件共 355 项、TypeScript 和生产构建通过。代码回滚点：`89f23f2`。
- [x] 第三十九阶段在真实浏览器冒烟中发现 Vidstack 的 `.vds-controls` 覆盖整块视频画面，导致播放后再次点击画面被误判为控件点击；`canvas-node-content.tsx` 现只排除菜单、按钮和滑杆等真实控件，空白视频表面恢复点击暂停，原有拖移阈值保持不变。新增播放选择器与暂停路径回归断言，78 个相关测试文件共 356 项、TypeScript、生产构建及本地浏览器“单击播放、再次点击暂停”验证通过。代码回滚点：`27235a7`。
- [x] 第四十阶段将节点删除后的界面状态清理迁入 `use-canvas-deleted-node-cleanup.ts`，统一关闭指向已删除节点的工具栏、编辑器、媒体面板、任务详情和右键菜单，并清理对应脚本滚动记录；画布菜单、未删除节点的面板和可撤销绘图文件继续保留。`project.tsx` 从 1803 行降至 1801 行，新增节点 ID、滚动记录和右键菜单边界直测，79 个相关测试文件共 359 项、TypeScript 和生产构建通过。代码回滚点：`390ee71`。
- [x] 第四十一阶段将撤销/重做应用历史快照后的临时界面清理迁入 `use-canvas-history-ui-cleanup.ts`，统一收起悬浮工具栏以及文本、绘图、媒体、时间线、导演台和版本对比等节点面板；恢复回调仍通过原 `historyRestoreUiRef` 接入历史模块。`project.tsx` 从 1801 行降至 1800 行，新增全部清理目标和入口职责直测，80 个相关测试文件共 361 项、TypeScript 和生产构建通过。代码回滚点：`d84eb3c`。
- [x] 第四十二阶段将容器尺寸监听、项目切换后的首次居中标记和默认视口判断迁入 `use-canvas-viewport-measurement.ts`；只有未经移动和缩放的 `{x:0,y:0,k:1}` 视口会按实际容器居中，已保存视口不会被覆盖。`project.tsx` 从 1800 行降至 1771 行，新增默认/已保存视口直测，81 个相关测试文件共 363 项、TypeScript、生产构建及真实画布刷新验证通过，刷新后原 21% 缩放保持不变。代码回滚点：`d5d5158`。
- [x] 第四十三阶段在 `use-canvas-workspace-shell.ts` 收口本机 Agent 自动连接和专注模式进入瞬间的面板收纳；项目未加载或无自动连接请求时不启动 Agent，紧凑本机入口只切换本地模式，普通入口打开本地 Agent；进入专注模式仍只在关闭到开启的瞬间收起 Agent、小地图和 Dock。`project.tsx` 从 1771 行降至 1763 行，新增自动连接与专注边界直测，81 个相关测试文件共 365 项、TypeScript、生产构建及真实“打开 Agent→进入专注→自动收起”验证通过。代码回滚点：`b75764b`。
- [x] 第四十四阶段将关联项目画风的解析、序列化、幂等判断及画风节点创建/更新迁入 `use-canvas-linked-project-style.ts`；无有效画风时不创建节点，已有节点仅在内容、配置或锁定状态变化时更新，节点继续固定为 420×240 并放在当前画布中心。`project.tsx` 从 1763 行降至 1731 行且不再直接依赖画风序列化细节，新增缺省、官方画风和幂等边界直测，82 个相关测试文件共 368 项、TypeScript 和生产构建通过。代码回滚点：`1f4a059`。
- [x] 第四十五阶段将节点、连线、会话、当前会话、选择和视口的实时引用同步，以及生成任务实时项目适配器注册迁入 `use-canvas-live-project.ts`；引用仍在同一次布局提交中更新，节点时间戳写入和项目作用域保持原路径。`project.tsx` 从 1731 行降至 1720 行且不再直接依赖实时生成项目注册服务，新增六类引用同步和入口职责直测，83 个相关测试文件共 370 项、TypeScript、生产构建及真实画布加载验证通过。代码回滚点：`7f9c89a`。
- [x] 第四十六阶段将 Agent 面板消费的实时快照视图迁入 `use-canvas-agent-operations.ts`，会话与当前会话的同步回调并入 `use-canvas-live-project.ts`；节点、连线和选区变化才更换快照对象，高频拖动/缩放通过 getter 读取最新视口、版本和状态哈希，避免整棵 Agent 组件树跟随重渲染。`project.tsx` 从 1720 行降至 1691 行，新增稳定数据与实时 getter 直测，84 个相关测试文件共 371 项、TypeScript、生产构建及真实 Agent 打开验证通过。代码回滚点：`9c5b272`。
- [x] 第四十七阶段将清空画布时的节点、连线、临时面板、选择、确认框和本地文件清理顺序迁入 `use-canvas-clear.ts`；清空仍可撤销，因此绘图文件继续保留到项目永久删除，未扩大原有清理范围。由于入口显式传递十一类面板重置器，`project.tsx` 从 1691 行调整为 1693 行，但不再维护清空流程细节；新增完整调用顺序直测，85 个相关测试文件共 372 项、TypeScript 和生产构建通过。代码回滚点：`6f1ecbd`。
- [x] 第四十八阶段将项目画风节点查找、缺失提示和视口定位并回 `use-canvas-linked-project-style.ts`，同步与定位共用同一文本画风节点判定；项目入口移除 Ant Design 通知依赖和定位回调。`project.tsx` 从 1693 行降至 1683 行，新增错误类型、普通文本与有效画风节点定位边界直测，85 个相关测试文件共 373 项、TypeScript 和生产构建通过。代码回滚点：`a4d1de6`。
- [x] 第四十九阶段将文本编辑、字幕、时间线、抽帧/分段、角色参考和绘图等活动节点解析，以及连线后快速创建绘图的资格判断迁入 `canvas-active-node-targets.ts`；缺失 ID 稳定返回空目标，只有单张有内容图片的 source 连接允许创建绘图。由于入口改为显式传递九类目标 ID，`project.tsx` 从 1683 行调整为 1686 行，但不再维护派生规则；新增缺失节点、目标/源、批量和空内容边界直测，86 个相关测试文件共 375 项、TypeScript 和生产构建通过。代码回滚点：`97cb5c0`。
- [x] 第五十阶段将自由画布、关联项目和短剧空状态的上传、创建文本/脚本及影视 Agent 启动动作装配并回 `canvas-project-empty-state.tsx`；短剧入口继续按“标记影视入口→切换在线模式→打开 Agent”的顺序执行。`project.tsx` 从 1686 行降至 1683 行，新增影视 Agent 启动顺序直测，86 个相关测试文件共 376 项、TypeScript 和生产构建通过。代码回滚点：`1672379`。
- [x] 第五十一阶段将画布生成执行器提前到 Agent 操作装配之前，`use-canvas-agent-operations.ts` 直接消费最新执行器，移除页面级 `generateNodeRef` 和同步 effect；Agent 仍只在实际生成操作发生时调用执行器。`project.tsx` 从 1683 行降至 1678 行，新增无 ref 桥接职责断言，86 个相关测试文件共 377 项、TypeScript、生产构建及真实 Agent 打开验证通过，未触发付费生成。代码回滚点：`2afb180`。
- [x] 第五十二阶段将多选节点转批量连接源的筛选规则迁入 `canvas-batch-connection.ts`，统一复用背板、脚本和生成配置不可作为聚合连接源的限制，并保持画布节点顺序。入口只保留 memo 装配，新增选区过滤与顺序直测，86 个相关测试文件共 378 项、TypeScript 和生产构建通过。代码回滚点：`499639e`。
- [x] 第五十三阶段将真正画布空白点击时的“取消选择→收起 Agent”行为迁入 `use-canvas-node-focus.ts`，与节点点击、画布取消选择和独立编辑器聚焦规则统一管理；节点或连线点击仍不会触发该路径。`project.tsx` 从 1678 行降至 1674 行，新增执行顺序直测并更新背景点击职责检查，86 个相关测试文件共 379 项、TypeScript 和生产构建通过。代码回滚点：`1ccaba6`。
- [x] 第五十四阶段将历史快照上下文与素材文件清理参数的组合迁入 `use-canvas-history.ts`；节点删除、清空画布和项目生命周期继续消费同一 `cleanupCanvasFiles`，撤销/重做需要的 past/future 与 lastHistory 不会因清理当前节点而丢失。`project.tsx` 从 1674 行降至 1668 行，新增清理目标与历史上下文组合直测，86 个相关测试文件共 380 项、TypeScript 和生产构建通过。代码回滚点：`1956a02`。
- [x] 第五十五阶段将“电影创作 Agent”的一次性入口标记、在线模式打开与消费后复位统一迁入 `use-canvas-assistant-visibility.ts`；短剧空状态只调用一个 `openCinematicAgent`，不再分别协调三个状态。`project.tsx` 从 1668 行降至 1663 行，新增入口标记与在线模式顺序直测，86 个相关测试文件共 380 项、TypeScript 和生产构建通过。代码回滚点：`b86a3d1`。
- [x] 第五十六阶段将节点媒体替换对节点 ID 的转换与回调稳定性收入 `use-canvas-upload.ts`，轻量适配放在 `canvas-upload-target.ts`；页面不再维护额外替换回调，上传、工具栏上传与节点替换继续共用同一目标通道。验证时还隔离了纯函数测试对插件节点注册表的加载副作用。`project.tsx` 从 1663 行降至 1662 行，86 个相关测试文件共 381 项、TypeScript 和生产构建通过。代码回滚点：`62e6fa2`。
- [x] 第五十七阶段将节点 React 状态、最新快照 ref 与时间戳补齐统一迁入 `use-canvas-node-state.ts`；直接替换继续以 `nodesRef.current` 为基准，函数式更新继续以 React 提供的最新状态为基准，两条路径都保留有意义编辑的 `updatedAt` 规则。`project.tsx` 从 1662 行降至 1647 行，新增直接/函数式连续更新直测，86 个相关测试文件共 382 项、TypeScript 和生产构建通过。代码回滚点：`ced3bb3`。
- [x] 第五十八阶段将扩展节点上游查询 Context 的创建迁入 `use-canvas-render-model.ts`，并由 `canvas-node-graph-context.ts` 统一构造查询值；页面不再获取渲染模型的内部图索引。纯拖动位置时继续复用语义节点与 Context，只有节点语义或连线变化才更新上游查询，避免扩展节点绕过 memo 重渲染。`project.tsx` 从 1647 行降至 1642 行，新增 Context 上游查询直测，86 个相关测试文件共 382 项、TypeScript 和生产构建通过。代码回滚点：`6be532e`。
- [x] 第五十九阶段将顶部栏所需的节点数、选中数、章节和镜头摘要迁入 `use-canvas-render-model.ts` 统一派生；页面不再直接依赖 `summarizeCanvasContext`，关联项目章节仍可通过 chapterId 解析标题。`project.tsx` 保持 1642 行，新增实时选区、关联章节与镜头编号直测，86 个相关测试文件共 383 项、TypeScript 和生产构建通过。代码回滚点：`a8bd190`。
- [x] 第六十阶段将清空确认、分享、LibTV/TapNow 导入、节点搜索、画风选择、导演模板与快捷键请求等项目级临时状态迁入 `use-canvas-project-dialog-state.ts`；原有 setter 语义与弹窗打开/关闭顺序保持不变。`project.tsx` 从 1642 行降至 1635 行，新增入口弹窗状态归属检查，86 个相关测试文件共 384 项、TypeScript 和生产构建通过。代码回滚点：`d8d56e2`。
- [x] 第六十一阶段将通用设置、文本/角色参考/绘图编辑、节点信息、字幕、时间线、超分、预览、脚本、肖像排查、导演台与版本对比等节点级面板目标迁入 `use-canvas-node-panel-state.ts`；节点删除、清空画布和历史恢复继续通过原 setter 统一关闭相关面板。`project.tsx` 从 1635 行降至 1622 行，新增节点面板目标状态归属检查，86 个相关测试文件共 385 项、TypeScript 和生产构建通过。代码回滚点：`6a2a358`。
- [x] 第六十二阶段将节点悬浮目标、右键菜单、小地图开关、画布背景、图片信息显示与当前工具迁入 `use-canvas-workspace-ui-state.ts`；持久化、历史恢复、Agent 收纳和工具栏切换继续使用原 setter 与默认值。`project.tsx` 从 1622 行降至 1616 行，新增工作区界面状态归属检查，86 个相关测试文件共 386 项、TypeScript 和生产构建通过。代码回滚点：`1543c29`。
- [x] 第六十三阶段将连线、Agent 会话与当前会话三类项目内容状态迁入 `use-canvas-project-content-state.ts`；状态初值、setter 语义、实时引用同步、历史恢复和项目保存通道保持不变，项目加载标记仍留在生命周期层。`project.tsx` 从 1616 行降至 1613 行，新增项目内容状态归属检查，86 个相关测试文件共 387 项、1,156 个断言、TypeScript 和生产构建通过。本阶段仅变更 PC 前端状态所有权，未读写真实画布数据，未触发生成；风险集中在刷新后连线与 Agent 会话的恢复顺序，需真实画布人工回归。代码回滚点：`e87ee04`。
- [x] 第六十四阶段将节点选中集合与连线选中 ID 迁入 `use-canvas-selection-state.ts`；空选区初值、单连线选择、setter 语义及实时引用、框选、拖动、历史和快捷键控制通道保持不变。`project.tsx` 保持 1613 行，新增选区状态归属检查，86 个相关测试文件共 388 项、1,161 个断言、TypeScript 和生产构建通过。本阶段仅变更 PC 前端状态所有权，未读写真实画布数据，未触发生成；风险集中在节点与连线选择互斥和历史恢复后的选区清理，需真实画布人工回归。代码回滚点：`b3bc41e`。
- [x] 第六十五阶段将视口变换与 setter 迁入 `use-canvas-viewport-state.ts`，并集中保留 `{x:0,y:0,k:1}` 默认值；尺寸测量、首次居中、实时预览、平移缩放、选区适配和项目持久化逻辑保持在原模块。`project.tsx` 保持 1613 行，新增视口状态归属检查，86 个相关测试文件共 389 项、1,165 个断言、TypeScript 和生产构建通过。本阶段仅变更 PC 前端状态所有权，未读写真实画布数据，未触发生成；风险集中在旧项目已保存视口的刷新恢复与容器变尺寸时的居中边界，需真实画布人工回归。代码回滚点：`cc91aed`。
- [x] 第六十六阶段在剩余页面职责审查后，将短剧功能门控、关联项目 ID 解析、`["project", linkedProjectId]` 缓存键、详情请求与刷新句柄收口到 `use-canvas-linked-project-query.ts`；原 `getProject` 接口、非空 ID 启用条件和下游项目素材/画风/章节消费保持不变。`project.tsx` 从 1613 行降至 1610 行，新增 2 项功能开关与空关联边界直测，87 个相关测试文件共 391 项、1,169 个断言、TypeScript 和生产构建通过。本阶段仅变更 PC 前端查询装配，未修改接口或业务规则，未触发生成；风险集中在功能开关及关联项目切换时的缓存更新，需真实项目人工回归。代码回滚点：`b9e9a2a`。
- [x] 第六十七阶段验收前四阶段提交后，将多选节点转批量连接来源的 memo 派生迁入 `use-canvas-selection-state.ts`；筛选仍复用 `selectBatchConnectionSourceNodeIds`，保留画布顺序并排除背板、脚本与生成配置，页面不再直接依赖批量连接实现。`project.tsx` 从 1610 行降至 1608 行，独立复验前四阶段后，87 个相关测试文件共 391 项、1,171 个断言、TypeScript 和生产构建通过。本阶段仅收口 PC 前端选区派生，未触发生成。代码回滚点：`325e527`。
- [x] 第六十八阶段将本地 Agent 的启动模式解析、自动连接与旧深链拒绝后的紧凑状态组合迁入 `canvas-local-agent-entry.ts`；页面只消费 `autoConnect` 与 `compactAgent`，不再直接读取 Runtime 引导状态或判断 URL 模式。`project.tsx` 从 1608 行降至 1606 行，新增明确启动模式、普通入口与旧深链组合直测，87 个相关测试文件共 392 项、1,174 个断言、TypeScript 和生产构建通过。本阶段仅收口 PC 前端 Agent 入口派生，未触发本地连接或生成。代码回滚点：`73a8046`。
- [x] 第六十九阶段将首次客户端挂载判断与刷新占位迁入 `canvas-client-mount-gate.tsx`；画布入口只装配挂载门和实际页面，项目数据加载中的 `CanvasRefreshShell` 仍由页面生命周期独立控制。`project.tsx` 从 1606 行降至 1603 行，新增挂载门唯一归属检查，87 个相关测试文件共 393 项、1,178 个断言、TypeScript 和生产构建通过。本阶段仅拆分 PC 前端入口职责，未改变刷新或项目加载顺序，未触发生成；风险集中在浏览器首次水合与慢设备占位切换，需真实页面人工回归。代码回滚点：`97cff11`。
- [x] 第七十阶段将短剧引导的显示条件、折叠状态与切换回调统一收回 `use-canvas-short-drama.ts`；页面只传入功能门控与是否关联项目，并消费完整 `shortDramaGuide`，创建流水线、完成后折叠和永久跳过语义保持不变。`project.tsx` 从 1603 行降至 1600 行，新增短剧引导状态归属检查，87 个相关测试文件共 394 项、1,183 个断言、TypeScript 和生产构建通过。本阶段仅调整 PC 前端状态派生，未改变短剧业务规则或接口，未触发生成；风险集中在关联项目切换后引导显隐和完成流程后的折叠状态，需真实画布人工回归。代码回滚点：`b2799b9`。
- [x] 第七十一阶段将本地 Agent 状态、AI 配置、素材库水合与清理、主题、绘图引擎、短剧开关和导演引导用户范围等外部 Store 订阅统一迁入 `use-canvas-project-environment.ts`；所有选择器、有效配置合并及主题映射保持原实现，页面只消费环境快照。`project.tsx` 从 1600 行降至 1583 行，新增环境订阅归属检查，87 个相关测试文件共 395 项、1,189 个断言、TypeScript 和生产构建通过。本阶段未修改 Store、权限、接口或业务规则，未触发生成；风险集中在主题/配置热更新和素材库水合后页面重渲染，需真实画布人工回归。代码回滚点：`0155a46`。
- [x] 第七十二阶段将画布路由 ID、查询参数、查询参数 setter 与本地 Agent 启动模式解析统一迁入 `use-canvas-project-route.ts`；素材交接继续消费同一组 `URLSearchParams`，普通/自动连接/旧深链行为仍复用原纯函数。`project.tsx` 从 1583 行降至 1579 行，新增路由入口归属检查，87 个相关测试文件共 396 项、1,195 个断言、TypeScript 和生产构建通过。本阶段未修改路由定义、URL 协议或接口，未触发 Agent 连接与生成；风险集中在带素材交接参数和旧深链参数的首次进入，需真实页面人工回归。代码回滚点：`8fb1f55`。
- [x] 第七十三阶段将裁剪、标注、蒙版、图片分割和超分五套重型图片工具改为各自按需加载，未打开工具时不再进入画布主包；首次加载显示统一图片工具等待层，原确认回调与数据结构保持不变。画布主入口构建产物由约 1201.76 kB 降至 1177.95 kB（gzip 约 361.97 kB 降至 355.19 kB），五个工具形成独立 3.79–7.15 kB 分包；87 个相关测试文件共 397 项、1,201 个断言、TypeScript 和生产构建通过。本阶段未修改图片处理逻辑或接口，未触发真实处理；风险集中在首次打开工具时的分包加载与加载失败反馈，需真实页面人工回归。代码回滚点：`651df8a`。
- [x] 第七十四阶段将角色参考、文本编辑和肖像排查改为选中对应编辑目标后再按需加载，并与原绘图编辑器共用统一全屏等待状态；保存、关闭、绘图摘要和人物候选回写回调保持不变。画布主入口构建产物由约 1177.95 kB 降至 1128.85 kB（gzip 约 355.19 kB 降至 342.32 kB），三套编辑器形成 5.63 kB、13.48 kB、30.23 kB 独立分包；87 个相关测试文件共 398 项、1,208 个断言、TypeScript 和生产构建通过。本阶段未修改节点数据、肖像服务或业务规则，未触发真实分析；风险集中在首次打开编辑器的分包加载和关闭后再次打开的状态重建，需真实页面人工回归。代码回滚点：`a40f5b4`。
- [x] 第七十五阶段将字幕、视频抽帧、视频分段和视频时间线四套面板改为选中对应目标后再按需加载，并为各工具提供明确的全屏等待文案；字幕同步、片段确认、时间线保存、素材导入和成片节点创建回调保持不变。画布主入口构建产物由约 1128.85 kB 降至 1044.48 kB（gzip 约 342.32 kB 降至 318.03 kB），四套工具形成 6.20–34.95 kB 独立分包；87 个相关测试文件共 399 项、1,213 个断言、TypeScript 和生产构建通过。本阶段未修改视频处理、字幕或时间线数据结构与接口，未触发真实处理；风险集中在首次打开工具的分包加载和时间线切换到字幕面板的连续交互，需真实页面人工回归。代码回滚点：`4e326d7`。
- [x] 第七十六阶段将个人素材库、项目素材和版本对比弹窗改为仅在打开状态下按需加载，并保留项目文件夹插入路由与版本定位的先关闭后聚焦顺序。画布主入口构建产物由约 1044.48 kB 降至 1035.66 kB（gzip 约 318.03 kB 降至 315.30 kB），三个弹窗形成 2.90–5.44 kB 独立分包；87 个相关测试文件共 400 项、1,219 个断言、TypeScript 和生产构建通过。本阶段未修改素材来源、项目资产接口或版本业务规则，未插入真实素材；风险集中在首次打开素材弹窗、外部素材源初始化和版本定位关闭顺序，需真实页面人工回归。代码回滚点：`b888025`。
- [x] 第七十七阶段将批量上传弹窗与旧兼容入口的无界面本地 Agent 改为仅在实际启用时按需加载；隐藏文件输入继续常驻以保留直接导入入口，节点信息因与节点工具栏共用模块暂不强拆。画布主入口构建产物由约 1035.66 kB 降至 1031.88 kB（gzip 约 315.30 kB 降至 314.32 kB），上传弹窗与本地 Agent 形成约 6.00 kB、74.50 kB 独立分包；87 个相关测试文件共 401 项、1,224 个断言、TypeScript 和生产构建通过。本阶段未修改上传、Agent 连接或兼容判断逻辑，未上传素材或发起连接；风险集中在首次打开上传弹窗与旧紧凑入口自动连接时的分包加载，需真实页面人工回归。代码回滚点：`aeadcb0`。
- [x] 第七十八阶段将分享、LibTV 导入、TapNow 导入、画风选择与导演模板五类入口弹窗改为仅在打开状态下按需加载；分享前保存、导入视口坐标、画风应用状态和导演模板发起落点仍沿用原回调。画布主入口构建产物由约 1031.88 kB 降至 1017.19 kB（gzip 约 314.32 kB 降至 310.00 kB），其中 114.57 kB 的画风选择器仅在打开时加载；87 个相关测试文件共 402 项、1,231 个断言、TypeScript 和生产构建通过。本阶段未修改分享、导入、画风或导演业务逻辑与接口，未执行真实导入或应用；风险集中在首次加载分包、导入坐标保持和导演模板选择后的落点，需真实页面人工回归。代码回滚点：`c8aa794`。
- [x] 第七十九阶段将任务详情弹窗、生成参数解析、模型显示、任务日志与取消入口迁入独立 `canvas-project-task-detail-dialog.tsx`，仅在选中任务时按需加载；状态总层继续负责简单预览、超分未开放提示和清空确认。画布主入口构建产物由约 1017.19 kB 降至 1007.99 kB（gzip 约 310.00 kB 降至 307.46 kB），任务详情形成约 4.62 kB 独立分包；87 个相关测试文件共 403 项、1,237 个断言、TypeScript 和生产构建通过。本阶段未修改任务查询、日志刷新、取消接口或状态规则，未触发真实任务；风险集中在运行中任务首次打开后的实时日志刷新与取消按钮状态，需真实页面人工回归。代码回滚点：`9fc9437`。
- [x] 第八十阶段将图片与视频预览装配迁入独立 `canvas-project-media-preview.tsx`，只在存在可预览内容时按需加载；视频播放器标题、84vh 高度限制、图片 0.5–12 倍缩放与关闭回调保持不变。独立预览分包约 1.05 kB（gzip 约 0.64 kB），专用测试、TypeScript 和生产构建通过；本阶段未修改媒体地址或播放逻辑，风险集中在首次打开的短暂加载状态，需真实页面人工回归。代码回滚点：`406114d`。
- [x] 第八十一阶段将 `CanvasNodeInfoModal` 从常驻节点工具栏拆到独立 `canvas-node-info-modal.tsx`，项目画布仅在信息目标打开时按需加载，共享只读画布改为直接复用新模块；基础信息、生成信息、图片分类、标签去重/增删与只读权限行为保持不变。节点信息形成约 7.76 kB（gzip 约 2.96 kB）独立分包，专用与工具栏测试、TypeScript 和生产构建通过；风险集中在首次打开加载及共享只读画布的权限提示，需真实页面人工回归。代码回滚点：`28f75ea`。
- [x] 第八十二阶段将节点搜索与分镜脚本编辑器改为仅在入口打开或存在脚本目标时条件挂载并使用异步入口；搜索定位仍展开折叠父容器、清理连线选中并聚焦目标，脚本行/列更新、图片与两类视频生成分流保持不变。节点搜索形成约 8.72 kB（gzip 约 3.44 kB）独立分包；脚本源模块因节点正文仍静态复用，当前仅获得条件挂载收益，未夸大首载减包。三组并发改动集中验收后，画布主入口由约 1007.99 kB 降至 969.28 kB（gzip 约 307.46 kB 降至 296.14 kB），90 个相关测试文件共 409 项、1,285 个断言、TypeScript 和生产构建通过。代码回滚点：`ede86f6`。
- [x] 第八十三阶段并发将快捷键弹窗、活动任务面板、多角度编辑器与表情工作区改为只在实际打开或存在运行任务时按需加载；快捷键 nonce/关闭语义、任务取消与 Agent 避让、节点浮层拖动、角度/表情提交及连线菜单常驻路径保持不变。四类次级工作区分别形成约 8.55 kB、9.55 kB、8.58 kB 和 151.06 kB 独立分包；画布主入口由约 969.28 kB 降至 786.14 kB（gzip 约 296.14 kB 降至 234.99 kB）。93 个相关测试文件共 415 项、1,332 个断言、TypeScript 和生产构建通过。本阶段未修改任务、节点或生成业务规则，未触发真实任务；风险集中在四个入口首次打开时的短暂加载，其中角度与表情加载层保留可关闭能力。代码回滚点：`3c909b2`。
- [x] 第八十四阶段并发将小地图、专注模式控制条、文件拖入提示和短剧项目侧栏改为只在对应状态激活时按需加载；缩放控制、素材托盘、活动任务、空画布和顶栏继续常驻。项目章节拖拽常量与轻量类型迁入独立模块，避免上传 hook 静态拉回整个侧栏，真正保留侧栏延迟加载收益。四类界面分别形成约 4.60 kB、3.49 kB、3.18 kB 和 13.68 kB 独立分包；画布主入口由约 786.14 kB 降至 766.31 kB（gzip 约 234.99 kB 降至 230.07 kB）。96 个相关测试文件共 421 项、1,381 个断言、TypeScript 和生产构建通过。本阶段未改变章节拖拽载荷、视口、上传或短剧业务逻辑，未触发真实操作；风险集中在首次打开小地图/专注模式、首次拖入文件和首次进入关联项目时的短暂加载。代码回滚点：`c09328f`。
- [x] 第八十五阶段并发将右键菜单、上传/合并/Agent 写回三类反馈提示与短剧流程引导改为只在实际出现时按需加载；右键菜单加载层保留原点位、Esc 与按钮关闭，反馈轻量入口继续导出 `CanvasUploadStatus`、`TaskDetailItem` 和任务状态文案，短剧空状态与节点内容保持在原模块。三类界面形成约 13.37 kB、6.08 kB 和 3.60 kB 独立分包；画布主入口由约 766.31 kB 降至 748.02 kB（gzip 约 230.07 kB 降至 225.62 kB）。99 个相关测试文件共 429 项、1,447 个断言、TypeScript 和生产构建通过；同时修正任务详情早已迁移后的旧测试路径，57 项本地任务回归全部通过。本阶段未改变菜单动作、反馈状态、Agent 回调或短剧流程逻辑，未触发真实任务；风险集中在首次右键、首次出现反馈与首次展开短剧引导时的短暂加载。代码回滚点：`85641b0`。
- [x] 第八十六阶段并发将普通节点设置、配置编排器、三类空画布入口和多选工具内容改为仅在有对应目标时按需加载；节点面板分流与全部生成/配置回调、自由/短剧/关联项目空态路由、首章插入与多选工具六类计数及操作保持不变。普通面板、配置编排、空态与多选内容分别形成约 61.74 kB、11.76 kB、7.01 kB 和 1.35 kB 独立分包；画布主入口由约 748.02 kB 降至 647.25 kB（gzip 约 225.62 kB 降至 197.47 kB）。102 个相关测试文件共 437 项、1,532 个断言、TypeScript 和生产构建通过。本阶段未改变节点数据、生成任务、空态创建动作或多选操作逻辑，未触发真实生成；风险集中在首次打开普通/配置面板、首次进入空画布和首次形成有效多选时的短暂加载。代码回滚点：`f45055d`。
- [x] 第八十七阶段并发将单节点悬浮工具栏、图片素材托盘展开内容和主工具栏设置弹窗改为仅在首次出现或打开时按需加载；节点工具全部回调与一次点击语义、素材筛选/拖拽/定位/尺寸及关闭再开状态、工具栏偏好持久化与主 Dock 即时操作保持不变。三类内容分别形成约 11.56 kB、10.92 kB 和 7.04 kB 独立分包；画布主入口由约 647.25 kB 降至 635.06 kB（gzip 约 197.47 kB 降至 193.72 kB）。105 个画布测试文件共 445 项、1,608 个断言、TypeScript 和生产构建通过。本阶段未修改节点、素材、上传、生成或计费逻辑，未触发真实素材写入或付费生成；风险集中在慢网络下首次悬浮节点、首次展开素材托盘和首次打开工具栏设置的短暂局部加载。代码回滚点：`4eef278`。
- [x] 第八十八阶段并发将主工具栏“添加节点”菜单、角色/短剧/分镜/导演/配置六类自定义节点正文，以及 Markdown、SVG、HTML、全景、对比、图表、调色和肖像排查八类扩展节点正文改为按实际入口或节点类型加载；节点创建 hover 与关闭延迟、分镜完整流水线、导演/配置回调和普通文本/图片/视频/音频/绘图路径保持不变。画布主入口由约 635.06 kB 降至 607.73 kB（gzip 约 193.72 kB 降至 186.68 kB）；创建菜单形成约 5.72 kB 分包，自定义节点正文形成约 1.84–32.28 kB 分包，八类扩展正文合计约 22.22 kB，并将图表约 281.03 kB、Markdown 约 190.39 kB 和全景 Three.js 约 725.68 kB 的重依赖延迟到真实使用时。108 个画布测试文件共 454 项、1,705 个断言、TypeScript 和生产构建通过。为保留视频中央按钮的一次点击播放与浏览器用户手势链，`VideoPlayer` 有意继续走同步路径；风险集中在同屏首次出现多种自定义/扩展节点时的并发分包加载。代码回滚点：`641eb09`。
- [x] 第八十九阶段完成 Agent 侧栏与主工具栏的上线前集中验收：网站 Agent 首开不再同步加载本机 Agent，首开相关资源约减少 69.93 kB（gzip 约 20.97 kB）；拖动调整侧栏宽度时收起或卸载会清理全局监听并恢复页面光标/文本选择状态。Agent 打开时主工具栏改为上移错层，1024px 和 1440px 下不再与左侧 Dock 或右侧面板重叠，极窄情况仅在 Dock 内部横向滚动。验收脚本同步修正了懒加载就绪误判和泛化 Dock 选择器；隔离的 Chrome P0 E2E 68/68 通过，110 个画布测试文件共 459 项、1,745 个断言、TypeScript 和生产构建通过。风险集中在 1024–1279px 且 Agent 手动拉到极宽时的 Dock 横滚提示裁切，按钮原生 `title` 仍可用；需人工回归浅色/深色主题、Agent 慢网首开、本机模式首次切换、极宽面板与工具栏横向操作。代码回滚点：`51013b8`。
- [x] 第九十阶段在首包并发审计中发现并修复快捷键弹窗的真实状态回放问题：弹窗所有权从会在专注模式卸载的顶部栏迁至始终挂载的项目弹层，状态从历史累计 nonce 改为明确布尔值。现在普通模式和专注模式均可直接按 `?` 打开，退出专注模式不会重放旧请求，弹窗仍只在打开时加载。Chrome P0 E2E 增至 74/74 并覆盖两种模式的打开、关闭、切换与不回放；110 个画布测试文件共 459 项、1,749 个断言、TypeScript 和生产构建通过。顶部栏剩余菜单的预计减包仅 1–2 kB gzip 且会引入首击/焦点风险，本阶段明确不再强拆；需人工回归不同键盘布局下的 `?` 键及专注模式中其他快捷键。代码回滚点：`99490c7`。
- [x] 第九十一阶段暂停结构拆分，修复单选素材后点击真正画布空白仍残留大功能栏的问题：空白点击现在进入完整取消选择流程，同时清理节点/连线选区、节点工具栏、设置面板和 Agent；画布拖移、节点点击及浮层操作不触发该路径。Chrome P0 E2E 新增“选中素材显示工具栏→识别真实空白点→单击后选区与工具栏同时消失”回归并增至 76/76；110 个画布测试文件共 459 项、1,748 个断言、TypeScript 和生产构建通过。代码回滚点：`cf08edb`。
- [x] 第九十二阶段将画布编辑器左上角固定出口从语义含混的“返回首页”调整为“返回画布列表”，目标由 `/` 改为 `/canvas`，图标同步为画布列表；主页仍保留在画布菜单中。新增静态路由门禁并更新真实浏览器导航断言，42 项相关测试、TypeScript、生产构建和 Chrome P0 E2E 76/76 通过。代码回滚点：`283919b`。
- [x] 上线前自动优化边界复核完成：生产构建中画布项目主包约 608.13 kB（gzip 约 186.77 kB），剩余大头为 Leafer 图形引擎、节点/连线控制、上传、生成、历史和视口等常驻核心。上传运行时继续拆分预计仅减少 3.1–3.4 kB gzip，但会扩大拖入/剪贴板用户激活、并发上传顺序、失败重试和节点最新快照的回归面；图片工具运行时预计减少 1.7–2.3 kB gzip，但会新增首次确认、重复点击、加载失败与蒙版取消竞态。基于近期上线优先稳定性，两项不再于本轮强拆；自动可证明的前端改动已收口，余下为真实素材、慢网、跨浏览器、本机 Agent 和付费生成的人工验收清单。
- [ ] 在慢速网络下首次悬浮“添加节点”并将鼠标移入菜单，确认菜单不闪退、分组/项目入口齐全、点击只创建一次且 480ms 延迟关闭正常；再分别打开含角色、画风占位、故事输入、分镜脚本、导演和配置节点的画布，确认节点内加载占位不影响拖动，加载后全部编辑、连线和生成入口与拆分前一致。
- [ ] 分别首次显示 Markdown、SVG、HTML、全景、对比、图表、调色和肖像排查节点，确认各正文仅在节点内短暂加载，图表/Markdown/全景依赖加载后内容正确、错误输入仍局部降级且不会造成画布白屏；视频节点仍保持中央按钮一次点击播放。
- [ ] 在正常网络和浏览器慢速网络下分别首次悬浮文本/图片/视频节点、打开图片素材托盘和工具栏设置；确认节点工具一次点击生效且移入工具栏不闪退，素材标签/搜索/拖拽/定位/高度调整及关闭再开状态保留，设置排序/显隐/重置和关闭均正常，三类加载占位不遮挡或拦截画布。
- [ ] 分别首次打开图片/视频普通设置与配置编排器，再打开自由空画布、短剧空画布、关联项目空画布并形成多选；确认加载层可关闭且不遮挡画布，面板参数/生成/关闭、空态全部入口、多选工具定位与全部动作与拆分前一致。
- [ ] 分别首次打开画布/节点/连线右键菜单，再触发上传进度、视频合并进度、Agent 写回反馈和短剧流程引导；确认加载层不遮挡画布、菜单坐标/动作/Esc 关闭、进度更新、Agent 查看/撤销/关闭与引导步骤跳转与拆分前一致。
- [ ] 分别首次打开小地图、进入专注模式、拖入文件与打开关联短剧项目；确认不遮挡画布、小地图拖动/预览、专注控件、拖入高亮、侧栏宽度和章节拖入画布与拆分前一致。
- [ ] 分别首次打开快捷键面板、启动一个长任务、打开多角度编辑器和表情工作区；确认慢网络下加载提示、关闭、任务取消、浮层拖动和提交回调正常，再次打开命中缓存且无明显等待。
- [ ] 首次打开节点搜索并定位普通、折叠画框和图片批次节点，再首次打开分镜脚本编辑器执行行/列修改与图片/视频分流；确认加载状态可关闭且所有定位、保存和生成入口与拆分前一致。
- [ ] 在项目画布和共享只读画布分别打开文本、图片、视频节点信息；确认生成信息、分类、标签编辑/去重、只读阻止与关闭后再次打开均正常。
- [ ] 分别首次打开图片和视频预览，再关闭并重复打开；确认图片缩放/拖动、视频播放控制、最大高度和关闭回调均正常。
- [ ] 打开排队中、运行中、已完成和失败任务的详情；确认首次等待提示、参数与日志显示、持续刷新、耗时计算和仅运行中任务可取消的规则均正常。
- [ ] 分别首次打开分享、LibTV、TapNow、画风选择和导演模板；确认等待提示后弹窗正常显示，分享前保存仍执行、导入位置不偏移、画风应用状态准确、导演节点仍落在发起位置。
- [ ] 首次打开批量上传弹窗并通过隐藏文件输入直接导入，再以旧紧凑 Agent 兼容入口进入画布；确认上传等待状态、文件过滤、失败保留与无界面自动连接均正常。
- [ ] 分别首次打开个人素材库、项目素材和版本对比；确认等待提示后弹窗正常显示，素材多选与文件夹插入作用域正确，版本定位仍先关闭弹窗再聚焦节点。
- [ ] 分别首次打开字幕、抽帧、视频分段和时间线；确认等待文案正确、取消/保存正常，并在时间线中切换到字幕编辑后确认时间线先关闭、字幕面板正常打开且数据同步。
- [ ] 分别首次打开角色参考、文本编辑、绘图编辑和肖像排查，再关闭并重复打开；确认统一加载状态、原内容恢复、保存/取消、绘图摘要和肖像候选添加均正常。
- [ ] 在图片节点依次首次打开裁剪、标注、蒙版、分割和超分，再重复打开一次；确认首次出现短暂正式加载层，工具可正常确认/取消，第二次打开无明显等待且结果写回与拆分前一致。
- [ ] 分别通过普通项目链接、素材交接链接、`mode=new/recent/choose` 链接和旧 Agent 深链进入画布；确认项目 ID、素材导入、URL 参数消费与 Agent 启动行为均与拆分前一致。
- [ ] 在画布打开期间切换浅色/深色主题、修改默认绘图引擎、完成素材库水合并切换 Agent 连接状态；确认画布主题、素材入口、配置可用性和顶部 Agent 状态仍实时更新。
- [ ] 在短剧功能开启时分别进入未关联空画布、已有流水线画布和已关联项目画布；确认引导仅在未关联且流程活跃时出现，折叠/展开、跳过、完成流程后的自动折叠与拆分前一致。
- [ ] 在正常网络与浏览器 CPU 降速下分别硬刷新空画布和已有项目画布；确认首次渲染持续显示画布占位、无白屏闪烁，客户端挂载后再进入项目加载与实际画布。
- [ ] 分别使用普通画布链接、`mode=new/recent/choose` 自动连接链接和带旧 `agentUrl/agentToken` 的深链进入画布；确认普通入口不自动连接，明确模式正常连接，旧密钥被清理且只在该次显示紧凑状态。
- [ ] 多选包含文本、图片、背板、脚本和生成配置的节点，分别通过选区工具栏与连线快捷入口启动批量连接；确认只显示可用来源、顺序与画布一致，切换选区后预览立即更新。
- [ ] 在短剧功能开启时分别打开未关联项目和已关联项目的画布，再切换关联项目并执行素材归档/刷新；确认项目名、章节、画风、素材与文件夹详情使用当前项目且无过期缓存。
- [ ] 在新建空画布和已保存平移/缩放状态的现有画布中分别刷新，再覆盖拖动、缩放、100%、适应画布、适应选区和窗口变尺寸；确认新画布居中且现有画布保持已保存视口。
- [ ] 在真实画布中覆盖单选节点、多选节点、框选、选中连线、点击空白取消选择与撤销/重做；确认节点与连线选择互斥、工具栏显隐和 Agent 快照选区与拆分前一致。
- [ ] 在一个已有连线和多轮 Agent 会话的真实画布中切换当前会话并刷新；确认节点连线、会话列表、当前会话、撤销/重做和保存恢复与拆分前一致。
- [ ] 切换移动/框选工具、小地图、点阵/线格/纯色背景和图片信息显示，并分别在节点、连线和画布空白处打开右键菜单；确认刷新后持久化项恢复，临时菜单和悬浮目标不残留。
- [ ] 依次打开文本、角色参考、绘图、图片/视频设置、字幕、时间线、脚本、肖像排查、导演台和版本对比；覆盖删除当前节点、清空画布、撤销/重做和切换项目，确认只关闭受影响面板，不残留旧节点编辑器。
- [ ] 依次打开并关闭节点搜索、分享、LibTV/TapNow 导入、画风选择、导演模板和清空确认，再从顶栏与底部工具栏各打开一次快捷键面板；确认各入口互不串状态，关闭后可再次正常打开。
- [ ] 在关联短剧项目的画布中分别选中单个镜头、同章多镜头和跨章多镜头；确认顶部栏的节点数、选中数、章节文案和镜头文案随选区即时更新，取消选择后正确清空。
- [ ] 创建图片→调色/对比/图表等扩展节点连线，连续拖动上下游节点后再增删连线；确认拖动顺滑、上游内容只在关系变化时刷新，断开连线后扩展节点正确回到空状态。
- [ ] 连续执行拖动节点、修改标题、任务结果写回和媒体资源加载，随后撤销/重做并刷新页面；确认所有操作都基于最新节点集合，不出现旧快照覆盖，搜索中的最近编辑顺序正确。
- [ ] 分别在图片、视频和音频节点上使用“替换媒体”，确认文件选择器只展示对应类型，替换只作用于当前节点，标题、位置、连线和选区不变。
- [ ] 在空短剧画布点击“用 Agent 创作”，确认 Agent 以在线模式一次打开并进入电影创作引导；关闭后通过普通 Agent 入口再次打开，确认不重复触发引导。
- [ ] 删除包含本地图片、视频或绘图的节点后撤销，再在新建临时画布执行清空并撤销；确认当前无引用媒体被清理，历史可恢复素材与绘图文件仍能正常显示。
- [ ] 打开 Agent 并选中节点/连线，分别点击节点、连线、浮层和真正画布空白；确认只有空白点击同时清除选择并收起 Agent，其余交互保持面板状态。
- [ ] 多选包含文本、图片、背板、脚本和生成配置的节点后启动批量连接，确认仅文本和图片进入预览，顺序与画布一致，禁用节点不会形成连线。
- [ ] 使用 Agent 提交一条低成本测试生成并在任务运行期间继续编辑画布；确认生成只提交一次、结果写回正确节点、Agent 操作回执和手动生成路径保持一致。
- [ ] 在空自由画布验证上传和文本入口，在空短剧画布验证创建流程、打开 Agent、上传、文本和脚本入口，在关联项目空画布验证首章与素材入口；确认各动作仍进入对应流程。
- [ ] 依次打开文本、字幕、时间线、抽帧、分段、角色参考和绘图节点，再从图片 source、图片 target 及批量连接呼出创建菜单；确认面板目标准确且仅合法单图 source 显示绘图能力。
- [ ] 在关联项目画布点击侧栏“项目画风”，分别验证画风节点已存在和正在同步两种状态；确认存在时准确聚焦，不存在时只提示且不改变当前选区或视口。
- [ ] 在新建的临时画布打开文本、图片或视频设置后执行“清空画布”，确认内容、选区和面板全部关闭；随后撤销，确认节点及绘图内容可恢复且确认框不残留。
- [ ] 打开 Agent 后连续拖动画布、缩放、切换节点选择并发送一条不执行生成的询问；确认面板不卡顿，Agent 读取到最新选区和视口，会话切换后刷新仍保持当前会话。
- [ ] 在生成任务运行期间移动节点、切换选择、缩放视口并继续 Agent 对话，随后等待任务完成；确认结果写回最新节点、会话和当前视口，不出现旧快照覆盖。
- [ ] 在关联短剧项目中切换两种项目画风并刷新画布，确认画风节点只保留一个、标题/提示词/锁定状态同步，未修改画风时刷新不会重复创建或移动节点。
- [ ] 使用本机自动连接链接分别覆盖普通入口和旧深链紧凑入口，再手动打开 Agent、小地图并进入专注模式；确认只在首次进入时收纳，专注模式内重新打开的面板不会自行关闭。
- [ ] 分别新建空画布和刷新一个已移动、已缩放的现有画布；确认空画布首次出现在容器中心，现有画布保持保存位置，拖动 Agent 宽度或窗口大小时不会二次跳回中心。
- [ ] 打开图片设置、时间线或导演台后执行一次撤销和重做，确认临时面板统一关闭，历史内容准确恢复，随后重新打开面板仍能编辑。
- [ ] 删除一个正在打开文本编辑、媒体设置或任务详情的节点，再撤销删除；确认旧面板及时关闭、未删除节点面板不受影响、撤销后的节点和绘图内容仍可恢复。
- [ ] 在画布依次点击多个视频中央播放按钮，播放中分别点击画面空白、控制按钮、进度条并拖移节点；确认画面空白单击暂停，真实控件正常响应，拖移不误触播放或暂停。
- [ ] 在登录态真实画布分别验证文本复制、图片复制、图片/视频地址复制，以及一张具备合法使用权的图片同步到方舟素材库；确认权限拒绝、本地 Blob 回退和重复点击保护文案与拆分前一致。
- [ ] 使用真实 LibTV UUID 和 TapNow 分享链接各导入一次，确认预览、节点/连线顺序、保存刷新恢复及保存失败时的完整回滚与拆分前一致。
- [ ] 分别从个人素材库和项目素材库向画布、时间线插入视频和音频，并混选图片/文本/角色；确认画布落点、文件夹插入位置、时间线回填和不支持类型提示与拆分前一致。
- [ ] 在节点、连线、画布空白和各类浮层上分别打开右键菜单，并覆盖系统图片、外部文本和画布内部节点复制后的粘贴；确认菜单归属、落点和权限拒绝提示与拆分前一致。
- [ ] 从创作页分别将一个图片结果和一个视频结果添加到新画布，确认素材尚未就绪时保持重试参数、素材就绪后只插入一次、刷新后节点保留且地址栏交接参数已清除。
- [ ] 在关联短剧项目的真实画布插入一个项目素材文件夹，拖入图片、视频和文本后刷新；确认归档成功、预览顺序、项目内素材归属、文件夹重命名/换肤同步及打开定位与拆分前一致。
- [ ] 在 768、1024、1440 三档 PC 宽度验证 Agent 初始宽度与拖动范围，切换标准/专业、自动/性能/画质模式并刷新，再完成画布标题保存与取消；确认持久化、面板占位和延迟预加载与拆分前一致。
- [ ] 在真实画布依次单击文本、文件夹、图片、视频、绘图、脚本、肖像排查和生成配置节点；确认独立编辑器与通用设置互不抢占，选择媒体时保留正在编辑的生成配置，视频仍可一次点击播放。
- [ ] 用一条可取消的生成任务验证任务详情、日志刷新和取消确认，再用一条上游成功但资源获取失败的任务验证“重新加载资源”；确认节点状态、任务中心列表与错误提示一致。
- [ ] 在图片、视频、文本和文件夹节点之间快速移动鼠标，覆盖节点到工具栏的移入、拖动节点、打开媒体设置、关闭设置与撤销/重做；确认工具栏不闪烁、不在拖动时误显，设置面板打开期间不与工具栏重叠。
- [ ] 对图片、视频和批次子图分别验证下载、创建副本、删除，调整图片真实尺寸并打开版本对比、图片预览和导演台；确认动作只作用于目标节点，元数据不会覆盖原提示词或任务信息。
- [ ] 为一个生成节点连接多张参考图并通过生成配置节点接收输入，逐项删除参考；确认对应引用标签和输入边同步消失、生成配置主链保留，同一素材连接的其他节点不受影响，刷新后结果一致。
- [ ] 分别对空脚本、有内容脚本、包含多个失败子图的批次根、单个失败子图和普通图片/视频执行重试；确认提示文案、批次加载态、完成后根节点主图和通用任务详情与拆分前一致。
- [ ] 从含富文本的文本节点点击“生成图片”，确认正文被整理为提示词、图片节点出现在右侧且自动打开设置，默认模型/规格/数量正确；空文本仍只提示且不创建节点。
- [ ] 打开旧版与新版肖像排查节点，交换直比输入并刷新，再将一个候选结果添加回画布；确认输入顺序、角色绑定、候选图落位/连线、选中状态和任务进度恢复与拆分前一致。
- [ ] 依次双击脚本、绘图、生成配置、图片、视频和音频节点，确认脚本/绘图只进入独立编辑器，配置节点打开组合器，其余节点打开对应提示词与媒体设置，关闭和拖动行为与拆分前一致。
- [ ] 在画布分别查看角色卡、空画风、故事输入、分镜脚本、导演镜头和普通生成节点；验证分镜增删行/生成/重试/停止/合并、导演预览进入和普通节点生成按钮均与拆分前一致。
- [ ] 分别打开角色参考、富文本、绘图和肖像排查编辑弹窗，修改并保存内容后刷新画布；确认关闭行为、绘图保存提示、富文本标题与正文、肖像候选回填和节点元数据均与拆分前一致。
- [ ] 在同一视频节点依次执行字幕编辑、抽帧、音频提取和视频分段，再打开时间线添加个人素材、项目素材和本地文件并保存；确认字幕轨偏移、弹窗切换、合成结果回填和刷新恢复均与拆分前一致。
- [ ] 在分镜脚本独立编辑器切换可见列、调整行顺序并刷新，再分别使用直接生成和关键帧模式生成图片/视频；确认引用素材、列配置、批次状态和结果节点均与拆分前一致。
- [ ] 打开含场景的 3D 导演台，使用画布图片添加布景并修改镜头后保存、应用和关闭；确认加载占位、图片筛选、草稿恢复、节点删除与持久化提示均与拆分前一致。
- [ ] 打开版本对比并定位任一版本，再分别从个人素材、项目素材和项目文件夹向画布/时间线插入内容；确认弹窗关闭顺序、多选能力、素材落点和时间线作用域均与拆分前一致。
- [ ] 搜索并定位普通节点、折叠画框内节点和收起的图片批次子节点；确认父容器自动展开、目标成为唯一选择、原连线选择清除且视口正确聚焦。
- [ ] 分别打开分享、LibTV 导入、TapNow 导入、项目画风和导演模板，确认关闭/取消、导入预览与应用、分享前保存、画风应用状态及指定落点创建镜头均与拆分前一致。
- [ ] 分别触发批量上传、视频合并和 Agent 写回，确认三类提示的位置、动画、进度、查看改动、撤销与关闭行为均与拆分前一致，多个状态同时出现时互不遮挡。
- [ ] 打开角度、情绪和普通节点设置浮层并拖动对应节点，再从节点连线端点呼出创建菜单；确认浮层跟随、框选时隐藏、脚本/绘图独立编辑器互斥和连线创建禁用理由均与拆分前一致。
- [ ] 多选图片、视频、文本和锁定节点，分别执行对齐、排列、分镜组、参考组、批量连线和视频合并，并在框选/拖动过程中观察工具栏；确认可用数量、禁用状态和显示隐藏均与拆分前一致。
- [ ] 依次悬浮文本、角色卡、图片、视频和音频节点，覆盖字号、信息、角度、情绪、裁剪、抽帧、字幕、时间线、锁定与删除；确认拖动/媒体设置期间隐藏、互斥面板关闭和按钮状态均与拆分前一致。
- [ ] 切换小地图、缩放、适应画布、自动整理和快捷键面板，打开素材托盘并在零选、单选、多选及专注模式间切换；确认活动素材、Dock 显隐和整理后视口适配均与拆分前一致。
- [ ] 在普通/专注模式间切换，展开/收起 Agent，触发运行任务、文件拖入和空画布状态；确认任务面板避让、专注 Dock、缩放与空态入口均与拆分前一致。
- [ ] 分别在普通模式、专注模式 Dock 收起/展开和 Agent 面板开关状态下操作主工具栏；确认新增各类节点、扩展节点、撤销/重做、上传、删除、清空和画布外观的位置与行为均与拆分前一致。
- [ ] 在 Agent 展开/收起时切换标准与专业工作区，再开关专注模式并操作短剧引导步骤；确认模式开关不被 Agent 遮挡，引导条的位置、折叠、跳过和步骤跳转与拆分前一致。
- [ ] 首次打开 Agent 观察懒加载占位，随后调整面板宽度、收起/再展开并切换专注模式；确认面板顶部避让、宽度持久化、收起动画、焦点返回和对话内容均与拆分前一致。
- [ ] 分别在普通模式、未关联项目的短剧模式和已关联项目中打开空画布；确认自由起点、短剧流水线/Agent 起点、项目资产及按 position 选中的首章均与拆分前一致。
- [ ] 分别在画布空白、节点和连线上打开右键菜单，验证当前位置新增/上传/粘贴、单节点复制/删除和单连线删除；确认落点、作用目标和菜单关闭行为与拆分前一致。
- [ ] 覆盖缩放/平移、框选、节点拖移/缩放、连线、批量连线、右键菜单、文件拖入和空白处收起 Agent；确认 Leafer 图形层、DOM 节点层、选区边界和事件优先级与拆分前一致。
- [ ] 在自由画布、已关联短剧项目和专注模式间切换，验证顶栏项目上下文、项目侧栏、章节添加、项目画风定位和资产入口；确认显隐、导航和操作结果与拆分前一致。
- [ ] 通过顶栏、主工具栏和节点替换分别打开上传，导入图片、视频、MP3 与 WAV，再打开节点信息与紧凑本地 Agent；确认文件选择范围、节点回填、信息保存和 Agent 连接与拆分前一致。

## 创作页增量模块化

创作页按“先拆纯界面、后拆状态和生成流程”的顺序逐步缩小路由入口文件。各阶段只迁移组件、类型或可独立验证的纯流程，不修改后端、接口、计费、生成提交顺序、样式或交互逻辑。

- [x] 第一阶段将对话消息、图片/视频结果卡、引用展示、媒体预览和下载操作移入 `creation-message-view.tsx`，共享页面类型移入 `creation-types.ts`；`index.tsx` 从 4520 行降至 4040 行。
- [x] 创作页、画布交接、附件与引用相关 59 项测试全部通过；TypeScript、桥接包和 Vite 生产构建通过。代码回滚点：`18117fe`。
- [x] 登录态真实 `/create` 页面、连续对话入口、输入区和历史对话抽屉加载正常，本次非破坏性浏览器检查新增 warning/error 为 0；现有历史仅含空对话，未执行付费生成。
- [ ] 在真实 `/create` 历史对话中分别打开文本、图片、视频结果，确认时间、引用、预览、下载、重新生成和添加到画布与拆分前一致。
- [x] 第二阶段将历史对话抽屉、历史搜索、连续对话/镜头创作切换和顶部工作区工具栏移入 `creation-workspace-toolbar.tsx`；`index.tsx` 进一步从 4040 行降至 3846 行。69 项相关测试、生产构建通过，真实浏览器完成模式往返切换、历史搜索和空结果验证，新增 warning/error 为 0。代码回滚点：`3b78e47`。
- [x] 第三阶段将底部输入框、参考素材轨道、附件卡片、生成类型/模型/方式/规格/时长/声音配置和提示词优化弹层移入 `creation-composer.tsx`；生成提交、上传执行、会话状态、接口调用和副作用仍由入口协调。`index.tsx` 从 3846 行降至 2703 行；69 项相关测试与生产构建通过，真实 `/create` 完成视频/图片类型往返、图片规格弹层、上传与素材库入口验证，新增 warning/error 为 0。代码回滚点：`846fd1c`。
- [x] 第四阶段将镜头轨道、中央预览、右侧参数检查器、下一镜引导、旧版镜头工具条和镜头结果展示移入 `creation-storyboard-workbench.tsx`，`CreationShot` 共享类型移入 `creation-types.ts`；镜头选择、变体、重试、提交和生成状态仍由入口协调。`index.tsx` 从 2703 行降至 1734 行；69 项相关测试与生产构建通过，真实 `/create` 完成镜头模式切换、空历史和输入区加载验证，新增 warning/error 为 0。当前真实历史仅有空对话，媒体结果与右侧检查器保留为真实数据验收项。代码回滚点：`d5673b5`。
- [x] 第五阶段将文本附件准备、生成任务上下文补全、结果资源化、完成任务投影和会话恢复移入 `creation-task-lifecycle.ts`；入口继续负责提交、状态和接口协调。新增 4 项模块直测，创作页相关 73 项测试、TypeScript 和生产构建通过；真实 `/create` 完成连续对话与镜头创作往返切换，新增 warning/error 为 0，未触发付费生成。`index.tsx` 从 1734 行降至 1542 行。代码回滚点：`cf4cbed`。
- [x] 第六阶段将上传计数与错误状态、本机素材分流、素材库读写与原子选择、外部素材来源和参考图文件替换移入 `use-creation-asset-workflow.ts`；生成提交、计费、会话与任务绑定仍留在入口。新增 3 项素材合并直测，创作页相关 76 项测试、TypeScript 和生产构建通过；真实 `/create` 可打开含 19 个现有素材的素材库、取消关闭并完成视频→图片→视频往返切换，新增 warning/error 为 0，未上传文件或触发生成。`index.tsx` 从 1542 行降至 1318 行。代码回滚点：`b364b8a`。
- [x] 第七阶段将提交前的视频方式判断、时长与素材兼容校验、参考内容拆分、设置快照、图片/视频参数归一化和请求配置准备移入 `creation-submit-preparation.ts`；技能加载、乐观消息、计费任务、结果落库、取消与重试仍由入口按原顺序协调。新增 4 项提交准备直测，创作页相关 80 项测试、TypeScript 和生产构建通过；真实 `/create` 完成图片、文本、视频三种模式往返与视频规格面板开关，新增 warning/error 为 0，未触发付费生成。`index.tsx` 从 1318 行降至 1237 行。代码回滚点：`561e6cb`。
- [x] 第八阶段将文本流式生成、图片批量生成与逐项资源化、视频生成与结果资源化迁入 `creation-generation-executor.ts`；页面仍创建取消信号、绑定任务、维护乐观消息，并统一处理完成、失败、取消和生命周期释放。新增 3 项批量图片任务映射与部分成功文案直测，创作页相关 83 项测试、TypeScript 和生产构建通过；真实 `/create` 完成图片、文本、视频模式往返和视频规格面板开关，新增 warning/error 为 0，未触发付费生成。`index.tsx` 从 1237 行降至 1101 行。代码回滚点：`01ca238`。
- [x] 第九阶段将会话加载与持久化、当前会话切换、历史排序/删除回退和后台任务恢复观察迁入 `use-creation-conversation-workflow.ts`；提交、重试、素材草稿和页面视图状态仍留在入口。新增 3 项历史排序、删除回退和唯一会话重建直测，创作页相关 131 项测试、TypeScript 和生产构建通过；真实 `/create` 完成连续对话/镜头创作往返、历史抽屉打开关闭及刷新后会话恢复，未触发付费生成。`index.tsx` 从 1101 行降至 976 行。代码回滚点：`200a984`。
- [x] 第十阶段将失败任务血缘准备、消息参数恢复、镜头选择、下一镜/变体草稿和提交后的镜头状态迁入 `use-creation-draft-workflow.ts`；提交前校验、乐观消息替换、任务绑定和生成生命周期仍由入口统一收口。新增 3 项参数恢复、失败消息配对和无效重试拦截直测，创作页相关 134 项测试、TypeScript 和生产构建通过；真实 `/create` 验证临时草稿在连续对话与镜头创作之间保留并进入 SC.01 草稿态，随后已清空，未触发付费生成。`index.tsx` 从 976 行降至 844 行。代码回滚点：`3226b79`。
- [x] 第十一阶段将模型选择、输入能力匹配、规格需求、视频方式和分类参考素材上限迁入 `use-creation-model-workflow.ts`；页面只消费派生结果，模型配置、接口参数和生成行为保持不变。新增 3 项规格需求、视频方式素材上限和单图尺寸直测，创作页相关 137 项测试、TypeScript 和生产构建通过；真实 `/create` 完成连续对话/镜头创作往返、视频→图片→文本→视频模式切换与 720P 规格恢复验证，未触发付费生成。`index.tsx` 从 844 行降至 802 行。代码回滚点：`817ab0d`。
- [x] 第十二阶段将创作类型切换时的可用模型回退、视频生成方式切换、首尾帧角色唯一性和方式变化后的帧标记归一化迁入 `use-creation-mode-workflow.ts`；上传期间的切换拦截、模型配置字段、附件顺序和生成提交参数保持不变。新增 3 项模型回退、帧角色初始化/清理和手动角色唯一性直测，创作页相关 140 项测试、TypeScript 和生产构建通过；真实 `/create` 完成视频→图片→视频及自动判断→文生视频→首/尾帧→自动判断往返验证，提交按钮全程禁用，未触发付费生成。`index.tsx` 从 802 行降至 784 行。代码回滚点：`89b1951`。
- [x] 第十三阶段建立 `creation-submission-transaction.ts`，统一用户/助手消息对、重试原位替换、任务与批次绑定、成功/取消/失败终态，并让入口在原副作用顺序下复用这些事务函数；技能准备、生成请求、消费者生命周期和重试锁释放仍保留在入口。新增 4 项消息创建、重试替换、任务绑定和终态收口直测，创作页相关 144 项测试、TypeScript 和生产构建通过；真实 `/create` 完成页面加载、连续对话/镜头创作、历史入口和视频配置检查，提交按钮保持禁用，未触发付费生成。`index.tsx` 从 784 行降至 757 行。代码回滚点：`2b8213f`。
- [x] 第十四阶段将提交入口校验、技能上下文准备、乐观消息写入、生成消费者生命周期、执行器调用、终态收口和重试锁释放迁入 `use-creation-submit-workflow.ts`；保持上传校验与重试会话校验先于消息替换、原会话定向更新、取消信号传递及生命周期只释放一次。新增 3 项提交起始校验优先级直测，创作页相关 147 项测试、TypeScript 和生产构建通过；真实 `/create` 完成连续对话/镜头创作往返、历史抽屉开关和空内容禁用状态验证，未触发付费生成。`index.tsx` 从 757 行降至 642 行。代码回滚点：`3e707dd`。
- [x] 第十五阶段将素材引用增删、清空、排序与图片替换迁入 `use-creation-reference-workflow.ts`，将新建/切换/删除对话、上传保护、草稿重置和历史抽屉状态迁入 `use-creation-workspace-actions.ts`；删除仍只影响对话记录，素材保留，上传期间仍禁止新建或切换。新增 3 项上传保护和删除文案直测，创作页相关 150 项测试、TypeScript 和生产构建通过；真实 `/create` 完成新建空白对话、删除确认、删除后回退及素材库开关验证，测试空白对话已清理，未触发付费生成。`index.tsx` 从 642 行降至 551 行。代码回滚点：`68c784c`。
- [x] 入口模块化收尾审查完成：`index.tsx` 目前只保留页面级状态装配、技能目录加载、滚动跟随、组件参数和三种工作区布局；生成执行、提交事务、会话持久化、素材上传与引用、模型能力、草稿重试和模式切换均已分层。继续拆分剩余 JSX 会增加跨文件传参与回归面，当前停止结构拆分。
- [x] 使用 MiniMax-H3-open 完成真实视频集中回归：480P、5 秒、无声音，预计与实际结算均为 0.525 积分，上游耗时 3 分 29 秒；任务中心、创作结果卡、预览、下载入口、历史切换与刷新恢复、镜头模式、素材入库及画布交接均通过。交接创建的测试画布已清理，创作历史与视频素材保留为回归记录。
- [x] 集中回归发现并修复新交接远程视频首次点击只加载、第二次才播放的问题：仅为唯一选中的视频提前准备鉴权 Blob URL，未选中视频继续保持封面模式。41 个画布测试文件共 280 项、TypeScript、生产构建及真实画布首击播放验证通过。代码回滚点：`ba170d9`。
- [ ] 文本和图片真实结果回归暂未执行：当前环境没有可用文本模型和生图模型；完成模型配置后再覆盖文本流式结果、图片预览/批量结果、下载、重新生成、添加到画布、会话切换与刷新恢复。
