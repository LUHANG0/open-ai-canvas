# PC 创作页对话与镜头工作区专项记录

## 范围与约束

- 仅优化 PC 端 `/create`，移动端沿用原布局。
- 后端、Admin、数据库、API、接口返回、权限和业务规则均未修改。
- `globals.css`、router、services、stores 均保持冻结。
- 连续对话、镜头创作、图片/视频生成、下载、重新生成、添加镜头和添加到画布的原调用链保留。

## 完成内容

- 连续对话与镜头创作共用固定三栏工具条；中间模式开关固定为 `248 × 36px`，状态切换只改变视觉状态。
- 镜头工作区改为工具条、对话/结果双滚动区、时间线、输入区四层正常文档流；长内容只在所属区域滚动。
- 输入区固定在工作区底部行，多行编辑器设置最大高度与内部滚动；时间线不再使用覆盖式定位。
- 消息改为连续阅读流，弱化重复卡片、边框和阴影，并区分正文、媒体结果与折叠明细。
- 图片结果使用固定比例缩略图网格；视频使用受限高度的 `16:9` 紧凑预览；点击预览仍可打开原大图/视频查看。
- 保留下载、重新生成、生成变体、添加镜头、添加到画布及历史入口。
- 1024px 窄 PC 对配置栏、声音按钮和费用提示做自适应收口，避免全局横向溢出。

## 修改文件

- `web/src/pages/create/index.tsx`
- `web/src/pages/create/creation-workspace.css`
- `web/src/components/ai/generation-tool-card.tsx`
- `web/test/create-chat-shot-layout.test.ts`
- `web/package.json`

## 验证记录

| 项目 | 结果 |
| ---- | ---- |
| 模式开关 | 1280px 下两种模式 `x/y/w/h` 完全一致；1024px、1440px 复核无位移 |
| 镜头布局 | 对话/结果区、时间线、输入区边界互不重叠，页面本身无纵向或横向溢出 |
| 1024px 窄 PC | 配置栏、参考素材入口和操作按钮均在视口内 |
| 专项与关联测试 | 45 pass，0 fail；专项文件 6 pass |
| TypeScript | `bun run typecheck` 通过 |
| 生产构建 | `bun run build` 通过，13,510 modules transformed |
| 差异检查 | `git diff --check` 通过；禁改目录无差异 |

候选工作树执行全量测试时共有 1129 项通过；唯一未执行成功的跨运行时用例源于该隔离工作树缺少 `canvas-agent/node_modules/zod`。同一用例在主工作树基线可通过，最终合并后需在主工作树再次执行完整测试。

## 回滚

- 开始前基线 Tag：`pc-create-chat-layout-r0-20260901`。
- 完成交付后建立 Tag：`pc-create-chat-layout-r1-20260901`。
- 若需撤回，优先对本专项提交执行 `git revert`；不使用覆盖式回滚。

## 已知边界

- 浏览器验收没有实际提交付费生成任务；生成动作及服务端结算不在本次前端专项的修改范围内。
- 项目既有的大 chunk 构建提示仍存在，建议作为独立性能专项处理。
