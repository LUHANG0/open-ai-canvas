# 品牌风格统一：D3 导演台与时间线

## 基线与范围

- 基于干净本地 main `ab0d0a6d` 创建独立 `feat/brand-director-d3`；确认 origin/main 是祖先，保留领先的 102 个本地提交。
- 仅整理正式画布入口的专用导演台、模板/引导、场景导航、检查器、序列器和多轨时间线。沿用公共品牌主题与现有业务协议，不修改模型渠道、账务、鉴权或数据结构。
- 隔离前端 3014、后端 8094，显式数据目录为本工作树 `.local/project-workbench-debug/brand-director-d3`。使用新建假账号、自生成图片、320×180 有声 WebM 和两秒 WAV；不写真实 8080，不使用付费模型。
- 独立上线验收任务使用 3013/8093，先验收稳定模块。D3 先完成必要专项、本地 main 与 3012 集成，然后正式交接组合验收；共享索引与待测项仅在集成时协调追加。

## 已复现的基线问题

1. 导演台和时间线直接读取旧 `canvasThemes`，未消费正式画布的品牌 Theme Provider；导演台常暗色序列器/悬浮控件留在 globals。
2. 390px 手机导演台左栏占 180px，视口仅 210px；时间线工具栏 `overflow:hidden` 裁掉保存与导出，预览的字幕区被挤成窄列。
3. 时间线按 96px/秒绘制，192px 点击实际定位 960ms，而预期为 2000ms。标尺按整条最小 960px 宽度比例映射时长，与片段使用的比例不同。

本轮改造前实际运行 `bun run test:director:e2e`：58/58 通过；包含确定性场景、模型失败/重试、视口恢复、保存失败与关闭保护。这是 DEV 专项基线，不能替代正式入口验收。

实施、正式入口证据、最终验证与回滚演练在本批收尾追加。真实触屏、系统 IME、Safari/Firefox、大场景和长媒体、付费生成与生产部署仍未验证。
