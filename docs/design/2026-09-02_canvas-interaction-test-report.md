# PC 画布交互集中测试报告

日期：2026-09-02  
分支：`fix/canvas-interaction-audit-20260902`

## 结论

本轮 PC 画布前端修复已通过生产构建、267 项画布/时间线自动测试及真实浏览器非破坏性回归。已覆盖主要按钮的可达性、启用条件、撤销边界、设置浮层、媒体按需加载、Agent 布局和大型画布裁剪。后端、Admin、数据库、API、权限和业务规则均未修改。

这份结论不把“按钮存在”当成外部业务成功：真实上传、第三方导入、公开分享变更、本机 Agent 写回和付费生成未在用户真实作品上执行，发布前应使用专门测试数据补测。

## 回滚节点

| 节点 | 提交 / 标签 | 内容 |
| --- | --- | --- |
| 原始基线 | `canvas-audit-baseline-20260902` / `032c576` | 专项检查开始前 |
| 阶段 A | `468b1e1` | 撤销、工具栏恢复、多选可用性、键盘边界 |
| 阶段 B | `1ef68a6` | 底栏避让、媒体热区、统一设置面板 |
| 阶段 C | `ae7e235` | 视口裁剪、封面任务、Agent/工具栏动效 |
| 阶段 D | `f50c431` | 批量上传、统一落位、分享反馈 |
| 阶段 E | `bfd8c5c` | 集中回归发现项与验收记录 |

## 自动检查

- `bun run build`：通过；桥接包、TypeScript 和 Vite 生产构建全部完成。
- 画布与时间线测试：41 个文件，267 项通过，0 失败，768 个断言。
- 覆盖：节点注册、生成能力、引用、连线、批量布局、文件夹、绘图存储、视频播放/提帧/时间线、保存状态、快捷键、Agent 写回校验、性能模式、上传落位与撤销。
- 构建仍提示部分既有大分包超过 500 kB；不阻断发布，但建议后续单列拆包优化，不在本轮交互修复中冒险调整加载边界。

## 浏览器验证

### 真实画布（1280 × 720）

- 页面、返回首页、画布菜单、新建/删除入口、搜索、LibTV/TapNow 菜单、媒体模式、分享、专注模式与 Agent 均可达。
- 添加节点显示文本、图片、视频、音频、绘图、分镜脚本、背板、文件夹、导演台、工作流、上传和素材库入口。
- 工具栏设置显示 9/9 项，“更多画布操作”恢复入口固定禁用隐藏；图片/视频设置能在视口内打开，视频模型显示 720P/1080P 与“生成声音”。
- 39 个逻辑节点的当前视图挂载 34 个节点；25 个视频封面对应 25 张图片，空闲状态完整播放器为 0。退出交互后无残留弹窗、Agent 关闭、播放器卸载、初始撤销禁用。

### 4 节点隔离夹具

- 首次加载撤销/重做均禁用；后台媒体恢复不再制造假历史。
- 节点标题点击编辑、点击其他节点提交、撤销恢复通过。
- 主工具栏删除选中节点后由 4 个变为 3 个，撤销后恢复 4 个。
- 自动整理产生一个撤销步骤；画布底纹切换为点阵并撤销回方格。
- 工具栏恢复入口、搜索空态、添加节点、快捷键、缩放、Agent 呼出与单层动画均通过。

### 324 节点压力夹具

- 等待首屏稳定后只挂载 30 个可见节点。
- Cmd+A 全选后仍挂载 30 个节点，没有因逻辑全选重新挂载全部 324 个节点。
- 多选工具栏可见，排列/连接按完整选择数工作；不适用于纯文本选区的“创建分镜组”正确禁用；Esc 清除多选后工具栏退出。

## 发布前保留项

- 用测试画布手工验证绘图编辑器“删除→撤销→重新打开”内容完整性。
- 实际拖动工具栏项目后取消，确认顺序不保存；验证系统剪贴板与中文输入法组合输入。
- Chrome 与 Safari 下补 1024、1440、1920 三档 PC 宽度；本轮真实浏览器为 1280×720。
- 使用专门测试账号执行真实上传、LibTV/TapNow 导入、分享创建/复制失败/轮换/停止、本机 Agent 写回和付费生成。
