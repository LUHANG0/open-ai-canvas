# PC 画布交互集中测试报告

日期：2026-09-02  
分支：`fix/canvas-interaction-audit-20260902`

## 结论

本轮 PC 画布前端修复已通过生产构建、326 项画布/时间线自动测试及真实浏览器非破坏性回归。已覆盖主要按钮的可达性、启用条件、撤销边界、设置浮层、媒体按需加载、Agent 布局、任务详情刷新和大型画布裁剪。后端、Admin、数据库、API、权限和业务规则均未修改。

这份结论不把“按钮存在”当成外部业务成功：MiniMax 画布付费生成已完成一次真实链路验证；真实上传、第三方导入、公开分享变更和本机 Agent 写回仍应在发布前使用专门测试数据补测。

## 回滚节点

| 节点 | 提交 / 标签 | 内容 |
| --- | --- | --- |
| 原始基线 | `canvas-audit-baseline-20260902` / `032c576` | 专项检查开始前 |
| 阶段 A | `468b1e1` | 撤销、工具栏恢复、多选可用性、键盘边界 |
| 阶段 B | `1ef68a6` | 底栏避让、媒体热区、统一设置面板 |
| 阶段 C | `ae7e235` | 视口裁剪、封面任务、Agent/工具栏动效 |
| 阶段 D | `f50c431` | 批量上传、统一落位、分享反馈 |
| 阶段 E | `bfd8c5c` | 集中回归发现项与验收记录 |
| 阶段 F | `6c313b4` | 浅色主题视频播放按钮可见性 |
| 阶段 G | `6b8bfed` | 用户端生成阶段、取消、恢复与模型异常文案统一 |
| 阶段 H | `256095e` | Agent 展开后支持点击画布空白处收纳 |
| 阶段 I | `69331cb` | Agent 展开后支持 Esc 优先收纳，输入框按键不受影响 |
| 阶段 J | `66ec2da` | 运行中任务详情实时跟随状态、进度与日志更新 |
| 阶段 K | `7192e07` | Agent 面板延迟加载并在画布空闲后预取 |

## 自动检查

- `bun run build`：通过；桥接包、TypeScript 和 Vite 生产构建全部完成。
- 画布与时间线测试：41 个文件，267 项通过，0 失败，768 个断言。
- 生成文案增量回归：生成任务、视频播放与画布交互 3 个文件共 78 项通过，0 失败，395 个断言；TypeScript 和生产构建通过。
- Agent 收纳增量回归：44 个画布相关文件共 324 项通过，0 失败，1,174 个断言；TypeScript 和生产构建通过。
- 最终集中回归：44 个文件共 326 项通过，0 失败，1,185 个断言；TypeScript、桥接包和 Vite 生产构建通过。
- 覆盖：节点注册、生成能力、引用、连线、批量布局、文件夹、绘图存储、视频播放/提帧/时间线、保存状态、快捷键、Agent 写回校验、性能模式、上传落位与撤销。
- Agent 从画布首包拆分并在首屏稳定后预取：画布项目主包由 1,320.48 kB / gzip 399.22 kB 降至 1,168.85 kB / gzip 353.05 kB，gzip 减少约 11.6%；Agent 独立分包为 59.09 kB / gzip 19.74 kB。
- 构建仍提示部分既有大分包超过 500 kB；不阻断发布，后续可继续按页面边界拆分剩余重依赖。

## 浏览器验证

### 真实画布（1280 × 720）

- 页面、返回首页、画布菜单、新建/删除入口、搜索、LibTV/TapNow 菜单、媒体模式、分享、专注模式与 Agent 均可达。
- 添加节点显示文本、图片、视频、音频、绘图、分镜脚本、背板、文件夹、导演台、工作流、上传和素材库入口。
- 工具栏设置显示 9/9 项，“更多画布操作”恢复入口固定禁用隐藏；图片/视频设置能在视口内打开，视频模型显示 720P/1080P 与“生成声音”。
- 39 个逻辑节点的当前视图挂载 34 个节点；25 个视频封面对应 25 张图片，空闲状态完整播放器为 0。退出交互后无残留弹窗、Agent 关闭、播放器卸载、初始撤销禁用。
- 浅色主题下视频封面播放按钮改为不受全局按钮颜色重置影响的固定高对比样式；实测按钮与图标均为白色、背景 `rgba(0, 0, 0, 0.68)`、层级 20，29 个当前视频节点的播放入口全部可见。
- Agent 展开后，点击左侧真实空白区域会取消选择并收纳面板；点击节点时面板保持展开，拖动画布时不会误收纳。真实浏览器完成三条边界验证，控制台无 warning/error。
- Agent 展开后，画布焦点下按 Esc 会优先收纳面板；焦点位于 Agent 输入框时按 Esc 不误关。延迟分包加载、空闲预取和再次呼出均正常，本次验证区间新增 warning/error 为 0。
- 已打开的运行中任务详情复用全局任务订阅实时刷新阶段、进度与日志；关闭或切换任务后会解除订阅并丢弃迟到日志响应。

### MiniMax 真实生成链路

- 画布新建视频节点后可选 `MiniMax-H3-open`，720P、16:9、5 秒任务成功创建、轮询、完成并回写为可播放可下载的画布视频节点。
- 任务 `e132e344f6bd82abcb584e4fee11b66f` 实际扣除 0.875 积分，上游创建 HTTP 200，最终状态 `succeeded`，视频资源 1,677,687 字节。
- 用户端不再直接显示“正在连接上游”等内部阶段；画布、任务中心和工作流测试统一显示“排队中 / 准备生成 / 生成中 / 已完成”。Admin、诊断及画布节点关系的技术术语保留。

### 4 节点隔离夹具

- 首次加载撤销/重做均禁用；后台媒体恢复不再制造假历史。
- 节点标题点击编辑、点击其他节点提交、撤销恢复通过。
- 主工具栏删除选中节点后由 4 个变为 3 个，撤销后恢复 4 个。
- 自动整理产生一个撤销步骤；画布底纹切换为点阵并撤销回方格。
- 工具栏恢复入口、搜索空态、添加节点、快捷键、缩放、Agent 呼出与单层动画均通过。

### 324 节点压力夹具

- 等待首屏稳定后只挂载 30 个可见节点。
- Cmd+A 全选后仍挂载 30 个节点，没有因逻辑全选重新挂载全部 324 个节点。
- 多选工具栏可见，排列/连接按完整选择数工作；不适用于纯文本选区的“创建分镜组”正确禁用；Esc 清除多选后工具栏退出。

## 发布前保留项

- 用测试画布手工验证绘图编辑器“删除→撤销→重新打开”内容完整性。
- 实际拖动工具栏项目后取消，确认顺序不保存；验证系统剪贴板与中文输入法组合输入。
- Chrome 与 Safari 下补 1024、1440、1920 三档 PC 宽度；本轮真实浏览器为 1280×720。
- 使用专门测试账号执行真实上传、LibTV/TapNow 导入、分享创建/复制失败/轮换/停止、本机 Agent 写回和付费生成。
