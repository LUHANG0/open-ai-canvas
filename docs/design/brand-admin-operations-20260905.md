# 品牌风格统一：C3 后台运营与请求明细

## 基线与范围

- 干净本地 main `bda5ebd8`，确认继承 origin/main 并保留领先的 58 个提交。
- 独立工作树 `admin-brand-c3`、分支 `feat/admin-brand-operations`；只处理积分运营、现有兑换码入口、公告和请求明细及私有样式。
- 沿用 C1/C2 后台组件、品牌语义变量和全局加载器；保留账务精度、服务端权限、兑换不可恢复禁用及公告发布结果核对合同。
- 3013 / 8093 用本批独立数据；长期 3012 与真实 8080 不作为写入验收目标。无真实账号、付费生成、外部消息、推送或部署。
- C3 先集成 main 和 3012，再向 D1 正式交接；索引和待测试各自追加并保留对方条目。

## 初始检查

| 证据 | 发现 | 处理路径 |
| --- | --- | --- |
| credit-operations-panel | 读取策略失败后旧字段仍可提交；订单失败只浮条并保留旧筛选数据 | 读取门禁、就地重试、清理旧结果、即时写入锁 |
| logs-page / api-log-detail-drawer | 列表失败保留旧记录，详情查询缺少迟到保护，查询结果与回读失败混淆 | 独立状态和读取序列，保留诊断与服务端脱敏 |
| redemption-codes-panel | 已有创建锁和结果核对，但嵌套确认与父抽屉焦点需要回归 | 保留合同，统一表单密度并验证键盘 |
| admin-announcements-panel | 已有发布、关闭及未知结果保护，没有服务端草稿状态 | 保留实际语义，统一样式和保存反馈 |

## 验证与回滚

实施中；完成后补充实际结果、证据、模块提交及可执行逆序回滚，并在独立工作树演练。代码回滚不撤销已保存业务数据。
