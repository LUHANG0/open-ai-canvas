# 品牌风格统一：B4 技能库与插件中心

## 基线与边界

- 基线 `de9469c2`：本地 main 干净，继承 origin/main，领先远端 45 个提交。独立分支 `feat/brand-skills-plugins`。
- 覆盖 `/skills`、`/plugins`、`/plugins/eagle` 及其详情、安装、编辑和配置；不修改后台插件管理、插件引擎、Agent 或协议。
- 延续共享品牌语义、成熟页面组件和四角加载器；页面样式回到所属文件，保留所有权、收藏、版本、服务端权限及外部集成边界。
- 隔离联调使用 3014 / 8094，数据置于本工作树忽略目录 `.local/project-workbench-debug/b4`；不使用真实账号、密钥、媒体或 Eagle 资料库，不安装第三方软件。
- C2 先集成并更新 3012，收到正式交接后 B4 再基于最新 main 集成。无推送或部署。

## 实施与验证

### 技能库

- 将技能卡片、安装和包文件浏览器的专用规则迁出 `globals.css`，在 `skills-pc.css` 合并同名规则；移除桌面独有色板、装饰网格与渐变，手机与桌面使用相同语义变量。保留共享控件、卡片结构及受控横向分类导航。
- 加载使用共享品牌组件；列表首次读取、筛选结果、真正为空和失败分别显示。范围徽标只记录无搜索/分类过滤的总数，变更后使旧计数失效。
- 详情、文件目录和文件内容各自有重试；详情失败先显示一处错误并禁用业务操作，恢复详情后重新读取目录。请求序号阻止关闭后被迟到详情重新打开。
- 保存和安装在校验前/提交前防重入；保存中禁止修改或关闭，失败保留输入与上传文件。编辑抽屉无修改时禁用保存；未保存确认暂时释放父抽屉焦点圈，关闭确认后恢复。
- 收藏、加入和同步按技能 ID 独立锁定；保留所有者不可移除、他人技能加入/移除、收藏、版本与 GitHub 同步规则。没有调用 AI 起草或外部 GitHub 安装。

技能浏览器证据：隔离账号创建与编辑回读成功；离线保存失败保留草稿，恢复后保存成功；Markdown 上传离线失败保留所选文件，恢复后安装成功；未保存确认 Tab 焦点留在确认框、取消后草稿保留；延迟 700ms 的详情请求在关闭后未重开（确认实际延迟 1 个请求）。详情离线只出现一处错误且操作禁用。完整矩阵与最终验证见下文。

### 插件中心与 Eagle

- 插件卡片、分类导航、筛选与配置跨端使用同一组品牌变量，移除装饰渐变和桌面独有色板；前台卡片规则限制在 `.plugins-page`，保留后台文档消费的样式。
- 明确区分个人开关、平台开放和实际生效；平台管理的模型插件显示“由平台管理”，缺失状态显示“状态待确认”。读取失败保留可查看的文档并禁用配置和开关，不从本地缓存推断为可用。
- 开关按插件 ID 防止并发重复提交，仅采用后端返回状态；失败保持原值。分类计数与列表使用一致的可见权限规则。
- Eagle 配置验证与后端保持一致：仅 HTTP、本机地址、41595 端口，无凭据/查询/额外路径。应用保存到当前浏览器，明确提示连接的是后端所在电脑；读取目录才验证连接。未应用修改可继续编辑或放弃，地址变化使旧目录响应失效。
- Eagle 素材页仅在目录和素材读取成功后显示“已连接”，失败可以重试；未连接或工作中禁止写入。手机显示可展开文件夹树，不再隐藏主要目录导航。

## 验证证据与边界

| 证据 | 结论 | 对应入口 |
| --- | --- | --- |
| [24 项断点与主题记录](evidence/brand-skills-plugins/responsive-evidence.json) | 三页在 390、1023、1024、1440px 的深浅主题均无整页横向溢出，无页面错误提示 | `/skills`、`/plugins`、`/plugins/eagle` |
| [12 项插件交互记录](evidence/brand-skills-plugins/plugin-functional-evidence.json) | 无结果重置、地址校验、配置回读/未保存、开关失败保持原值、三连点单次写入、键盘文档与焦点返回、目录失败禁用及重试全部通过 | 插件列表、配置与详情 |
| [技能加入/收藏变更](evidence/brand-skills-plugins/skill-toggle-evidence.json)、[刷新回读](evidence/brand-skills-plugins/skill-toggle-readback.json) | 已加入和收藏成功，移除/取消成功，刷新后状态一致 | 隔离账号与内置测试技能 |
| 本地截图 `detail-mobile-dark.png`、`editor-light-keyboard.png`、`install-mobile-dark.png`、`skill-save-recovered.png` | 手机详情/安装与桌面编辑可读；离线草稿和所选文件保留；在线恢复写入成功 | 技能详情、编辑、安装 |
| 本地 `eagle-fixture.py`、Eagle 截图 | 41595 上仅运行内存假接口；真实走后端代理读取空库、创建假目录并回读，离线刷新失败后恢复 | Eagle 素材页 |
| `combined-tests.log`、`combined-build.log`、`backend-scoped-tests.log` | C2 组合版本全量 1795 项通过、0 失败，10391 个断言；类型/构建及 7 项预算通过；后端 Skill/Eagle/Plugin 专项通过 | B4 与 C2 组合版本 |

- 前端新增 6 项状态/地址行为测试；B4 专项 20 项通过。后端代码和插件引擎未改。
- 已检查浏览器页面异常列表为空。自动化焦点检查曾因独立标签页隐藏导致 CSS 关闭动画暂停；激活目标标签页后重跑 12 项全部通过，没有为验收修改产品动画。
- 全量测试首次因独立工作树缺少 Agent 的依赖链接失败，补齐现有依赖链接后全部通过；没有改依赖或锁文件。
- 组合构建预算（gzip）：启动 JS 593.9/610.4 KiB，启动 CSS 105.3/125.0 KiB，画布 837.7/859.4 KiB，创作 405.6/415.0 KiB，最大业务入口 181.1/187.5 KiB，最大 JS 725.8/761.7 KiB，最大 CSS 102.9/123.0 KiB。
- 截图、运行日志和隔离脚本归档于主仓库忽略目录 `.local/cache/brand-skills-plugins-20260905`。浏览器交互使用独立会话 `b4-brand`；假接口脚本只用于隔离验收，不应指向日常后端 8080。
- 用户观感确认、其他浏览器、真实 Eagle 文件读写/导入/自动上传、GitHub/ZIP 安装与同步、AI 起草和真实模型调用尚未验收。本批没有使用真实账号/密钥/媒体、安装第三方软件、推送或部署。

## 提交与本地集成

| 模块 | 提交 |
| --- | --- |
| 技能库 | `0c345d47` |
| 插件中心 | `cfda2a5c` |
| Eagle 素材 | `f50fbf17` |

C2 正式交接基线为 `56bb44df`；组合提交 `bd611f73` 完整保留 C2 实现、80 项证据与共享文档。组合测试在该提交通过；本批仍属于本地集成，用户确认入口为长期预览 3012。

## 回滚

保留本批以前的全部成果与 C2 改动。在干净的 main 上按实际实现提交逆序撤销：

```bash
git revert --no-edit f50fbf17 cfda2a5c 0c345d47
```

已经从 `f50fbf17` 建立独立演练工作树，真实执行上面的命令，生成 `99f1ec95`、`36df0302`、`28e3b369` 三个撤销提交；`git diff --exit-code de9469c2 -- web` 返回 0。演练工作树已移除；不重置 main、不撤销 C2 提交、不回退用户数据。将来回滚后应重新执行前端测试、构建与关键路径验收。
