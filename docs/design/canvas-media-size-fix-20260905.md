# 图片视频刷新尺寸回退修复（2026-09-05）

已修复手工放大的图片、视频刷新后缩回默认范围并偏移的问题。代码与回归测试独立提交：`1d8230e4`，修复前基线为 `716272fc`。这次没有修改相邻图形手势的历史合并规则。

## 修改内容

- 媒体恢复函数 `ensureMediaNodeMinimumSize` 在任何几何整理之前，保留 `manualSize`、`freeResize` 或 `locked` 节点的原尺寸和位置。旧标题仍可归一化。未标记的媒体继续执行默认占位、旧图生图比例修正和大小限制。
- 图片完成解码后的自动适配也避让 `locked`，防止恢复时保留的锁定布局随后又被 `onLoad` 改写。
- 共享接口原来只保留 `freeResize`，会过滤掉 `manualSize`、`locked`；公开媒体布局现在保留这两个布尔字段。原有私密字段过滤及资源地址重写规则不变，没有扩大共享权限。

## 自动验证

新增 16 项媒体恢复回归先在旧代码运行：4 项通过、12 项失败，复现手工尺寸、锁定、自由尺寸、手工空节点和图生图恢复问题。修复后，新增用例及相邻专项共 **43 项通过、0 失败、133 个断言**；前端类型检查通过。

覆盖图片/视频的大尺寸、小尺寸和竖向布局，序列化后恢复、旧标题、自动空节点、默认 fit、手工图生图及生成中状态恢复。

后端共享专项 **5 个顶层测试通过**，新增媒体布局测试包含 8 个子场景：图片/视频分别验证手工、锁定、二者同时开启及显式 false。尺寸、位置与布局标记保留，API 密钥、任务 ID、存储键及未知字段仍被过滤。

复验命令（分别在 `web/`、`backend/` 目录运行）：

```sh
bun test test/canvas-media-size-restore.test.ts test/canvas-interaction-system.test.ts test/canvas-node-resize.test.ts test/canvas-generation-layout.test.ts
bun run typecheck
```

```sh
go test ./internal/service -run 'Test(CanvasShareTokenHash|PublicCanvasProject|CanvasResourceID)' -count=1
```

本次没有重复运行此前整套 95 项 Chrome 回归；使用上述专项和下面的真实素材复测验证改动。

## 真实素材复测

继续使用 [原 96 节点测试画布](http://localhost:8888/canvas/eXrhz6-luUXWJxhaj4BgB)，通过鼠标手工拉伸、保存、刷新和播放器按钮操作，完成 **6 项检查，全部通过**：

| 检查 | 结果 |
| --- | --- |
| 视频从 600 宽手工放大 | 最终 840×482.222，`manualSize=true` |
| 大视频保存刷新 | 仍为 840×482.222，位置 `(1333.760, -446.222)` 保持 |
| 大视频刷新后播放 | 解码尺寸 864×496，采样时播放时间 0.330 秒、12 帧 |
| 高清图片从 600 宽手工放大 | 最终 840×560，原图解码尺寸 6582×4388 |
| 大图片保存刷新、完成解码 | 仍为 840×560，位置 `(-2474.667, 78)` 保持 |
| 整张画布保存刷新 | 96 个节点逐个比较位置、宽高，变化数量 0，容差 0.1 画布单位 |

页面 warning/error 抽查为空。测试结束时两个大媒体节点保留 840 宽，画布已保存、无活动播放器；临时诊断和测试标签页已清理。没有发起生成、删除或编辑原素材。

首次打开测试页时，当前视口仅挂载 26 个节点，但项目数据仍有 96 个；使用“适应画布”定位后继续测试。此视口采样没有计为产品失败。

## 生效范围与未覆盖项

- 普通编辑页已在当前前端开发服务完成真实素材刷新复测。
- 共享链路已通过前端恢复函数及后端投影测试；没有创建公开分享链接，因此不能宣称共享页端到端实测已完成。修复验收阶段未重启后端；随后用户授权更新重启，结果见下方运行记录。
- 修复保留当前保存的几何；历史上已经被回退并保存成小尺寸的节点，需要重新调整，无法从当前值推断先前的手工尺寸。
- 首轮发现的快速相邻操作撤销合并仍待单独修复。冷缓存、长期性能与显存测量仍不在本轮范围。

## 回滚

只撤销本次尺寸恢复修复和对应测试：

```sh
git revert 1d8230e4
```

这不会撤销此前 `34f4cffb` 的交互性能优化，也不删除测试画布或素材。回滚后的前端/后端须运行对应版本才生效。本次未推送、合并或部署。

修复前的失败证据保留在 [真实媒体验收](canvas-real-media-validation-20260905.md) 及其结构化记录中，没有将历史失败改写为通过。

## 用户授权后的本机重启

2026-09-05，用户明确授权重启。检查发现 macOS 管理的后端仍为 `2d9078a1`、schema 6，直接重启旧二进制无法加载修复；本次构建并安装 `6c1fcea4ef37ac31c8be73366a1c2e40e3b45066`（包含 `1d8230e4` 修复），版本 `v1.2.2-preview.5`。

先使用 SQLite 一致性备份副本预演迁移到 schema 8，比较所有既有业务表结构及数据摘要，无变化。确认后台活动任务为 0 后，停下旧服务，保存旧程序、原启动配置和整个数据目录，包括隐藏配置、媒体与迁移标记；备份数据库完整性检查通过。随后显式迁移并 verify，既有业务表结构及数据摘要仍一致，以原 launchd 配置启动新程序，`CANVAS_AUTO_MIGRATE=false` 保持不变。

- 后端直连及前端代理 `/api/health` 均为 HTTP 200、`code=0`、`ready=true`、`started=true`、`draining=false`，数据库/运行环境/schema 检查全通过。
- 运行提交与构建目标一致；schema 当前/预期均为 8。前端开发服务保持运行，未重启。
- 使用原登录会话重新打开测试画布：96 节点（48 图片、48 视频）完整读取；图片 840×560、视频约 840×482.222 仍保留手工尺寸，页面 warning/error 抽查为空。
- 本机备份位于应用数据目录同级 `backups/media-size-restart-20260905-050134/`，内含恢复说明和运行记录，未提交数据库、日志、密钥或媒体到仓库。

回退到原本机旧版本时必须配套恢复旧程序与 schema 6 数据备份，并先另存更新后数据；不能仅更换旧二进制或执行 Git 回滚。本次没有公开发布或改变分享权限。
