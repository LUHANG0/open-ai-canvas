# 品牌风格统一：C2 系统渠道与前台模型

## 基线与范围

- 基线为干净本地 main `de9469c2`，继承 origin/main 并保留领先的 45 个提交。
- 独立工作树 `admin-brand-c2`、分支 `feat/admin-brand-channels-models`；仅修改系统渠道、渠道模型管理、前台模型目录及所属组件与样式。
- 复用 C1 的 AdminPageFrame、AdminDataTable、共享主题与加载器；保留密钥掩码、渠道协议、价格微积分单位、价格版本校验、能力范围、路由和服务端鉴权。
- 隔离验收使用 3013 / 8093 和 Git 忽略的独立数据目录。真实 8080、付费上游、账号与媒体不作为写入目标。
- C2 先集成本地 main 并更新 3012，B4 技能与插件随后集成。共享文档双方追加完整保留，不推送或部署。

## 初始证据与处理路径

| 证据 | 发现 | 处理路径 |
| --- | --- | --- |
| channels-page 的 reload / save | 失败仅浮条；保存后同步失败混入写入失败 | 内容区重试、提交锁、保存和同步反馈分离 |
| channel-model-manager 的 reload / openHistory | 旧列表和历史缺少迟到隔离；失败草稿缺少持久提示 | 读取序列、局部错误、关闭与重试保护 |
| logical-models-page 的 reload / runSimulation | 部分目录未读完仍可新增；模拟结果可能迟到覆盖 | 完整读取门禁、结果与请求隔离、明确模拟边界 |
| admin-ui.css 模型编辑器浅色覆盖 | 局部硬编码色板覆盖共享主题 | 原规则迁回所属样式并消费语义变量 |

实施、验证结果与可执行回滚将在本批完成后写入此记录。
