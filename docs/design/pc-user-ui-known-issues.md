# PC 用户端已知问题

- Tasks 默认只读取 30 条，却允许 20/50/100 的客户端分页；本轮保留行为，不修改后端。
- Projects 搜索与排序只作用于已经无限加载的数据。
- `/home` 当前没有 `RequireAuth`，保持现状。
- `/test-voice-recording` 是生产可达内部原型，保留但不扩展业务。
- 页面滚动所有权已由壳层统一；R7 仍需在长列表、sticky 分类栏和嵌套工作台中做浏览器回归。
- AntD Portal 挂载到 body；用户端专项浮层已逐步私域化，R7 仍需集中检查层级、焦点恢复和暗色主题。
- Canvas 历史全局规则在 1024px 与 PC 私域断点同时命中，当前已有更具体的私域复位保护；全局断点清债延后。
- 当前缺少全用户端 E2E 与视觉基线。
- 本地没有项目和画布数据，相关详情页最终验收需要测试夹具或测试账号数据。
