# 品牌风格统一：D1 短剧项目详情与章节

## 基线与范围

- 基线 `bda5ebd8`，来自本地 main，继承 origin/main 且领先 58 个提交。独立分支 `feat/project-brand-detail`。
- 范围为项目详情框架、概览、章节编辑与导入弹窗、分镜章节定位和现有制作表面；附属资产、画布、设置只整理共享外观。导演台、时间线与自由画布算法保留。
- 并行 C3 先集成，D1 等待正式交接后基于最新 main 验证集成。共享索引和待测清单保留双方条目。
- 隔离前端 3014、后端 8094；显式 CANVAS_BACKEND_DATA_DIR 指向本批忽略目录。不读写真实项目，不使用付费模型。

## 初始发现

- 内容域主题只在桌面启用；章节编辑头、导入头使用装饰渐变，Modal 仍有旧容器选择器。
- 顶栏样式错误依赖页面祖先，而顶栏通过 extension 渲染在页面外；窄屏分镜章节定位被隐藏。
- 章节保存提示笼统，读取失败只提供返回，缺少原位重试。

## 实施

| 提交 | 范围 |
| --- | --- |
| `bd8d5b5d` | 详情顶栏直接消费页面私有样式，移除重复位置文案；跨屏概览和附属表面；项目读取错误提供原位重试及返回 |
| `733b683a` | 章节编辑与导入表面、服务端保存文案、空搜索清空操作、当前章节/格式选中语义；链接与颜色改用公共 Modal，保留富文本选择与焦点 |
| `e8405d6d` | 分镜加载与空态复用品牌组件；章节定位样式从 globals 迁回页面，手机保留定位入口；制作表面使用语义变量 |
| `7eb52e29` | 概览主操作采用公共主按钮；导入与定位弹窗居中、限定视口高度，内容独立滚动 |
| `e4ffdc13` | 章节创建/保存/导入/删除错误回调不返回消息 thenable，避免已失败的操作继续等待消息结束、阻塞再次提交 |

按当前章节加载、草稿存储、生成任务和版本保存逻辑保持原合同。仅浏览器验收涉及的隔离项目被创建或编辑，没有数据迁移、计费、鉴权、调度或几何算法变更。

## 验证

- 专项：`cd web && bun test test/project-*.test.ts test/short-drama-*.test.ts test/workflow-*.test.ts test/pc-ui-detail-polish.test.ts`，81 项通过，384 断言。
- 全量：`cd web && bun test`，1795 项通过，0 失败，10391 断言。首次全量因独立工作树缺 canvas-agent 依赖链接出现 zod 解析错误；复用现有依赖后通过，未改锁文件。
- `cd web && bun run build` 内含类型检查、生产构建和 7 项预算，均通过。
- 真实隔离浏览器：创建章节、重命名、编辑与刷新回读；Markdown 小文件追加两章；离线 PATCH 失败保留正文，恢复后可保存；离开取消保留输入，确认离开后返回恢复本机草稿；空搜索清空；手动新增分镜、必填提示、补齐视频提示词后保存新版本并刷新回读；核心读取 503 后重新加载恢复。
- 首轮深浅主题 1440/390px 覆盖章节、概览、导入和定位；无整页横向溢出，390px 正文滚动区约 246px。定位弹窗 Tab 焦点保持在 Modal，Escape 可关闭。扩展到 1023/1024 临界断点进行稳定截图复核。
- 首轮截图暴露手机弹窗下边界与概览主操作被链接样式覆盖，补充修正采用公共主按钮、居中且受视口约束的弹窗；最终复验另记。

本地 `test:project-delivery:e2e` 使用独立 Chrome、临时空闲端口及两个浏览器生成的微型 WebM，质量门禁、真实指针提交与合成进度通过；随后停在“正在加载本地视频工具”，180 秒没有 ZIP。此项未通过，不以单元 ZIP 测试替代浏览器交付结果，也没有更改 FFmpeg 引擎。

### 未确认边界

真实中文输入法候选窗口、触屏软键盘、其他浏览器、付费模型与真实产物、后台交付及大媒体项目仍按上线完整流程验收。自动化中文填入与富文本操作不能代表真实 IME 验收。专用导演台、时间线和自由画布算法不在本批。

## 回滚

无数据库迁移，回滚代码不撤销已保存内容。原三个模块已在独立演练工作树逆序实际执行 `git revert --no-edit e8405d6d 733b683a bd8d5b5d`，生成 `aedca1c8`、`a63cc3ea`、`9107c914`；`git diff --exit-code bda5ebd8 -- web` 返回 0，演练工作树已清理。

最终已基于 C3 `be8ea47b` 合并为 `2fd39ab7`；索引和 pending-test 的追加冲突逐条保留双方条目。

最终可执行回滚（从最新干净 main 建立独立临时分支后）：

```bash
git revert --no-edit e4ffdc13 7eb52e29 e8405d6d 733b683a bd8d5b5d
```

已从 `e4ffdc13` 的组合树实际执行上述命令，生成 `bfd4802f`、`e6ac9895`、`5e983a75`、`18b67fa3`、`9b2b6e1a`；`git diff --exit-code be8ea47b -- web` 返回 0。C3 前端完整保留，演练工作树已移除。不撤回合并提交、不重置 main、不恢复真实数据库。

## 最终组合验收

- C3 + D1 全量 1795 项、10391 断言、0 失败，类型检查、生产构建和全部预算通过。启动壳 JS 593.9 KiB、CSS 104.1 KiB。
- 390/1023/1024/1440px 深浅主题复核章节、概览、导入与定位；稳定截图修正后无整页横向溢出，定位关闭与分页均在手机视口内。导入内容可滚动，手机实际完成本地文稿确认与追加。
- 概览主按钮实测：深色浅底配深字，浅色深底配白字；不再使用透明底链接呈现主要命令。
- 503 导入故障后按钮退出 loading，文稿仍保留；同一弹窗成功重试，准确记录一条失败 POST 与一条成功 POST。
- 格式按钮真实切换 `aria-pressed`，链接弹窗应用到原选择文字；离线保存、取消离开、保留草稿离开再返回恢复均通过。
- 手机定位搜索并切换到另一实际章节；项目资产、项目画布、项目设置和已统一的项目列表完成只读外观回归。C3 公告/兑换/请求入口组合只读检查无页面异常。
- 后端无业务改动，本批数据只在隔离目录。证据摘要见 [浏览器检查 JSON](evidence/project-brand-d1/checks.json)，完整脚本/日志/截图归档主仓库 `.local/cache/project-brand-d1-20260905`；隔离数据库归档 `.local/project-workbench-debug/project-brand-d1-20260905`。这些归档均不提交凭据或数据库。

本机 FFmpeg 交付 E2E 超时边界仍保留，未宣称已完成媒体导出。

## 本地集成与预览

- `c2d1c3fd` 已快进合入本地 main，brand-launch 同步该提交；C3 与 D1 的索引、待测项和证据均保留。无推送或部署。
- 3012 原进程 89878 保持运行；只读浏览器官网 HTTP 200、标题“帧镜”、0 页面异常，章节/分镜定位/概览与 C3 积分模块均返回本轮代码。未以真实账号执行写入验收。
- 本批 3014/8094 进程按 PID 和工作目录核对后停止；后端正常退出。脚本、日志、截图和隔离数据库已归档上述主仓库忽略目录。依赖链接不提交；完成交接后移除本批工作树及临时分支，长期预览依赖链接保留。
