---
title: v1.2.2-preview.3 发布候选记录
description: 2026-09-03 全量改动冻结、验证、部署与回退记录
---

# v1.2.2-preview.3 发布候选记录

## 候选范围

候选版本：`v1.2.2-preview.3`。

基线提交：`d54f573`。候选从 `develop/yingce-platform` 当前工作区冻结，包含以下已联调能力：

- 创作页对话账号级云同步、离线队列、冲突合并和软删除。
- 网站品牌与登录页自定义、资源上传、管理审计和恢复默认。
- 媒体上传持久化与 HTTP 读取边界收紧。
- 统一模型目录系统报价和协议请求边界拆分。
- 前端全量测试发现、生产构建体积预算和文档入口修复。

该候选会创建独立 Git 分支和提交作为代码回滚锚点。发布标签使用 `v1.2.2-preview.3`，`VERSION` 与 `CHANGELOG.md` 已具有同名版本。

## 自动验证

2026-09-03 在 macOS arm64 本机完成：

- `go test ./...`：后端全部通过。
- `bun test`：前端 232 个文件、1645 项全部通过。
- `bun run build`：TypeScript、Vite 生产构建及全部体积预算通过。
- `npm test && npm run build`：Canvas Agent 共 321 项，316 通过、5 项平台跳过、0 失败，TypeScript 构建通过。
- `docker compose config --quiet`：GHCR 部署配置与源码构建叠加配置均通过。
- 生产 Dockerfile：Backend 与 Web 镜像构建通过。

## 隔离容器验收

使用独立 Compose 项目 `yingce-rc-20260903` 启动 PostgreSQL 17、Redis 7.4、迁移服务、Backend 和 Web，未复用本地或生产数据卷。

验收结果：

- 独立迁移服务成功退出，后端报告 `schema.current=6`、`schema.expected=6`、`schema.ready=true`。
- PostgreSQL、Redis、Backend、Web 均进入 healthy。
- `/api/health/ready`、`/api/public/branding`、`/api/auth/settings` 和网页根入口均返回成功。
- 默认品牌投影完整；新数据库正确识别首个管理员初始化状态。
- 验收结束后临时容器、网络和数据卷已删除。

## 生产发布顺序

1. 以候选提交构建或发布固定版本镜像，不使用 `latest`。
2. 对生产 PostgreSQL 执行 custom-format dump，并备份 Backend 数据目录；校验备份可读与 SHA-256。
3. 保持 PostgreSQL、Redis 运行，停止 Web/Backend，并等待 Backend drain。
4. 使用候选 Backend 镜像独立执行 `migrate-schema up`。
5. 启动 Backend，等待 `/api/health/ready` 连续稳定并确认 schema 6。
6. 启动 Web，验证首页、登录、品牌公开接口、创作对话云同步和资源读取。
7. 保留旧镜像标识、旧 Compose 和本次备份，完成观察窗口后再标记发布成功。

## 回退锚点

- 代码回退：候选提交的父提交 `d54f573`。
- 数据回退：发布前 PostgreSQL dump 与 Backend 数据目录归档；数据库迁移失败或新版本健康检查失败时必须一并恢复，不能只切换旧容器。
- 品牌配置回退：管理后台“品牌与外观 → 恢复默认”，或根据 `branding.*` 管理审计恢复上一修订。
- 详细系统回退状态机见 `docs/content/docs/backend/system-update.mdx`。

## 本机原位部署

2026-09-03 13:38 CST 按用户确认的“只部署本地”边界，原位升级了已有本机实例，没有创建第二套空数据库：

| 项目               | 结果                                                               |
| ------------------ | ------------------------------------------------------------------ |
| 部署代码           | `eea9ff2c91c00a089fecdb1e7c694a29e85344af`                         |
| 应用版本           | `v1.2.2-preview.3`                                                 |
| 前端入口           | `http://127.0.0.1:8888`                                            |
| 后端监听           | `127.0.0.1:8080`                                                   |
| 数据目录           | `.local/project-workbench-debug`                                   |
| Schema             | `5 → 6`，当前 `current=6`、`expected=6`、`ready=true`              |
| 完整备份           | `.local/deploy-backups/20260903-133800-before-eea9ff2`             |
| 备份数据库 SHA-256 | `3d0934c87fc87e407d93ef6febb95161c13a9f6a24dd1a9baff96cf7674f786e` |
| 旧后端 SHA-256     | `8dfe1bdc29075bd0fbfc340e24f193156dacb587fed920e4a4fd74a664b4b4ce` |

备份包含发布前 SQLite 一致性快照、资源、插件/技能包、设置密钥和旧后端二进制。备份数据库已执行 `PRAGMA integrity_check`，结果为 `ok`。

### 部署后验收

- 新后端健康结果中的版本、提交和 schema 均与本记录一致。
- 直连 `8080` 与经 `8888` Vite 代理访问 `/api/health/ready` 均返回 `200`。
- `/api/public/branding` 返回“影策 / YINGCE STUDIO”默认品牌、登录页文案和主题色。
- `/api/creation-conversations` 未登录访问返回 `401`，证明云同步路由已注册且保持身份保护；登录后的创建、冲突合并和软删除由后端与前端自动测试覆盖。
- 真实浏览器打开后进入 `/login?next=%2Fcreate`，页面标题为“影策”，登录表单、品牌标语与核心创作能力正常渲染。
- 前端生产构建、体积预算和 Director Chrome E2E `58/58` 通过。

存活性可重复验证：

```bash
curl -fsS http://127.0.0.1:8888/api/health/ready
curl -fsS http://127.0.0.1:8888/api/public/branding
```

### 本机回退

1. 先精确停止当前监听 `8888` 和 `8080` 的本项目进程，不使用模糊进程名批量终止。
2. 保留当前数据作为故障快照，再将 `.local/deploy-backups/20260903-133800-before-eea9ff2/project-workbench-debug` 整体恢复到 `.local/project-workbench-debug`。
3. 将备份中的 `bin/canvas-backend` 恢复到 `.local/bin/canvas-backend`。
4. 使用部署前环境参数重启后端和 `8888` 前端，然后重新执行上述两个存活性检查。

回退时必须同时恢复数据目录与旧后端，不要在 schema 6 的原数据库上直接运行部署前二进制。

## 未执行的公网发布

本次范围为已有 SQLite 数据的本机实例，未推送发布标签、未切换公网环境、未发布 GHCR 镜像。如后续改为公网发布，必须先解决仓库所有者 `LUHANG0` 与 Compose 镜像命名空间 `ddcat-ai` 不一致，再按“生产发布顺序”执行。
