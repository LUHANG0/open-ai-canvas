---
title: 上线监控与值守手册交付记录
description: 2026-09-04 工作包的基线、差异、验证证据、提交和回滚说明。
---

# 上线监控与值守手册交付记录

## 实施基线

| 项目 | 记录 |
| --- | --- |
| 工作包 | 周日上线监控、值守与发布/回滚手册 |
| 计划上线日 | 2026-09-06 |
| 基线分支 | `main`，开始时与 `origin/main` 一致 |
| 基线 SHA | `2d9078a150608d370eeae7ba5f3ea5584cd0d8b1` |
| 实施分支 | `feat/launch-operations-runbook` |
| 独立 worktree | `/Users/hanglu/Documents/影策/.worktrees/launch-operations-runbook` |
| 手册提交 | `a33d848564295b1d08083515acf43dd0381c9f88` |

## 变更范围

- 新增 `docs/content/docs/backend/launch-operations.mdx`，集中定义周日负责人槽位、检查频率、唯一告警入口、观察指标、阈值、停止条件、四批灰度、问题分级、恢复顺序和客户沟通模板。
- 将手册加入 Backend 文档导航和 AI 文档索引。
- 从在线更新文档链接到新手册，并明确自动回退不覆盖 Backend 数据目录和外部对象存储的边界。
- 没有修改应用代码、Compose、环境变量或外部服务配置；没有接入第三方监控、发送数据、部署或恢复生产数据。

配置差异为“无”。运行环境差异为“无”。本批只改变仓库文档。

## 验证证据

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| JSON 导航 | 通过 | `jq empty docs/content/docs/backend/meta.json` |
| MDX 代码围栏 | 通过 | 统计到 12 个围栏标记，数量成对 |
| Bash/SQL 片段语法 | 通过 | 提取全部 `bash` 代码块后执行 `bash -n` |
| Git 空白错误 | 通过 | `git diff --cached --check` 无输出 |
| 必需章节 | 通过 | 已检查日期、负责人、隐私、客户沟通和“不能只回滚二进制”章节 |
| 文档站构建 | 未执行 | 当前仓库没有文档站 `package.json` 或可调用的构建脚本；没有把静态检查表述为构建通过 |

上述验证只证明文档结构和命令语法，不证明生产环境、账号、数据库、Redis、对象存储或恢复流程已经实测。手册已把非生产恢复演练和实名值守登记列为上线阻断条件。

## 关键发现与处置

| 证据 | 发现 | 处置路径 |
| --- | --- | --- |
| Host Updater 创建 `database.dump` 和 `backend-data.tar`，恢复函数只读取 `database.dump` | 当前自动回退不是完整的数据目录恢复 | 手册将 `/data` 变化列为自动回退限制，并规定人工同时间点恢复顺序 |
| 健康接口只检查数据库、Schema 和启动时运行状态 | Redis 启动后失联不能由 ready 单独发现 | 周日值守单独执行 Redis PING；后续增加第一方健康摘要 |
| 管理分析只有普通成功率、P95 和当前队列数量 | 首次体验、队列年龄、保存和播放尚无完整聚合 | 手册定义精确口径和人工证据；后续按最小字段补第一方聚合 |
| 生产 Compose 只有一组 Web/Backend | 不能原生执行百分比流量灰度 | 入口层无切流能力时，用未公开入口和分批账号；已公开且无法限制时按全量发布处理 |

当前发布结论为“未满足 go 条件”：必需角色尚未实名指派，Backend 数据目录恢复尚未经过隔离环境演练。该结论不会因为健康接口返回 200 自动转为可上线。

## 逐项回滚

本批只含文档，回滚不影响运行服务或数据：

1. 撤销提交 `a33d848564295b1d08083515acf43dd0381c9f88`，删除新增手册并恢复三个导航/交叉链接文件。
2. 若只撤销导航，将 `launch-operations` 从 `docs/content/docs/backend/meta.json` 移除，并删除 `docs/index.md` 与 `system-update.mdx` 的对应链接；保留手册文件不会影响应用运行。
3. 回滚后再次执行 JSON、代码围栏、Bash 片段和 Git 差异检查。

推荐使用 `git revert a33d848564295b1d08083515acf43dd0381c9f88` 保留审计记录，不使用覆盖式回滚。
