# 品牌风格统一：核心创作流程

## 基线与范围

- 基线：`48c054c2`，主仓库开始时干净；确认继承 `origin/main`。
- 临时分支：`feat/brand-core-flow`，预览继续使用 `3012`。
- 本批为 B 的第一部分：创作、任务、素材三个高频页面，沿用 A 批共享主题。
- 保留模型调用、上传与引用、任务状态、费用、取消/重试和云同步合同。后台完整页面、项目库、画布与导演台等后续批次继续独立处理。
- 同时修正上一批已证实在基线失败的画布旧测试断言，不修改画布运行行为。
- 按创作、任务、素材、测试分别提交；联调使用隔离开发数据，不修改生产账号与配置。

## 阶段记录

| 阶段 | 状态 |
| --- | --- |
| 基线与回滚记录 | 已建立 |
| 创作页面 | 进行中 |
| 任务中心 | 进行中 |
| 素材库 | 进行中 |
| 旧断言修正 | 进行中 |
| 浏览器与构建验证 | 待执行 |

## 验证与回滚

完成后记录实际测试、浏览器截图、提交号与限制。各模块可按提交分别 `git revert`；撤回整批时按提交的逆序操作，保留 A 批主题基础。回滚前先检查工作区并保留用户及其他任务修改，不使用强制重置，不回退数据目录。预览更新不代表线上部署。
