# 品牌风格统一：画布库与项目库 B2

- 基线：`736c2c4c`，主仓库干净，确认继承 `origin/main`。
- 实施分支：`feat/brand-library-pages`；预览继续使用 `3012`。
- 范围：画布库、文件夹式作品卡片、项目库、创建与关联弹窗、筛选、批选及加载/空/异常反馈。
- 保留画布编辑器、几何与撤销逻辑、项目生成/导入、同步与删除业务合同。
- 按模块独立提交；浏览器写入验证仅使用隔离本地测试数据。
- 实现及本地验证完成，待用户验收；不代表已部署。

## 入口与本批范围

“画布库”指现有「画布」列表 `/canvas`；“项目库”指导航「短剧创作」下的「短剧项目」列表 `/projects`。本批没有新增入口或改变两者的数据关系：自由画布可以独立使用，也可以关联到短剧项目；项目继续管理章节、画布和素材。

## 实施结果

- 桌面与手机使用同一套搜索、筛选、空态、项目读取失败提示及画布批选操作，移除按 1024px 分成两套 JSX 的旧实现。
- 两页沿用品牌语义变量、黑白主操作、紫色选中态、统一封面与信息层级；长标题完整内容可读取，标题不会在滚动时遮挡搜索栏。
- 画布新建、导入和项目关联增加处理中反馈；自动打开画布复用全局加载器，创建失败可重试。云端同步失败的关联提示明确说明本地已经更新，保留原同步合同。
- 项目卡片的打开链接与删除按钮分离，封面读取失败显示替代图标；章节完成度使用真实进度。项目初次读取失败显示「暂未读取」与中文重试提示，不显示误导性的零项目结论。
- 项目列表样式移到 `web/src/pages/projects/project-library.css`，清除原共享样式中的列表专属规则；保留项目详情样式，并限制其旧手机弹窗规则的作用范围。
- 创建与关联弹窗在手机上保留标题、说明、选择和提交动作，使用可滚动内容区域。画布编辑器、项目制作和后端接口没有重构。

## 验证

- 31 项专项测试、164 个断言通过；完整前端回归 1783 项、253 个文件、10343 个断言通过，0 失败。
- 最后文案和视觉修正后重新执行类型检查、生产构建及 7 项体积预算，全部通过。启动壳 JS 593.6 KiB，启动壳 CSS 110.3 KiB；画布新增 JS 837.7 KiB，创作新增 JS 405.6 KiB，最大业务入口 181.1 KiB，最大 JS 文件 725.8 KiB，最大 CSS 文件 107.9 KiB。
- 浏览器覆盖 390、1023、1024、1440px，明暗主题；没有页面横向溢出，搜索框没有重复，临界断点保留同一套入口。
- 在隔离账号实际创建项目并进入概览，随后删除该临时项目；验证画布多选、清除选择、重命名、加入项目和移出项目，移出后画布与节点仍保留。
- 验证搜索无结果后的重置、长标题、真实 33% 章节进度，以及手机 AI 构思参数的展开和两列排布。没有发起真实模型调用。
- 停止隔离后端，在已登录页面内切换验证项目错误态和画布项目列表警告；本地画布仍展示。恢复服务后通过「重新加载项目」恢复读取。

浏览器联调仅用 `3013` / `8093`。数据库在 `.local/project-workbench-debug/brand-style-20260905`，测试前快照、测试数据清单、截图与验证日志在 `.local/cache/brand-library-20260905`。没有写入正式 `8080` 数据或生产数据库。

用户仍需确认实际观感；真实 AI 生成、ZIP 导入导出、操作系统减少动画设置及其他浏览器需按原上线验收流程检查。故障注入覆盖已登录后的页内请求；整个后端不可用时刷新页面仍使用既有会话恢复逻辑。

## 提交与回滚

| 提交 | 内容 |
| --- | --- |
| `f1bdfae4` | 建立本批基线与记录 |
| `1e330ce4` | 画布列表、卡片、批选、弹窗及等待反馈 |
| `05d3f4d7` | 短剧项目列表与创建界面、样式归属清理 |
| `8d88b30c` | 项目读取失败时的数量与中文提示 |

在干净工作区使用独立回滚分支，按以下顺序撤回实现；记录文件可以保留：

```sh
git switch -c revert/brand-library-pages
git revert 8d88b30c
git revert 05d3f4d7
git revert 1e330ce4
```

已在临时 Git 索引中依次反向应用三个实现提交，验证结果与 `f1bdfae4` 的文件树完全一致；没有改动真实工作区或索引。回滚代码不会撤销项目、画布、媒体或用户操作的数据，不需要数据库迁移。
